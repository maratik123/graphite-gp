# Agent doc rows — verbose bodies

Verbose bodies (writers, lifecycle, special cases) for the `AGENTS.md § Agent Docs` table rows. Grows as conventions accrue; start minimal — AGENTS.md's one-line purposes suffice for now.
