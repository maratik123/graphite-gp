# Unsafe index

Every `unsafe` block in **production** code, with its safety invariant. Kept in sync by `self-review`. The physics core (`gp-core`) targets zero `unsafe`.

| File:line | Unsafe op | Safety invariant |
|-----------|-----------|------------------|
| _(none yet)_ | | |
