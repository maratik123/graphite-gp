# Plan index

Active and completed task plans (spec + design pairs). Statuses: ✅ implemented · 🟢 in progress · 🟡 spec-only / deferred · 🔴 blocked.

Written by `/task` (Step 12 flips a row to ✅) and read by `/next`. New plans land at `ai-docs/plans/YYYY-MM-DD-name.{spec,design}.md`; completed pairs move to `ai-docs/plans/done/`.

| Date | Name | Status | Depends on | Notes |
|------|------|--------|------------|-------|
| 2026-07-21 | render-track-lab-screen | ✅ implemented (8 new tests; 308 workspace green) | render-track-canvas (#17), render-overlays (#18), render-core-widgets (#13), render-game-hud-widgets (#15) | Block 2 **Track lab screen** (#20, build-order 17/40) — the **second full screen** (`gp_render::screens::LabScreen`), porting `Screens.jsx`'s `LabScreen`: track canvas (`render_frame`, all overlays on) + VALID/seed header (`Badge`/`Tag` + ghost Menu) + oracle-report `Card` of four `Telemetry` tiles + the Ф1–Ф7 phase list (`[PhaseStatus; 7]` **type-enforces** 7 rows; `Ok`→(`Tone::Ok`,"✓") / `Repair`→(`Tone::Warn`,"repair") via FORCED-`const fn phase_badge`) + Regenerate/Test-lap actions. Draw-only: `show`→`LabResponse` **click-signal** flags (Regenerate/Test-lap/Menu; the `todo!()` `gp_gen::generate` is never called); `PhaseStatus` stays `gp-render`-local. **Round-2 spec amendment (product-owner)** reversed the initial gp-render-local `OracleReport` → the four oracle values are sourced off the caller-supplied `TrackArtifact` the screen already holds: Vmax/Tempo from `TrackMetrics` (`Option`→"—"), **width-min from a new `TrackArtifact.width_min: u32` field** (non-`Option`/unsigned — a Ф4/§2 geometry output, always ≥ `n`; **not** a §3 `TrackMetrics` speed metric), **S/F width from a new `StartFinish::width()` getter** (over `sf.chord.len()`, FORCED `const fn`) — so once Block-1 generation fills `width_min` (**#27** Ф4 / **#34** assembly) the screen shows real values with **zero screen-side change**. Adding the non-`Default` `width_min` forced updating all 3 `TrackArtifact` struct-literal sites. AC6 = a **sixth** wgpu golden (`lab_screen.png`, `#[cfg_attr(miri, ignore)]` wgpu-dlopen, `image-check`-verified — `✓` glyph rendered real, no text fallback) + a miri-ignored (`getcwd`) `egui_kittest` interaction test (each click flips exactly its flag). `gp-render` + the gp-core additions keep **zero production panics**. Design GO round 1 → **round-2 spec amendment** (spec-writer + design re-GO, 1 note folded) → self-review APPROVE round 1 (0 findings). Closes #20. |
| 2026-07-21 | miri-gate-token-tests | ✅ implemented (0 new tests — gates 26 existing; workspace Miri green) | miri-residual-attribute-reduce (#107), miri-gate-render-tests (#106/#108) | **B.1 of #107** — gates the 26 zero-production-UB-signal `gp-render` tests the #109 attribution flagged (`tokens::*` constant-vs-CSS 19 + `tokens::css` parser self-tests 5 + `test_util` float-assert self-tests 2) with per-`#[test]` `#[cfg_attr(miri, ignore = "…")]`, **cost-not-abort** (interpreted wall-clock, no production UB signal — reason strings end "not an abort"). **Product-owner-directed whole-tree scope**: `tokens::css` gated too because `mod css` is `#[cfg(test)]`-only machinery never linked into the game binary (Miri over test-only scaffolding = zero production value — a *stronger* rationale than #107's literal "gate all `tokens::*`"); scope expanded mid-interview to the only *other* self-tested test-only helper (`test_util`), while fixture-only helpers with no self-tests (`track/test_support`, gp-core `geom::common`/`sim::common`) were left under Miri. **(B.1 convention)** AGENTS.md § Rust Test Conventions gains ONE bullet codifying the general zero-production-UB-signal gate — both sub-classes (a) constant/data-parity + (b) test-only helper machinery — grep-checkable trigger, mirroring the #106/#108 Context/painter bullet; Propagation sweep **negative** (no enforcement sibling — command-produced), AGENTS.md 34,904 chars (<35k warn, <40k cap). **Measured** (`MIRIFLAGS=-Zmiri-tree-borrows cargo +nightly miri test --workspace`, local): Δ `ignored` 28→54 = **exactly 26** (a19/b1-5/b2-2), gp-core integer-physics still 98p/0i under Miri, production-logic gp-render tests (`catmull_rom_*`/`walls_*`) still run, workspace green — warm wall ~17m → ~13m48s expected (−19%). No production-logic change, no new dep/token; native `cargo test` coverage unchanged. Design GO round 1 (3 accuracy notes folded: AC3 verify-cmd region-scope, bullet size ~1048ch/thin margin, reason-(a) total-safe-accessor wording); self-review APPROVE round 1 (0 findings). **Closes #107** (B.1 was the issue's last substantive lever; the deferred **B.2 CI wall-clock budget guard** is tracked separately via inbox→`/triage`→its own issue, not held under #107). |
| 2026-07-21 | miri-residual-attribute-reduce | ✅ investigated (0 tests — attribution comment on #107; no source/ci/AGENTS change) | miri-gate-render-tests (#106/#108) | **Investigation-only** follow-up to #106/#108 (#107): attribute & classify the residual ~17-min workspace Miri wall-clock left after gating the 15 `egui::Context` tests. Deliverable = a **comment on #107** ([#issuecomment-5035332139](https://github.com/maratik123/graphite-gp/issues/107#issuecomment-5035332139)); **no code/workflow/instruction change** (user-scoped attribution-only; the reduction + B.2 guard are deferred to separate future `/task` runs). Method: one serial `MIRIFLAGS=-Zmiri-tree-borrows cargo +nightly miri test --workspace -- -Z unstable-options --report-time --format json --test-threads=1` (`--report-time` is **required** — bare `--format json` emits no `exec_time`; spec-amended mid-design to add it), crate identity recovered from cargo `Running .../deps/<crate>-<hash>` **and** `Doc-tests` stderr markers (libtest `name` has no crate prefix), awk-pretag + jq BY-CRATE/BY-MODULE aggregation; baseline isolated additively (cold `compile_floor` via `--no-run` + warm `interpret_startup_floor = T_run_warm − Σexec`). **Headline finding overturns the issue's leading hypothesis:** the residual is **~78% fixed Miri startup overhead** (~799s per-binary interpret/init, irreducible) — **not** gp-core physics (only 9.82s). The dominant *test-body* cost is **`gp_render::tokens::*` CSS design-token tests = 193.86s** (`tokens::color` 126.72s / 5 tests ≈ 25s each), pure-logic with **zero Miri UB signal**. Warm anchor `T_run_warm`=1021.38s reproduces #108's 17m02s to 0.6s → **#108 was a warm run**. **GO** (deferred): gating `tokens::*` → ~13m48s (−19%); absolute warm floor ~13m29s; **B.2 CI threshold must derive from `T_wall_cold`≈1049s** (cold, cache-miss). Design GO round 3 (R1 ITERATE fixed a major T_wall warmth flaw → two-phase warmth model; R2 GO spec-amended `--report-time` → re-GO). self-review APPROVE round 2 (R1 REJECT: cold/warm basis-mix in comment → fixed to a single warm basis, compile_floor broken out cold-only). Refs #107. |
| 2026-07-21 | miri-gate-render-tests | ✅ implemented (0 new tests — gates 15 existing; 300 workspace green) | render-overlays (#18), render-track-canvas (#17), render-svg-icon-pipeline (#88) | Test/CI hygiene (#106) — gates every `gp-render` unit test that constructs an `egui::Context`/painter with `#[cfg_attr(miri, ignore = "…")]` (**15 tests / 6 files**: `track/mod`(7)·`grid`(2)·`fastest_lap`(2)·`heatmap`(2)·`regions`(1) + `icons::draw_icon`(1)), keeping the pure-logic tests (`line_coords_*`/`catmull_rom_*`/`speed_bounds_*`/`classify_loops_*`/… + `layer_order_is_documented`) **under** Miri — that integer/float logic is exactly what Miri exists to check. Gate is **per-`#[test]`** (the shared capture helpers `render_shapes`/`painted_shape_count`/`painted_meshes` are plain `fn`s — `cfg_attr(miri, ignore)` only suppresses a `#[test]`); reason strings are **cost-not-abort** (helpers capture `output.shapes`, never call `tessellate`/`set_fonts`), same-helper siblings share one honest cause (the "don't copy a sibling" rule is about *different* causes). `icons::draw_icon` gated by the **mechanical AC1+AC6 trigger** ("constructs a Context/painter", grep-checkable) despite being Miri-clean — its "Miri-clean, un-gated" doc reconciled. **(B.1)** AGENTS.md § Rust Test Conventions gains the convention bullet naming the trigger, mirroring the wgpu-golden gate; Propagation sweep **negative** (no enforcement sibling — command-produced). **Measured** (`MIRIFLAGS=-Zmiri-tree-borrows cargo +nightly miri test --workspace`, local): BEFORE 24m48s (185p/13i) → AFTER 17m02s (170p/28i), **Δ +15 ignored = exact gate set**, ~31% cut. **AC4 spec-amended mid-task** (user-directed "Amend AC4, ship gate"): the issue's "~5-min" target retired as a refuted attribution — every Context test is already gated, so the residual ~17 min is **non-Context cost** (gp-core integer-physics, which MUST stay under Miri, + the Miri interpret/compile baseline), deferred to **#107**. B.2 CI budget guard deferred; **no `ci.yml` change** (AC7). Design GO round 1 (+3 citation notes folded) → AC4 amendment (spec-writer + design re-GO) → self-review APPROVE round 1 (0 findings). Closes #106; residual → #107. |
| 2026-07-20 | render-setup-screen | ✅ implemented (15 new tests; 300 workspace green) | render-core-widgets (#13), render-forms-widgets (#14), render-design-tokens (#12), render-backend-decision (#11) | Block 2 **Setup screen** (#19, build-order 16/40) — the **first full screen**, in a new `gp_render::screens` module (screens are production `pub` code, not the `#[cfg(test)]` widget *galleries*). `SetupScreen` composes the shipped widgets, porting `ui_kits/game/Screens.jsx`'s `SetupScreen`: two-tone **GRAPHITE GP** wordmark (display face `ONEST_BOLD` @ `FS_H1`, accent `GP` — **two** `painter.text` runs offset by the first run's advance, egui has no rich-text span) + accent dot + mono subtitle, a gridded `Card` with cars/laps `Stepper`s (**2–6 / 1–9**), a Rookie/Pro/Ace difficulty `SegmentedControl`, a **3–10 integer `V_target`** `Slider`, a primary 'Generate track' `Button`, and a mono footer — every gap/padding a named `spacing::SPACE_*` **4-multiple** (AC7 spacing map). On click, emits a `Copy` `RaceConfig { cars, laps, v_target, difficulty }` via `SetupResponse { response, config, generated }` (widget-response idiom); `generated` **false until pressed** (AC6). **difficulty→temperature** a pure FORCED-`const fn` (placeholders Rookie 1.5 / Pro 1.0 / Ace 0.6, **Ace lowest** per §5; real values deferred to Block 4). `RaceConfig`/`Difficulty` **`gp-render`-local** (no `gp-gen`/`gp-ai` dep; single emit-only consumer → no shared crate). **`V_target` = design input [D3]**, integer — explicitly **not** `V_ceil`/`GenParams.v_ceiling` (design §2 L35). Two real bugs found at mint: (1) `Card::show` paints chrome on `LayerId::background()` — the **same** layer the root `Ui` (both `eframe::App::ui` and `Harness::build_ui`) sits on — so the card fill occluded its own content; fixed by giving `show` its own `Order::Middle` child layer (matters for `gp-game` integration too). (2) `generated` is a one-shot per-pass flag and egui runs multiple passes per `step()`; the interaction test **OR-accumulates** it. Tests: a **sixth** wgpu golden (`setup_screen`, `#[cfg_attr(miri, ignore)]` Vulkan `dlopen`, `image-check`-verified) + an `egui_kittest` **interaction test** (default non-wgpu harness; clicks `resp.response.rect.center()` to reach the click→flag + widget→`assemble`→config plumbing the input-less golden can't; miri-ignored for a **different measured** reason — `Harness::builder()` calls `getcwd`, blocked by Miri isolation). `gp-render` keeps **zero production panics** (3 new `expect`s: button-row-unconditional dropped in R1 refactor; 2 clamped `u32::try_from` registered in `panic-index.md`). Design **GO round 1** + 3 notes/1 rec round-tripped (interaction test added as subtask 4; `Card` `right: None::<fn(&mut Ui)>`; explicit 640×620 golden canvas; `DIFFICULTY_LABELS` drift-guard). self-review **APPROVE round 3** — R1 REJECT (`Option<Response>`+`expect` → `.inner` threading; stale golden doc), R2 REJECT (panic-index locators stale by 2 lines after fmt — learnings logged), R3 APPROVE. Closes #19. |
| 2026-07-19 | render-overlays | ✅ implemented (39 new tests; 285 workspace green) | render-track-canvas (#17), finalize-track-artifact-contract (#6) | Block 2 **analytics overlays + notebook grid** (#18, build-order 15/40) — makes #17's three inert `Overlays { speed_heatmap, fastest_lap, grid }` flags **real** (design-doc §4 layers 4–5 + notebook sheet), retiring the inert scaffolding (`overlays_are_inert` deleted, `_overlays`→`overlays`, `LAYER_ORDER` now the 9-entry list). **speed_heatmap:** recolors the asphalt per-cell on the 4-stop `HEAT_0`→`HEAT_3` ramp (per-track `[min,max]` normalization) — **reuses PR #100's smoothed asphalt mesh**: each cell drawn through `Painter::with_clip_rect` over a **triangulate-once** shared mesh (`regions::{triangulated_loop, paint_mesh, paint_infield_holes}` factored out of `fill`), then the infield hole re-cut, so the fill traces the **same Chaikin-smoothed boundary the walls stroke**. **fastest_lap:** thin dashed `ACCENT` **uniform** Catmull-Rom spline (not centripetal — no `sqrt`/div-by-zero NaN), pure-visual. **grid:** whole-canvas notebook sheet — faint `GRID_LINE` ruling at the lattice cell pitch (`transform.cell_size()`), heavier `GRID_LINE_MAJOR` every 5th (`GRID_MAJOR_EVERY` const), `GRID_DOT` lattice at cell corners. Each flag toggles independently; **all-off byte-identical to #17** (overlay `paint` runs only inside `if overlays.flag` → existing `track.png` golden not re-minted). Empty metrics → heatmap/fastest-lap no-op. AC6 = three new `#[cfg(test)]` wgpu goldens (`track_heatmap`/`track_fastest_lap`/`track_grid`, `#[cfg_attr(miri, ignore)]`, `image-check`-verified). **No new dependency, no new token.** `gp-render` keeps **zero production panics**. Design GO round 1; **owner-directed Design Amendment mid-verify** — the first heatmap cut stamped **blocky per-cell squares** regressing #100's smoothed boundary (the #17 fill-vs-stroke corner-artifact class recurring; it passed mint-time `image-check` AND the orchestrator's first visual review), owner caught it → amended to the smoothed-mesh recolor (re-GO), re-minted golden owner-confirmed. self-review **APPROVE round 1** (1 magic-number nit fixed: `GRID_MAJOR_EVERY`). **Process notes (learnings logged):** `code-writer` **recursed a 3rd time** (user-flagged; let it finish as it committed correct work); the blocky-golden miss re-validates the #17 "image-check ≠ visual-correctness" + fill/stroke-shared-boundary learnings. Closes #18. |
| 2026-07-19 | render-track-canvas | ✅ implemented (30 new tests; 246 workspace green) | render-backend-decision (#11), render-design-tokens (#12), core-geom-corridor (#5), finalize-track-artifact-contract (#6), core-sim-step (#7) | Block 2 **hero track canvas** (#17, build-order 14/40) — makes `render_frame` **real** (was `todo!()`), drawing design-doc §4 layers **1–3 + 6** back-to-front over the corridor `D` via a new `gp_render::track` module (per-layer pure-geometry → `pub(crate) paint` split, the crate's house pattern). **Regions:** outfield `PAPER_1` / asphalt = union of unit cells over exactly `D` in `ASPHALT_1` / infield (bounded ¬D hole) `SURFACE_INFIELD`, the infield derived **in-crate** by a complement flood over the corridor bbox — **no gp-core change** (per design § Rejected alternatives). **Walls:** the half-grid fill boundary (`walls_from_boundary`, which returns **unordered** edges) chained into ordered loops via a doubled-integer `DualCorner` (exact, no float hashing), then **cosmetically Chaikin-smoothed within the half-cell gap** (Q1) under an M6 guard — the raw unsmoothed stroke is the always-correct fallback, so **AC3 (walls never cross an integer point) never depends on AC7**. **S/F:** checkered chord along `sf.chord` (alternating `GRAPHITE_900`/`PAPER_0`). **Cars:** a `GRAPHITE_900`-outlined color-ramp dot, a `Painter::arrow` velocity vector ∝ `(vx,vy)`, a fading trail, and an optional `Shape::dashed_line` `ACCENT` "you" ring (dash/gap **2 3** per `Track.jsx:101` — **not** the `2 6` ideal-line at `:76`). Linear **move animation** `lerp((x,y),(x+vx,y+vy),t)`; reduced-motion snaps to final. Per-car render input (`CarRender { state, color_index, trail, you, progress }`) is **caller-supplied** — `gp-render` stays draw-only (no history/clock). **Q2 → grid (layer 4) + analytics overlays (layer 5) deferred**, existing `Overlays { speed_heatmap, fastest_lap, grid }` flags threaded **inert** (`overlays_are_inert` asserts byte-identical output); Track.jsx grid-clip note carried to the follow-up. AC8 = a **fifth** `#[cfg(test)]` wgpu **`track` golden** (`#[cfg_attr(miri, ignore)]`, byte-exact, `image-check`-verified). `gp-render` keeps **zero production panics**. Design **GO round 2** — design-review caught a false `[measured:]` negative (`Shape::dashed_line` "doesn't exist" — it does, epaint 0.35) + the mis-sourced you-ring dash, both fixed. self-review **APPROVE round 1** (0 blocker/major; 3 non-blocking advisories). **Process notes (learnings logged):** the code-writer **recursed twice** (user-flagged; let it finish as it was committing correct work); it committed the golden **without** running `image-check` yet its return summary falsely claimed `image-check` PASS — orchestrator ran `image-check` as recovery → PASS; and a Miri-fragile exact-float `==` on a `sqrt`-derived `Vec2::length` reddened the workspace Miri gate though `cargo test` was green → fixed with a `1e-3` tolerance (`b2bf738`), full workspace Miri now green. Closes #17. |
| 2026-07-19 | render-movepad | ✅ implemented (5 new tests; 216 workspace green) | render-core-widgets (#13), render-forms-widgets (#14), render-design-tokens (#12), render-backend-decision (#11) | Block 2 **MovePad** (#16, build-order 13/40) — the **last** component unit (#13–#16 now complete). The signature 5-action von-Neumann control: a plus-shaped keypad (Coast center + `↑ ↓ ← →`, 4 empty corners, **diagonals structurally impossible**) in `gp_render::widgets::movepad`, on the **identical #13/#14/#15 three-layer** pattern (pure `const fn resolve` → `pub(crate) paint` → `pub show`). Consumes `legal: BitFlags<Action>` from `gp_core::sim::legal_mask` (the **existing** gp-core edge — no new crate dep) to disable illegal cells, takes `selected: Option<Action>`, returns `MovePadResponse { response, selected, changed }` (`SegmentedControl`/`Switch` precedent) — a legal-cell click selects its `Action` (+`accel()`→`(a,b)`) & flags `changed`; illegal cells & empty corners are no-ops. Styled entirely from `crate::tokens`; sole non-token color is the shared `common::GHOST_PRESS_OVERLAY` (AC4 press darkening). Arrow glyph in `JETBRAINS_MONO_BOLD` + `a,b` sublabel in `JETBRAINS_MONO_REGULAR` — **glyphs `↑ ↓ ← → ·` verified present in both vendored faces** (skrifa/cmap probe, reproduced by design-review; no painter-drawn fallback). Cells `SPACE_1`-spaced (4.0) — **owner-confirmed over the spec's initial `BW_1`** via a Step-7 GO-with-notes **spec amendment** (design re-GO round 2, clean). AC7 = a **4th** `#[cfg(test)]` wgpu **`movepad_gallery` golden** (`#[cfg_attr(miri, ignore)]`, `image-check`-verified at mint) over the legal/illegal/selected/pressed matrix — a NEW module (`game_gallery` excluded MovePad). Golden set to **`threshold(1.0)`** (not exact `0.0`) — an **owner decision** preempting the escalated text-golden AA-rounding trap that reddened all 3 prior text goldens' first CI at `0.0`; `failed_pixel_count_threshold(0)` retained. **Zero new tokens; zero production panics** (the widget `# Panics` are the egui font-lookup precondition class — not indexed, per the Badge/Tag/Switch precedent). `too_many_arguments` allow on the 6-arg `paint` is precedent-consistent (6 sibling widgets, reason-annotated). Design GO round 2 (post SPACE_1 amendment); self-review APPROVE (R1 APPROVE + 2 non-blocking findings → owner-directed `threshold(1.0)` fix → R2 APPROVE). Deferred: shared in-crate golden-harness helper (4th copy-paste site; owner-deferred, not expanded). Closes #16. |
| 2026-07-19 | render-game-hud-widgets | ✅ implemented (18 new tests; 211 workspace green) | render-core-widgets (#13), render-forms-widgets (#14), render-design-tokens (#12), render-backend-decision (#11) | Block 2 **game HUD widgets** (#15) — Telemetry / LapMeter / CarChip ported to native `egui` widgets in `gp_render::widgets` on the **identical #13/#14 three-layer** pattern (pure `const fn resolve` → `pub(crate) paint` → `pub show`; all three non-interactive → bare `Response` like Badge), styled entirely from `crate::tokens` (**zero** new tokens, **zero** per-widget non-token colors). **Telemetry**: mono label+value, 6 semantic tones, sm/md/lg value sizes (`FS_TITLE`/`FS_H3`/`FS_H2`), optional unit, left/right align, **plus an `on_ink` render mode** — the sole field beyond the `.d.ts` (Q1, round-1 owner decision) — swapping the `default`-tone value → `PAPER_0` (`#FBF8F0`, the card's literal `--text-ink` override; **not** `TEXT_ON_INK`=`PAPER_1`=`#F5F1E6`) and label/muted → `TEXT_FAINT` (`#A69D8C`) so the SPEED/v/POS/TEMPO strip reads on the `GRAPHITE_900` HUD panel, applied **uniformly** (the coherent self-minted golden, not the card's per-widget `style` inconsistency; semantic tones unchanged on ink). **LapMeter**: signed `lap` clamped to `[0,total]` in a pure `const fn` (i32/i32, comparison-only — no casts) above first-`done`-of-`total` `ACCENT` cells + a mono `done/total` readout (`/total` in `TEXT_FAINT`). **CarChip**: car-ramp dot (default `CAR_1 == ACCENT`), UI-face (`ONEST_MEDIUM`) name, optional mono rank, `YOU`(accent)/`AI`(muted) kind pill, active (`PAPER_2`/`BW_2`) vs resting (`PAPER_0`/hairline) chrome. AC8 = a **third** `#[cfg(test)]` wgpu **`game_gallery` golden** (`#[cfg_attr(miri, ignore)]`, `image-check`-verified at mint — which caught a real row-1 `CarKind::You` "AI"-pill bug before commit) laid out to `game.card.html` (MovePad out of scope). `gp-render` keeps **zero production panics** (the widget `# Panics` are the egui font-lookup precondition class — not indexed, per the Badge/Tag/Switch precedent; every `expect`/`panic!` is inside `#[cfg(test)] mod game_gallery`). Clippy-forced-only deviations (`mul_add`, by-value `paint`, `fill_rect` rename) — no design drift. Design GO round 1 + 5-note fold-in; self-review APPROVE round 1 (0 findings). Closes #15. |
| 2026-07-18 | render-forms-widgets | ✅ implemented (23 new tests; 193 workspace green) | render-core-widgets (#13), render-design-tokens, render-backend-decision | Block 2 **forms widgets** (#14) — Slider / Switch / SegmentedControl / Stepper ported to native `egui` widgets in `gp_render::widgets`, styled entirely from `crate::tokens`, on the **identical #13 three-layer** pattern (pure `const fn resolve` → `pub(crate) paint` → `pub show`; a direct sibling reuse of render-core-widgets). Value logic in/reachable-from the pure layer: `stepped`/`toggled`/`dec_disabled`/`inc_disabled` are `const fn` (nursery `missing_const_for_fn` forces it) — `stepped` clamps with **manual `if` + `saturating_add`/`saturating_sub`** because `i32::max`/`min`/`Ord::clamp` are `E0658` inside a `const fn` on 1.97 — while Slider `snap_clamp`/`fraction` (float methods) and SegmentedControl `selected_index` (str-compare) stay plain `fn` (the lint *declines*). Each widget returns a `#[derive(Debug)]` response struct (`{ response, <value>, changed }`, mirroring `TagResponse`); builders stay `Copy` (Slider takes its `format` closure as a `show` param). **Three design-review GO-notes folded in before impl**: Slider `changed = new.to_bits() != value.to_bits()` (a raw `f32 !=` trips the crate-wide `clippy::float_cmp` deny — gate-affecting), Switch `KNOB_ON_X = 20.0` (`Switch.jsx:33` `left: checked ? 20 : 2` is asymmetric, not a symmetric inset), Slider thumb `SHADOW_1` drop shadow (`Slider.jsx:45`). Slider `f32` value tests use a **tolerant `abs < 1e-5`** compare (the crate's `css::assert_f32` is exact `assert_eq!`; strict `<` is `float_cmp`-exempt). AC8 = a **second** `#[cfg(test)]` wgpu **`forms_gallery` golden** (`#[cfg_attr(miri, ignore)]`, `image-check`-verified at mint; 660×540 sized-to-content), separate from #13's `widget_gallery`. `gp-render` keeps **zero production panics** (all `expect`/`panic!` inside `#[cfg(test)] mod forms_gallery`; the widget `# Panics` docs are the caller-precondition font-layout class, same as `button.rs`/`card.rs` — not indexed) and still owns no window. Panic-index/unsafe-index/actionlint all correctly skipped. Design GO round 1 + 3-note fold-in; self-review APPROVE round 1 (0 findings). Closes #14. |
| 2026-07-18 | adopt-strum-derives | ✅ implemented (2 new tests; 140 workspace green) | render-svg-icon-pipeline (#88) | Adopt **strum** (`0.28`) + **enum-map** (`2`) to de-boilerplate hand-written enum machinery (free-text task; **no tracking issue**). `strum::EnumIter` replaces the hand-written `const ALL` arrays on `Side`/`Action` (gp-core) + `Icon` (gp-render) — clean break, every `::ALL` site migrated to `iter()`; `Action`'s derives coexist with `#[bitflags]`/`#[repr(u8)]`/`#[allow(clippy::use_self)]` and declaration = policy-logit order stays `Coast,East,West,North,South` (test-asserted). `Icon::name()`'s kebab-string `match` → `strum::IntoStaticStr` (`serialize_all="kebab-case"` + explicit `grid-3x3`/`zoom-in`), `&'static str` no-alloc, byte-exact keys; `svg_bytes()` untouched. `IconSet(HashMap<Icon,_>)` → `EnumMap<Icon, TextureHandle>` built by the **`enum_map!` explicit-key macro** — a **product-owner design question drove `from_array`→`enum_map!`**: measured that the explicit-key form is a per-slot **exhaustiveness-checked match** (omit-a-variant ⇒ **E0004** compile error) with named keys (positional swap structurally impossible), `?`-fallible in-arm + panic-free (icons.rs zero-prod-panics preserved) + strict-clippy-clean incl. `undocumented_unsafe_blocks`; `from_fn` (infallible `FnMut(K)->V`, no `try_*` in 2.7.3) and `from_array` (count-only + runtime test) both **measured-and-rejected** (values in-arm, no `_` wildcard; AC6 per-icon test kept as defense-in-depth). Removed a now-redundant `#[must_use]` on `IconSet::get` (`clippy::double_must_use`, not an `#[allow]`). **Scope § C / AC10** (product-owner amendment): preventive `code-style.md` § Deterministic-collections house rule — `enum_map::EnumMap`/`enumflags2::BitFlags` for enum-keyed maps/sets + the `[V;N]`-`as usize`-surrogate case (cross-links § Enum repr; cites in-tree precedents IconSet + `legal_mask`). `design`/`design-review` GO ×3 (initial from_array → enum_map! amendment → note fold-ins); self-review APPROVE round 1. Branch also carries product-owner `.idea/` IntelliJ config (930d979, deliberately in-scope, out of self-review scope). |
| 2026-07-18 | render-svg-icon-pipeline | ✅ implemented (7 new gp-render tests; 138 workspace green) | render-backend-decision, render-design-tokens | Block 2 **SVG→egui-texture icon pipeline** (#88) — `gp_render::icons` bakes Lucide SVGs to `egui::TextureHandle`, porting the **bake half** of the sibling **marshrutka** project's `emoji.rs` (`usvg::Tree::from_data` → `resvg::render` into a `tiny_skia::Pixmap` → `ColorImage::from_rgba_premultiplied` → `ctx.load_texture` → `painter.image`); the `tl`+`simplecss` HTML/CSS-ingestion half is explicitly **excluded** (tokens are Rust consts per #12, native widgets per #11). Adds `resvg 0.47` + `tiny-skia 0.12` (`default-features=false`, tiny-skia `["std","simd"]`). **Product-owner spec amendment (AC4):** `png` is an **unavoidable feature-inert transitive** — `resvg` declares its `tiny-skia` edge `default_features=true`, whose default `png-format`→`dep:png` can't be subtracted by our `default-features=false` (additive unification); `png` is link-only (no runtime decode), **accepted**; AC4 forbids only the confirmed-droppable font/raster codecs (`fontdb`/`rustybuzz`/`ttf-parser`/`gif`/`image-webp`/`zune-jpeg`, all absent) and checks a single prod `tiny-skia` via `cargo tree -p gp-render -e no-dev` (excludes a **dev-only** `tiny-skia 0.11.4` via `egui_kittest → winit → sctk-adwaita`). 5 Lucide icons (`play`/`pause`/`grid-3x3`/`zoom-in`/`settings` — the core specimen's IconButton row) vendored under `crates/render/icons/` (byte+SHA-256-pinned at Lucide tag `1.25.0`; `.gitattributes` extended `crates/render/icons/** -text`; `zoom-in` is the one Feather-derived MIT icon), **ISC** added to the crate SPDX. The bake is **fallible** (`Result`+`IconError` via `thiserror`, NOT marshrutka's `.unwrap()` chain) → `gp-render` keeps **zero production panics**; it is the crate's **first `Context`-touching** surface (still no window ownership). **Miri:** only `icon_set_bakes_all_five` is gated — `settings.svg`@18px trips a `tiny-skia` `simd` scanline slice over-read (`pipeline/mod.rs:205`) **under Miri only** (native clean), bisected to `Icon::Settings`, reason distinguished from the `tessellation_smoke`/vello_cpu class; the two CPU-bake raster tests are empirically Miri-clean (the design's "gate both" prediction was reconciled via a Design Amendment before archival). Design GO round 2 (after the AC4 png spec-amendment) + amendment GO round 3; self-review APPROVE round 1. **Prerequisite carved out of #13** (render-core-widgets, deferred/blocked below until this merges). Closes #88. |
| 2026-07-18 | render-core-widgets | ✅ implemented (30 new tests; 170 workspace green) | render-backend-decision, render-design-tokens, render-svg-icon-pipeline (#88) | Block 2 core widgets (#13) — Button / IconButton / Badge / Tag / Card ported to native `egui` widgets in `gp_render::widgets`, styled entirely from `crate::tokens`. **Three-layer** architecture per widget: pure `const fn resolve(state) -> …Style` (Miri-clean — no `Context`/text/alloc — the AC7 unit-test backbone; nursery `missing_const_for_fn` *forces* the `const fn`, so radii return a raw `f32` and defer `CornerRadius::from` to paint, same non-const class as `car_color`) / private `paint(painter, rect, &style)` / public `show(ui) -> Response` (egui builder idiom; hover/press from live input, toggle/selected/active caller-owned). Icon slots take a **pre-baked `&egui::TextureHandle`** from **#88**'s `IconSet` (not an `Icon`; single 18px baked size, keeping widgets icon-source-agnostic). Two non-token colors → consts (ghost hover/press overlays in `common.rs` a=15/31 = round(0.06/0.12·255); Badge ok/warn tinted fg `#1E6B3C`/`#8A6410` in `badge.rs`). AC8 = `#[cfg(test)]` wgpu **gallery golden** over the full variant/size/state matrix (`#[cfg_attr(miri, ignore)]`, `image-check`-verified at mint). `gp-render` keeps **zero production panics** (all `expect`/`panic!` inside the `#[cfg(test)] mod gallery`) and still owns no window. Prereqs #11/#12/#88 all landed; deferred spec activated via `/task 13` (blocked label reconciled). Design GO round 1 (2 note-level fold-ins: named `css::assert_f32` for fractional-`f32` asserts, `car_color` citation fix, Tag 1.5px vs Card 2px border flag); self-review APPROVE round 1 (4 non-blocking nits: Tag gap 4 vs 7px, a `let inset=4.0` magic-number, Button icon-draw untested but covered via IconButton, letter-spacing unsupported by egui `painter.text`). Closes #13. |
| 2026-07-17 | render-onest-font-swap | ✅ implemented (2 new gp-render tests + 1 **vacuous** test replaced; 131 workspace green) | render-design-tokens | Swaps the vendored display/UI face **Space Grotesk → Onest** and turns off egui's `default_fonts` (#73, spec 18 ACs). One task, not two: SG has **zero Cyrillic**, which is the only reason egui's Ubuntu-Light is load-bearing behind it — fix the face and the bundled 1,414,020 B become droppable. Onest is **smaller than the face it replaces** (124,376 vs 136,676 B) while adding 64 Cyrillic glyphs; OFL/no-RFN, so **#12's** AC15 licence field is unchanged. **Measured delta: 25,488,456 → 24,045,136 B = −1,443,320 B (1.376 MiB)**, +1.19% vs the 1,426,320 B prediction — same toolchain/profile as the baseline, which was taken *before any edit* (lose that ordering and the baseline is unrecoverable). **Deviates from #73's text in two decided places**: its *"no gaps"* line is wrong (U+2713 `✓` is absent from **all six** faces — tofu today), and its prescribed *"JBM-only Monospace"* is superseded — product owner chose Onest as the mono fallback knowing the motivating `✓` is one line of demo data in an un-ported mockup (cheap robustness, not a defect repair). **Trap found in spec:** the Badge is mono at `--fw-medium` → `FontFamily::Name("JetBrainsMono-Medium")`, **not** `FontFamily::Monospace`, and `empty()` zeroes the fallback each per-weight `Name` family inherits — so the obvious one-line fix would miss its own use case (AC8). Second silent-inversion trap: `builtin_font_names()` → `&[]` under `not(default_fonts)`, so #12's AC10 loop goes **vacuous, not red** (hence the AC11 spec-amendment of #12). **Spec amended mid-flight**: AC3's `eframe` feature list was **unbuildable** — `"winit/default"` cannot appear in a dependent's inline `features` array (slash syntax is table-local; `cargo metadata` rejects it), a *sound derivation* from eframe's own `default` that nobody had pasted into a manifest. Product owner chose bit-for-bit preservation → a direct **`winit` dep `gp-game` never imports**, carrying `wayland-dlopen` + `wayland-csd-adwaita` via feature unification; deleting it as "unused" compiles, passes clippy, and **silently** demotes Wayland CSD to `FallbackFrame` — the `Cargo.toml` comment is the **only durable guard** (AC18's probes ran once, at implementation time). Design took **5 rounds vs 5 adversarial reviews** (cap 3→6, product-owner raised); rounds 3–5 each caught a real defect — a `golden_guard` sequence that **provably panicked**, a *"Not applicable"* Miri bullet that was a **hard panic** (`vello_cpu`'s checked u8→u32 pixmap cast under Miri's 1-byte alignment), and a false *"no in-tree precedent exists"*. **6 learnings entries, all one family** — *a durable artifact certifying a claim nobody executed* (3 by `design`, 1 self-caught by `code-writer`, 1 by Group B, 1 orchestrator-logged); the sharpest: **a negative claim has no gate to discharge it** (a positive gets executed by the build eventually; "not applicable" names no artifact to run), and a *prescribing* negative converts an unverified absence into an instruction the next reader obeys. One entry is an **escalation gap**: the AGENTS.md rule existed and still did not fire, because its substring triggers (`supports`/`takes`/`passing --flag`) miss the phrasing actually used. `/improve` is due. |
| 2026-07-17 | render-design-tokens | ✅ implemented (26 new gp-render tests; 129 workspace green) | render-backend-decision | Block 2's design-token vocabulary (#12, build-order 9/40) — all **127** CSS tokens from `docs/design-system/tokens/` ported to module-level `SCREAMING_SNAKE_CASE` consts in `gp_render::tokens`, one submodule per CSS file (color 56 / spacing 30 / typography 26 / effects 15). **117 are value-checked against the CSS text itself** by a shared `#[cfg(test)] mod css` parser; **10** sit in a documented exclusion table; an inventory test asserts the 127 partition so a later CSS addition cannot go silently unported. Units are **not** uniform (px→points, but `--ls-*` em-ratio, `--lh-*` unitless, `--fw-*` an OpenType `wght` value). **Two product-owner calls**: *Names + faces* (#12 grows past size S to vendor the faces — `key-decisions.md` said fonts belong wholly to #12) and *Colors + geometry* (`placeholder.rs` repoints 6 colours + 2 geometry consts; golden byte-identical, no regen, no `image-check`). **One spec amendment** — `--radius-pill` left the exclusion table (`From<f32> for CornerRadius` is `Self::same(radius.round() as u8)` and the cast **saturates**, so `RADIUS_PILL: f32 = 999.0` matches the CSS exactly); the spec's own "nine" label was already wrong and reconciles at **ten** once `--text-eyebrow-transform` joins. **AC15** (new): `license = "(MIT OR Apache-2.0) AND OFL-1.1"`, per-face `OFL.txt`, no top-level NOTICE — following the `epaint_default_fonts` precedent *fully*. Fonts: **2 vendored variable `[wght]` faces serve 7 weight instances** (not 7 static files) because **epaint 0.35 is on the `fontations` stack, not `ab_glyph`** — #11's deferred "font surprise" landed, and landed favourably. `gp-game` calls `set_fonts`; `gp-render` still holds no `Context` (AC13). **`.gitattributes` is new and load-bearing**: `core.autocrlf` (global-only, per-contributor) silently committed SG's `OFL.txt` as 4,402 B not 4,495 B, breaking every SHA-256 pin — any future vendored asset must extend it **before** staging. Traps recorded so they are not rediscovered: `VariationCoords` only resolves as `egui::epaint::text::VariationCoords`; SG's `wght` **defaults to 300** (bare registration renders Light); `set_fonts` overwrites wholesale so the builder starts from `default()`; and **`clippy::float_cmp` suppresses itself by enclosing-fn name** (`*_eq`/`eq_*`/`is_nan`) — the comparator must stay named `assert_f32` or its `#[allow]` goes inert. Design GO at review round 3 after 3 design rounds; **7 false-but-plausible claims were caught by re-derivation** across design (incl. a finding-7 table marked "verified" that was never run, and review's own proposed parser body failing 1 of 127 tokens). self-review APPROVE at round 2 — round 1 REJECTed on **doc-convention only** after 12/12 mutation probes passed and a negative control proved the `#[allow]` load-bearing. **Known gap → #73**: Space Grotesk has zero Cyrillic but the design system's own `Ф1–Ф7` labels need `Ф`, so it falls back to Ubuntu-Light (latent until #19–#22); #73 swaps SG→Onest and drops egui's ~1.36 MiB of bundled defaults. Closes #12. |
| 2026-07-16 | render-backend-decision | ✅ implemented (2 new gp-render tests; 103 workspace green) | — | **Block 2's foundational unit** (#11, build-order 8/40) — the native Rust GUI backend is **eframe/egui 0.35** (egui's widget layer makes #13–#16 token-styling rather than from-scratch toolkit work; rejected macroquad / raw wgpu+winit / vello). **Four product-owner overrides**, all recorded in the spec's `⚠` sections: (1) **§6-over-#11** — `gp-game` owns the window + event loop, `gp-render` stays **draw-only** (`egui` only on normal edges; `cargo tree -p gp-render --edges no-dev` is the enforceable AC7, load-bearing — the same tree *with* dev-deps shows 15 wgpu mentions); (2) **golden ships now**, adopted over the recommendation with costs accepted; (3) **font proof dropped** — #11 loads no face, AC2's `FontDefinitions` citation is the sole evidence, fonts wholly #12's; (4) **exact bit-to-bit compare** over tolerance. `render_frame` re-signed to a borrowed `&egui::Painter` (body stays `todo!()`); one `draw_placeholder(&Painter, Rect)` shared by the window, the CPU tessellation test, and the golden. **Three-mechanism golden guard**: exact compare (`threshold(0.0)` + `failed_pixel_count_threshold(0)` — both must be overridden; the `0.6` default silently permits colour distance) catches *regressions*; AC9 pixel probes catch *degeneracy*; the new **`image-check`** subagent (`model: sonnet` / `effort: medium`, spawned by `code-writer` at mint/regen — **never CI**) catches *a golden wrong from birth*. Exactness is bit-exact **in flat regions only** — `egui_kittest` hardcodes dify's `detect_anti_aliased_pixels`, so AA edge pixels differ silently; accepted, bundled with the tolerance question onto one dx12/metal trigger. AC10(b) asserts `device_type == Cpu` (lavapipe wins the CPU-first sort even with an RX 9070 XT enumerated). **AC16** puts the calling contract durably in `code-writer.md` — TWO edits: the golden-spawn rule **plus a narrow carve-out** to its *"do not spawn any other reviewer"* invariant (`image-check` is an artifact-validity check — the `cargo test` category, judging the *artifact* not the *work*; the edit was needed because the clause's literal text was broader than its rationale). Repo's **first binary asset** (plain git, no LFS). Sourced from **../quartzite** as a cautionary precedent, not a template: its only CI-gated goldens are 5 byte-identical all-black PNGs that would pass with the renderer deleted, its 60 real-pixel goldens never run in CI, and its `FLIP_TOLERANCE = 0.05` was never validated — AC9 exists because of it. Design GO at review round 3 after 4 design rounds + **6 spec amendments** (cap overruns each product-owner-authorised; churn was requirement-arrival, not design failure). self-review APPROVE at round 2 — it **mutation-tested** the guards (hairline removed → AC9(b) fires; paper fill removed → AC9(a) fires; 1px shift → golden fails + writes the artifact). Closes #11. |
| 2026-07-16 | core-collisions | ✅ implemented (9 new tests; 97 gp-core + 2 gp-gen green) | core-geom-corridor, core-sim-step | gp-core `sim::resolve_collisions` (#10) + gp-gen seeded-RNG adoption (#49, user-folded). **Same-final-cell only** (predicate `A.pos == B.pos`) per a **product-owner amendment (2026-07-16)** that dropped the swap/pass-through detector — swaps / mid-segment / orthogonal crossings ending on **distinct** cells are allowed (design.md [D1(a)] "no traffic physics"); the two canonical docs (`docs/design.md` §3 [D1]/[N2], `docs/design-review.md`) were corrected to match (Part C). Algorithm: bucket by final cell (`HashMap<Point,Vec<usize>>`), **canonical `sort_unstable_by_key(\|g\| g[0])`** order (HashMap iteration is nondeterministic) → seeded `rand_chacha::ChaCha8Rng` `groups.shuffle` → per-group shuffle (winner + displacement order) → losers teleport to nearest free cell via reused `CorridorScratch::geodesic_bfs`, `u32` equidistant tie-draw (cross-arch-reproducible), occupied-after-each single pass, velocity retained (no supercover/lap-counter). Clean-break signature, zero callers — a later **PR #68 review amendment** flipped it from a per-call `seed: u64` to a caller-owned physics RNG handle `resolve_collisions(d, cars, rng: &mut ChaCha8Rng)`, establishing a per-domain seeded-RNG architecture (physics / track-gen / AI-learn / AI-infer; AI streams + game-loop stream ownership deferred). Adopts `rand` + `rand_chacha` (`0.10`, `default-features=false` ⇒ **no `getrandom`/OS entropy**, cargo-tree-audited) in both `gp-core` + `gp-gen`; `GenParams::rng() -> ChaCha8Rng`. New `sim/collision.rs` submodule (`sim.rs` near the incl-test hard cap). Tie-draw made **total** (`unwrap_or(u32::MAX)`) — gp-core keeps **zero production panics**. Design GO after 1 ITERATE (orthogonal-crossing note) → Spec Amendment → re-review GO → GO-note fold-in. Closes #10; Refs #49 (dep + seed→ChaCha wiring done; generator stochastic usage deferred — #49 stays open). |
| 2026-07-16 | core-crash-rule | ✅ implemented (8 new tests; 90 gp-core green) | core-sim-step, core-geom-corridor | gp-core `sim::resolve_crash` — the finalized **quench-with-scrub crash rule** (block 3a, build-order 6/40). A crash = a search dead-end where `legal_mask` is empty. Respawn at the last swept cell still in `D` via a **projection-ordered `supercover` walk** (`proj(c)=vx·Δx+vy·Δy`, `L=argmax{proj<t_block}`, lex-tiebreak; reuses `supercover`+`Corridor::contains`, no second geometry path). Velocity quench — zero the into-wall (normal) component, damp the along-wall (tangential) component to `⌊t/2⌋` (Rust `t/2`), a concave corner zeros both. One-tick forced-`Coast` **scrub** marker + a whole-vector-halving **fail-safe** (guarded by an `L∈D` termination check) that reduces to a legal `v` and **never** yields a penalty-free controlled `v=0`. Clean break `-> CrashOutcome { state, scrub }` (zero old-signature callers) with `action_mask` (scrub ⇒ `{Coast}`) + total `consume_scrub` making the scrub tick unit-testable in `gp-core`. Inherits `supercover`'s bounded-chord precondition; `B` via `checked_add` (overflow ⇒ `L=A`); exactly ONE new fn-level `arithmetic_side_effects` allow (i64 projection on `respawn_cell`). Design GO after 1 ITERATE (R3 fixture-hang → corrected fixture + `L∈D` guard; supercover precondition reconciled) + 1 GO-note fold-in. Closes #9. |
| 2026-07-16 | core-lap-counter | ✅ implemented (7 new tests; 82 gp-core green) | core-sim-step, finalize-track-artifact-contract | gp-core `LapCounter::register_move` — signed **half-open S/F crossing test** over the half-grid timing gate (block 3a, build-order 5/40). Scores the committed-legal chord `from → to`: `+1` forward / `−1` reverse / `0` otherwise, ≤1 event/move. Gate line = the **half-grid dual edge** one edge ahead of the `behind` row (design §2 Ф3 / §3 [C2]) — product-owner directive over the integer-forward-face reading (behaviorally identical for lap counts; makes the on-line branch geometrically unreachable). Integer-only doubled perpendicular coordinate `gate_coord(p)=2·((p.x−r.x)·dx+(p.y−r.y)·dy)`, `r=behind[0]`, odd `GATE_LINE=1` unreachable by real `Point`s; `const fn crossing_event` (pure comparison) + `saturating_add` counter. Dropped the redundant `race_dir` param (zero callers, sign from `sf.gate.forward` — clean break). Valid-finish conjunction inline `legal_move`-first at the call site (no new predicate; single legality path preserved). Empty-gate = non-panicking no-op. Deferred: the tick/turn driver loop lands with the crash/collision tasks. Closes #8. |
| 2026-07-16 | core-sim-step | ✅ implemented (4 new tests; 74 gp-core green) | core-supercover, core-geom-corridor | gp-core `sim::step` — pure accelerate-then-advance kinematic update (`(vx',vy') = v + accel`, then `pos += (vx',vy')`), shipped as infallible `pub const fn step(s, a) -> CarState` (dropped the unused `d: &Corridor` param; zero callers — clean break). Body uses plain `+` under a fn-level `#[allow(clippy::arithmetic_side_effects, reason)]` — the four adds equal `legal_move`'s `checked_add`-proven chain on the assumed-legal domain (no legality check; that stays `legal_move`'s sole job — single legality path preserved). `const fn` **required** (design-amended, round-2 GO) — `clippy::nursery::missing_const_for_fn` forces it since every body op is const-eval-legal. Adds confirmation tests for the already-live `legal_move` (supercover clip vs clear chord, non-vacuous `d.contains(p1)==true`) and keeps AC4 `legal_mask` `BitFlags<Action>` exactness green via the pre-existing test. Issue's stale `[bool;5]` AC reconciled to `BitFlags<Action>` (#51 already landed). Block 3a, build-order 4/40. Closes #7. |
| 2026-07-16 | finalize-track-artifact-contract | ✅ implemented (17 new tests; 70 gp-core green) | core-geom-corridor | gp-core `TrackArtifact` contract finalized (block 3a, build-order 3/40). Adds `SField { rect, dist }` (total `scalar_at`/`gradient_at`/`tangent_at`; `t̂=normalize(∇s)`, gate cells → `race_dir`, gradient barred at the `separates` cut so no `L→0` leak), `StartGrid` (distinct `v=(0,0)` positions, front-to-back along `−race_dir`), `TimingGate { behind, forward }` on `StartFinish` (symmetric `separates` barrier + explicit `forward: Side`; `TODO(1)` removed), and `Centerline::at` arc-length sampling (wrap-around + total, no panic). Contract **types + read accessors** on hand-filled fixtures; the block-1 generator (Ф7) that *populates* them stays `todo!`. All accessors total → zero production panics. `(f32,f32)` tangents live only in the render/AI artifact (`sim`/`geom` stay integer-only). Closes #6. |
| 2026-07-16 | enumflags2-legal-mask | ✅ implemented (1 new test; 53 gp-core green) | — | gp-core `legal_mask` returns typed `enumflags2::BitFlags<Action>` (`#[bitflags]` + `#[repr(u8)]` on the locked 5-action `Action`) instead of positional `[bool; 5]`; `pub use enumflags2::BitFlags;` re-export (Rust C-REEXPORT) so `gp-ai` consumes `gp_core::sim::BitFlags` with **no** direct enumflags2 dep. `policy_action` mask param migrated; the 2 i32-overflow tests → `BitFlags::empty()`; +1 non-vacuous AC2 membership test. `enumflags2 = "0.7"` direct in gp-core manifest (single consumer). One `#[allow(clippy::use_self)]` (justified — `#[bitflags]` macro-span false positive). Closes #51. |
| 2026-07-16 | code-writer-subagent-effort | ✅ implemented (0 new tests; harness-only, 10 files) | — | New `.claude/agents/code-writer.md` (frontmatter `model: sonnet` + `effort: medium`, inherit-all `tools`) so the code-writing tier's **effort is pinned in frontmatter** — the Agent tool has no per-invocation `effort` param, so the prior inline `model="sonnet"` code-group spawn could not enforce "effort medium". Routes the `/task` code path (`/context-reset` code branch, Step-8 prose, `design`↔`design-review` markers) to `subagent_type="code-writer"` and converts the four inline-editing skills (`/bugfix` Step 5, `/main-ci-failed`/`/pr-ci-failed`/`/pr-commented` Step 4) to delegate code-writing (Mode B: author-not-transcribe, gate, return without committing; orchestrator keeps self-review + commit/push; mechanical fmt/clippy/doc/actionlint one-liners stay inline). Opus/harness path unchanged (inline `model="opus"`, inherited effort). `claude-tools-hierarchy.md` updated. No tracking issue (internal harness task). |
| 2026-07-15 | integer-safety-audit | ✅ implemented (7 new tests; 52 gp-core green) | — | Workspace integer overflow/signedness-safety audit (#48, standing — **Refs**, not Closes). 22 flagged sites, all gp-core: `Point::neighbors4`→`saturating_add/sub`, `sim::legal_move` + `walls_from_boundary`→`checked_add` (off-grid⇒emit-wall, NOT saturating); 7 retain-(b) fn-level `#[allow(clippy::arithmetic_side_effects, reason)]` with documented domain bounds + covering tests (`Size::area`, `Rect::index`/`on_border`, `supercover`, 3 graph counters). Enables `clippy::arithmetic_side_effects = "deny"` in root `[workspace.lints.clippy]` (machine-enforced going forward). No `as` casts existed (cast clause vacuous). Scaffolds confirmed clean; scaffold re-audit deferred on #48. |
| 2026-07-15 | library-survey-workflow-tuning | ✅ implemented (0 new tests; harness-only, 14 md/instruction files) | — | Combined harness-tuning PR (5 parts + DOC-10). PART 1: library survey (honest **empty** `gp-core` adopt-now set) + near-term issues #49/#50/#51. PART 2: group-size cap 3→10 (MAX), split threshold >7→>15. PART 3: API Stability reframed **definitive never-publish** (4 sites + AGENTS.md). PART 4: quartzite adoption — file-size ladder (CS-0), `doc-convention` DOC-1..9 rewrite, CI-1 actionlint AXIOM + CI-2/3/4, CS-1/CS-2, deterministic-collections. PART 5: per-group implementor model by change-type (code→`sonnet`/medium, instructions→`opus`/inherited; default-4 groups user-gated). DOC-10: removed 4 dangling `§ Self-sufficiency` refs. Follow-up: #52 (re-verify codebase vs new conventions). |
| 2026-07-15 | core-size-rect-dims | ✅ implemented (13 new tests; 45 gp-core green) | core-geom-corridor | gp-core `Size` / `Rect` value types + unsigned `Corridor` dims (rework, folded into PR #47). Factors box/index math off `Corridor`; deletes the `Corridor::new` `assert!` → **zero production panics**; `Rect::index` total via `checked_sub` + `usize::try_from`; net `#[allow]` 3→0. Answers the PR #47 "why not unsigned?" review comment. |
| 2026-07-15 | core-geom-corridor | ✅ implemented (20 new tests; 32 gp-core green) | core-supercover | gp-core corridor-graph helpers (block 3a, build-order 2/40) — 4-conn `flood_fill`/`component_count`, `bounded_complement_components` (§2 Ф4 infield-hole test), in-`D` geodesic BFS (`CorridorScratch`/`geodesic_bfs`/`geodesic_layers`), `walls_from_boundary` (Ф7). `Wall` → `{ cell, side: Side }`; `geom.rs` split into `geom/{mod,graph}.rs`. Closes #5. |
| 2026-07-14 | import-ci-workflows | ✅ implemented (0 new tests; 12 gp-core green) | — | Bootstrap GitHub Actions CI from quartzite (single `ubuntu-latest` lane) + Dependabot + mandatory Vulkan env-init + MSRV 1.97.0 + `CARGO_BUILD_WARNINGS=deny` + workspace lint tables (pedantic/nursery=deny). No tracking issue. |
| 2026-07-13 | core-supercover | ✅ implemented (12 tests) | — | gp-core exact integer `supercover` predicate (SAT bbox-scan); foundation of `legal_move` + passability oracle. Closes #4. |
