# Plan index

Active and completed task plans (spec + design pairs). Statuses: ✅ implemented · 🟢 in progress · 🟡 spec-only / deferred · 🔴 blocked.

Written by `/task` (Step 12 flips a row to ✅) and read by `/next`. New plans land at `ai-docs/plans/YYYY-MM-DD-name.{spec,design}.md`; completed pairs move to `ai-docs/plans/done/`.

| Date | Name | Status | Depends on | Notes |
|------|------|--------|------------|-------|
| 2026-07-24 | gp-gen-phase7-centerline | ✅ implemented (+20 net new gp-gen tests — 232→252; **140 gp-core + 252 gp-gen + 248 gp-render**, gp-gen rides #134 Miri carve-out) | gp-gen-phase4-static-validation (#27), finalize-track-artifact-contract (#6) | **Block 1 — Ф7 racing centerline (render-only): medial axis → loop → arc-length resample** (#33, build-order 30/40). `racing_line(d: &Corridor, gate: &TimingGate, race_dir: RaceDir) -> Centerline` (new `crates/gen/src/phase7.rs`, `pub use phase7::*;`) — the renderer's ideal line P2, **strictly render-only**: the block1→block4 AI contract stays `{ D, walls, sf, race_dir, s_field }`, and a **source-scan guard test** (`ac5_gp_ai_never_references_centerline`, case-sensitive `rg`-equivalent over `../ai/src`) enforces that no `gp-ai` symbol reads the `Centerline` type or `TrackArtifact::centerline` (the AI frame derives from `∇s`, not this curve). Consumes existing gp-core primitives only, **zero gp-core change**: `DistanceTransform::compute`+`medial_axis`, `TimingGate::forward_face`, `Centerline`/`CenterlineSample`, `RaceDir`, `Corridor`. **Pipeline** (every failure path returns `Centerline::default()`, never panics): medial set → **`bridge_gaps`** (close diagonal/corner + spur gaps to one loop) → **`prune_spurs`** (iterative degree-<2 removal) → gate-anchored straightest-continuation 4-conn **`walk_cycle`** (thins 2-cell bands, `s=0` at gate) → i64-shoelace **`orient`** vs `race_dir` → arc-length **`resample`** (`RESAMPLE_STEP=1.0`, `samples[0].s==0`, monotone `s`, `length`=perimeter, closed) → wraparound unit tangents. **User-approved Design Amendment mid-`/task`:** the design's named `supercover` bridging primitive produces degree-3/4 junctions on corridors thicker than 1 cell (the realistic case) and yielded **zero samples on the design's own annulus fixture** → replaced by `bridge_path` (minimal 4-connected rectilinear path, both axis orders, prefers entirely-in-`d`); the "while >1 component" stop condition → leaf-cell tracking (a single ring's last corner gap else leaves two leaves in one already-merged component, peeled by `prune_spurs`). `MAX_BRIDGE_GAP=6` (annulus corner gap Manhattan 4, +2). **One production panic-index row:** `phase7.rs:332` `order.last().expect(...)` (unreachable — `order` init `vec![start]`, push-only). Tests split into `include!`d `phase7_tests.rs` (phase7.rs 529 / tests 446 lines; direct `rustfmt --edition 2024` on the `include!`d file). Fixtures reuse the shared `testfix.rs` `corridor` builder (zero new copy; legacy `phase4`/`phase4_defects` copies left for a follow-up dedup). Design GO round 1 (3 GO-notes folded) → Design Amendment (supercover→bridge_path) → design re-GO round 2 (1 minor doc-fidelity note folded) → self-review **APPROVE round 1** (2 non-blocking: `ac1` insensitive to `prune_spurs` — has direct mutation-tested unit coverage; 26-line soft-500 overage). Integer/float-render-curve, deterministic, total, **zero `unsafe`**, no new dep. 8 impl commits (1 code-writer group) + 1 panic-index commit. Closes #33. |
| 2026-07-24 | s-field-bfs-gate-cut | ✅ implemented (+19 net new gp-core tests — 121→140; **140 gp-core + 232 gp-gen + 248 gp-render**, workspace 622 green, gp-core Miri-clean pure-integer) | gp-gen-phase3-startfinish (#26), core-geom-corridor (#5), finalize-track-artifact-contract (#6) | **Block 1 — Ф7 s-field: fold-free BFS distance on the annulus cut at the gate (N1)** (#32, build-order 29/40). The producer that fills the monotone `0→L` progress coordinate `s` (source of the AI frame `∇s` + reward `Δs`), **split across its two natural gp-core owners** — gp-gen untouched (`generate()` is a stub; end-to-end wiring is out of scope). **(1) `barrier_distance_field(d, seeds, barrier) -> Vec<Option<u32>>`** (new, `crates/core/src/geom/graph.rs`): a multi-seed, barrier-aware, layer-by-layer double-buffered **4-conn BFS**, sibling to the single-seed/barrier-free `geodesic_bfs`/`geodesic_layers`. Row-major over `d`'s box (`len == d.area()`), `Some(dist)` for reachable in-`D`, `None` for `¬D`/unreachable. Barrier is a **generic `impl Fn(Point,Point)->bool`** — NOT a `TimingGate` — so `geom` acquires no dependency on the higher-level `track` module (preserves `track`→`geom` layering). **Must** live in `geom`: needs `Corridor`'s **private** `index`/`area` (gp-gen has only public API). `distance.saturating_add(1)` on `u32` is a **total** form (no `#[allow(arithmetic_side_effects)]`, no panic-index entry), landing directly in `SField.dist: Vec<Option<u32>>`. **(2) `SField::from_gate_bfs(d, gate) -> SField`** (new, `track.rs`): the domain wrapper wiring `gate.forward_face()` seeds + `\|a,b\| gate.separates(a,b)` barrier + `d.rect()` into the primitive. **(3)** supporting members `TimingGate::forward_face()` (the `{ behind[i] + forward.delta() }` seed set, `checked_add`-overflow-filtered exactly as `separates`) and **`Corridor::rect()`** (new `pub const fn` box accessor). **Barrier consulted on every 4-neighbor step + symmetric `separates`** so the cut blocks both directions (a one-way barrier would leak the antipode fold back in). **AC crux tests** (all hand-derived on an 8-cell 3×3 ring, seed `(2,1)`, cut `(1,1)–(2,1)`, `L=7`): AC1/AC6 exact cell-by-cell distances (cross-cut `(1,1)` reads **7 not 1** — proves the barrier); AC3 walks the ring asserting **Δs≥0 for every forward move except the single `L→0` gate reset** (the antipode fold a naive full-ring BFS would produce); AC4 the only discontinuity is that reset; AC5 a **U-hairpin** fixture where a nearest-point-on-centerline projection would fold two arms (arm-A `(0,3)` at `s=2`, arm-B `(4,3)` at `s=6` — kept distinct, 4 apart along the true path); AC6 determinism. **Design GO round 1** (2 notes + 3 recs — misattributed `flood_component` citation, third-BFS-in-`geom` duplication note, `dist.len()==d.area()` doc postcondition, saturated-self-neighbor edge test — all folded back into the design and verified against the durable artifact before Step 8). self-review **APPROVE round 1** (0 blocker/major; 1 comment-nit fixed in `8c345fc`). **Step-9.5 caught + fixed a false test count** (a code-writer summary said "gp-core 228→248"; reconcile-vs-README showed real 121→140 — learning logged). Integer-only, deterministic, total, **zero production panic**, no `unsafe`, no new dep. 4 impl commits (1 code-writer group) + 1 self-review nit commit. Closes #32. |
| 2026-07-24 | gp-gen-phase6-derived-ord | ✅ implemented (+4 net new tests — gp-core 118→121, gp-gen 231→232; **121 gp-core + 232 gp-gen + 248 gp-render**, workspace 603 green, gp-gen rides #134 Miri carve-out) | gp-gen-phase6-local-repair (#31) | **Block 1 — Ф6 ordering surrogates → derived `Ord`** (#153, follow-up to #31/PR #152 review). **Behaviour-preserving refactor** replacing the hand-rolled sort keys `phase6_repair::{issue_rank, axis_rank, issue_sort_key}` + `phase5b::wall_sort_key` with `#[derive(PartialOrd, Ord)]`. **Narrowly supersedes #31's `AC11 = no gp-core change`:** gp-core's `Orient` (`Horizontal<Vertical`), `Side` (`East<West<North<South`), and `Wall` (`cell` then `side`) each gain `Ord`+`PartialOrd` — verified no `BTreeSet`/`BTreeMap`/`BinaryHeap` keyed by `Wall`/`Side`, so the new order reaches **zero** existing collections; nothing else in gp-core changes. **`Issue` reordered to severity order** (`Disconnected<BadTopology<LostHairpin<ArmsMerging<ConcaveChordCut<Narrow<NarrowSf<NoBraking`, #31 Decision 4 ranks 0–7) then derived — **option (a) reorder-and-derive** over a hand-written `Ord` (which would merely relocate `issue_rank`, not delete it, failing AC4); reorder is workspace-safe because `Issue`'s declaration order is unobserved (no `as`-cast/strum/`EnumIter`/manual `Ord`; gen-internal, `gp-game` never names it). `Side`/`Wall` needed **no** reorder — declaration/field order already equals the old rank term-for-term, so a plain derive reproduces `wall_sort_key = (cell, side_rank)`. `DispatchLabel` derives `Ord` with `DynamicStall` kept last; dispatch sort is now `labels.sort()`. **Owner-directed spec amendment mid-`/task`** pulled `Side`/`Wall`/`wall_sort_key` in from *deferred* (round-1 spec covered only `Orient`/`Issue`/`DispatchLabel`). Both equivalence claims (dispatch key + wall key) verified **term-for-term** by design-review and self-review against live source; the retained determinism/tie-break/canonical-min tests genuinely still pin the old behaviour (not weakened). Step-9 reconciliation reworded 3 gp-core test-name/comments off deleted-symbol names so AC4/AC11 `rg` are whole-`crates/` grep-clean. Integer-only, deterministic, **zero production panic**, no `unsafe`, no new dep, no other gp-core change. 6 impl commits (1 code-writer group) + 1 Step-9 reconciliation commit. self-review **APPROVE round 1**. Closes #153. |
| 2026-07-24 | gp-gen-phase6-local-repair | ✅ implemented (78 new gp-gen tests — 153 → 231; **118 gp-core + 231 gp-gen + 248 gp-render**, workspace 599 green, gp-gen rides #134 Miri carve-out) | gp-gen-static-validation (#27), gp-gen-phase5b-full-oracle (#29), gp-gen-frontier-gap-mapping (#30) | **Block 1 — the full Ф6 local-repair phase** (#31, build-order 28/40). `phase6_local_repair(ctx, issues) -> RepairOutcome` is a **single pass** (owner: not a repair-budget loop — that stays with `generate()`): each issue → dispatch → one dual-edge shift → **edit-type-scoped `[C3]` recheck** (add→local, remove→global flood-fill, run-out→**speed-sink→speed-sink**) → commit iff that arm's **own metric strictly improved on a scratch copy** (owner: per-arm progress); `Repaired { d, edits }` iff ≥1 edit committed, else `Failed` (the Ф1-reseed signal). Six defect labels → five repair arms across new modules `phase6_repair.rs` (driver + types + `dispatch` over all 9 labels + fixed severity rank), `phase6_arms.rs` (`push_outer_wall_out`/`fill_inner_tooth`/`lengthen_straight`/`widen_corner` + the `apply_edit` one-wall/one-cell chokepoint), `phase6_remove.rs` (`trim_arm_wall`/`nudge_finger`, each gated on a global flood-fill). **Owner chose "detectors too":** three of the design's six labels had **no producer in code**, so this task also **built the three missing detectors** so no arm ships as dead vocabulary — `ConcaveChordCut` + `ArmsMerging` (static, new `phase4_defects.rs`; `axis_width` + the R1-backstop-relocated `narrow_at`/`narrow_issues` live here to keep `phase4.rs` under the 800-line cap) and `NoBraking` (dynamic, new `phase5_runout.rs`). `Issue` (in `phase4.rs`) gains all three; helpers `block_origin`/`block_points` lifted to new `coarse.rs`. **The load-bearing derivation (spec-amended round 3, owner-approved after orchestrator verification):** the spec's own path-indexed `runout_room` made every `NoBraking` add-arm **provably vacuous** (AC3 freezes the fastest-lap path ⇒ the term is constant under an edit, while `attainable(c)` is monotone non-decreasing ⇒ the deficit can never strictly decrease), so it is **geometric** (`wall_run(D,c,dir) − 1`, grown by `lengthen_straight`) plus a measured `v_corner` term (grown by `widen_corner`); and `NoBraking` **cannot** be a lappability verdict — #30's AC7 proof makes the dynamic-only stall class empty — so it is a `v_target`-referenced **run-out budget** check. Two more spec formulas amended: the sink set is not "non-empty by construction" (`speed_heatmap` is a per-point max ⇒ index 0 added unconditionally), and `fill_inner_tooth`'s metric is "named tooth drivable + one bounded hole survives" (tooth-count-decreases is unsatisfiable on a line hole). **AC3, the flagged highest-risk piece — a fixture where a fixed-radius recheck reports "fixed" while sink-to-sink correctly reports "still deficient" — was built and passes with the exact hand-derived numbers** (sink deficit 4→3, radius 1→0, `attainable == 2`): both scopes call the SAME `window_speed`, differing only in seeds/**barriers** (the barrier set is mandatory and IS the seed cells — design-review round 1 caught that with `barriers = ∅` the "radius" flood legally backs up and re-accelerates, collapsing onto the global answer; seed states expand even when their own cell is a barrier). Design **GO round 2** (round-1 major: the AC3 `barriers` gap + `flood.peak[c]` panicking index + a cheaper `phase4.rs` split; all folded back and orchestrator-verified in-file). self-review **APPROVE round 2** — **mutation testing** round 1 found four load-bearing clauses (AC1 single-cell-flip, AC4's strict-metric gate on 3 arms, the run-out tie-break, three false `#![allow(dead_code)]`) with **zero** guarding tests despite 597 green; all fixed and re-verified to bite. Two residual objections stand **verified, not waved**: `push_outer_wall_out`'s `MetricNotImproved` branch is provably unreachable (orchestrator-confirmed from the code), and the two-`widen_corner` `wall_sort_key` tie is possibly-unreachable with no AC riding on min-vs-max. Integer-only, deterministic, total, **zero production panic**, no `unsafe`, no `gp-core` change, no new dep, `generate()` still `todo!`. 14 impl commits (2 code-writer groups) + 1 self-review-hardening commit. Closes #31. |
| 2026-07-24 | gp-gen-frontier-gap-mapping | ✅ implemented (15 new gp-gen tests — 9 `phase6` + 6 `phase5b`; **118 gp-core + 153 gp-gen**, workspace 521 green, gp-gen rides #134 Miri carve-out) | gp-gen-phase5b-full-oracle (#29), gp-gen-phase5a-reachability (#28) | **Block 1 — Ф6 `map_frontier_gap_to_edge` (`[N3]`), the design's single riskiest, explicitly-unproven step** (#30, build-order 27/40). New `crates/gen/src/phase6.rs` (`pub use phase6::*;`): **`map_frontier_gap_to_edge(d, grid, sf, race_dir) -> RepairCandidate`** — the `DYNAMICALLY_DISCONNECTED` arm of the Ф6 dispatch table. **Scope widened at spec time (owner-approved) to AMEND Ф5b's diagnostic:** the committed `break_points` **provably could not localize a stall** — every member came from `proj(R)` (whose non-seed states satisfy `legal_move`'s `p1 ∈ D`) or the `grid.positions` fallback, so `break_points ⊆ D` and it could never name the **non-drivable** cell a geometric sever needs added (on the broken ring it named behind-gate `(2,0)`; severed `(4,2)` was provably absent). Payload therefore **renamed + re-typed `break_points: Vec<Point>` → `stall_walls: Vec<Wall>`** (clean break, no shim — `phase5b.rs` was the sole referencing site under `crates/`), computed by **`p0_boundary_walls(d, &p0)`** (one `Wall` per off-`D` side of each `P0` cell, so the dual edge IS the diagnostic), two-tier fallback (tier 2 = `walls_from_boundary`) under a documented **`D` non-empty** precondition — a tier-3 from `grid.positions` was considered and **rejected** (those cells are non-drivable on a degenerate `D`, violating AC4). **The mapper is a verified-growth greedy, not a heuristic:** re-validates each wall is a genuine boundary edge (never trusting the diagnostic), scratch-applies the add-edit, keeps it only if `\|P0\|` **strictly grows** at `V_ceil = 1`; picks **max growth**, ties by **min `wall_sort_key`** — a function of the candidate *set*, so order-independent. Measured: `Edge(Wall { cell: (4,1), side: North })`, `\|P0\|` **3 → 16**, oracle flips **`NotLappable` → `Lappable`** off a single shift. **`RepairCandidate` ships with TWO variants, not three:** the owner declined to fix the arity on an unexecuted derivation and required an **executable proof gate (AC7) to run BEFORE the shape was locked** — the biconditional `oracle_liveness_v1 == matches!(phase5_full_oracle, Lappable(_))` over the whole fixture battery **plus a purpose-built counterexample** (`trap_ring`: 12×8 ring + 5-cell braking-trap spur; V=1 lappable AND `\|R \ B\| = 10` at `V_ceil = 2`, witness `(6,5,0,2)` with **zero** legal actions), since pre-existing fixtures alone would discharge AC7 vacuously. It held everywhere ⟹ the dynamic-only stall class **is empty** and no `Declined` arm exists. Emptiness is **structural, not merely unobserved**: `live` is monotone in `V_ceil`, so the `let Some(fastest) … else` arm can only fire at `V_ceil = 1` — the AC7 test is a regression guard on a proven theorem, and the rustdoc forbids "no counterexample was found" framing. Design **GO round 1** (5 issues + 4 recommendations folded back + orchestrator-verified in-file) → self-review **REJECT round 1** (mutation proved `wall_neighbor`'s `checked_add` overflow guard had **zero** coverage — `wrapping_add` left all 151 green; the test naming the hazard never reached it) → **APPROVE round 2** (both new tests red under the same mutation, and under a sharper `is_none_or` → `is_some_and` branch mutation). Integer-only, deterministic, total, **zero production panic**, no `unsafe`, no new dep. 7 impl commits / 1 code-writer group. Closes #30. |
| 2026-07-23 | gp-gen-phase5b-full-oracle | ✅ implemented (24 new gp-gen `phase5b` tests; **118 gp-core + 138 gp-gen**, workspace 506 green, Miri gp-core green pure-integer / gp-gen rides #134 carve-out) | gp-gen-phase5a-reachability (#28), finalize-track-artifact-contract (#6) | **Block 1 — Ф5b full `Vmax` passability oracle (iterative deepening) + tempo/fastest_lap/heatmap + break_points** (#29, build-order 26/40). New `crates/gen/src/phase5b.rs` (`pub use phase5b::*;`): **`phase5_full_oracle(d, grid, sf, race_dir) -> OracleResult`** **composes** the Ф5a floods (AC5, one code path — no reimplementation). Iterative deepening over `V_ceil = 1, 2, 4, 8, …`: `R = forward_reachable`, goal set `G = lap_close_goals` (states in `R` crossing the S/F forward via a `crosses_sf_forward` helper reusing core `register_move`), `B = backward_reachable(G)`, `live = R ∩ B` each round; **halts** when `Vpeak = max L∞ speed over live < V_ceil` reporting `Vpeak` as `Vmax_attain` (`saturating_mul` doubling, ≤2× over-shoot). A state in `R` but not `B` (**provable crash**) is excluded from `live` (AC2). **`OracleResult`** (new **gp-gen** enum, not in the exported `gp-core` `TrackArtifact` contract) = `Lappable(TrackMetrics)` \| `NotLappable { break_points: Vec<Point> }`. On success populates existing `gp_core::track::TrackMetrics`: `vmax_attain`; `fastest_lap` (fewest-**move** path through `live`, a confined augmented `(CarState, LapCounter)` BFS `fastest_lap_through_live` also returning the **phase-0 cell set `P0`**); `tempo = lap_length / len(fastest_lap)` (V=1 fewest-move `lap_length` captured once at ceiling 1; `len ≥ 1` guards the ratio); `speed_heatmap` (per-point max L∞ over `live`, `Point`-sorted). On no lap: `break_points = frontier_gap(&proj(R), &P0)` (outer 4-frontier of `P0` within `proj(R)`) with a **`bp.is_empty() → seed_cells` fallback** (fires on `P0 == ∅` **or** `P0 == proj(R)`) — the fallback is the **unconditional AC3 non-emptiness guarantor**; the P0-frontier is the diagnostic for the future Ф6 `map_frontier_gap_to_edge` (out of scope). **Design Amendment mid-implementation (owner-approved):** the initial `frontier_gap(d, &R)` (drivable-vs-`R`) was **proven always-empty** (a unit 4-move's `supercover` = its 2 endpoints ⟹ `proj(R)` = the whole 4-connected drivable component ⟹ no unreached-but-adjacent drivable cell), making AC3 unsatisfiable; redefined to the goal-aware phase-0 form. Design **GO round 3** (R1 GO+2 notes → **Design Amendment** [always-empty `frontier_gap`] → R2 ITERATE [proof "phase 0 ⟺ ahead side" false on a *valid* ring — Ф5a bounded-chord `register_move` skips the far-wall crossing, so a behind-gate seed re-enters at phase 0 after a full loop] → R3 GO, proof re-scoped to non-loopable topologies + AC3 reframed onto the fallback) → self-review **APPROVE round 1** (0 findings; AC1–AC7 verified non-tautological, grounded in code). `vnorm` branchless `const fn` L∞ (`.max()`/`try_from` E0658 non-const). Integer-only, deterministic, total, **zero production panic**; no new dep; gp-core untouched. 6 impl commits (subtasks 1–7 incl. frontier_gap amendment) + 1 AC7 long-straight gap-fill / 2 code-writer groups. Closes #29. |
| 2026-07-23 | gp-gen-phase5a-reachability | ✅ implemented (18 new tests — 2 gp-core `register_move` + 16 gp-gen `phase5`; **118 gp-core + 114 gp-gen**, workspace 482 green, Miri gp-core green pure-integer) | gp-gen-phase3-startfinish (#26), core-lap-counter (#8), core-sim-step-legal-move (#7) | **Block 1 — Ф5a passability reachability substrate + V=1 liveness + core S/F bounded-chord fix** (#28, build-order 25/40). New `crates/gen/src/phase5.rs` (`pub use phase5::*;`) adds three **deterministic, integer-only, total, zero-production-panic** floods over the 4D `CarState`, all reusing core `legal_move` as the edge (AC3, one code path): **`forward_reachable`** (VecDeque + HashSet, `Action::iter()` order, L∞-box `within_v_ceil` bound), **`backward_reachable`** (reversed predecessor via a `const fn predecessor` `checked_sub` invert of `step`, still gated on the same `legal_move` — the `B` half of Ф5b's future `R ∩ B`), and **`oracle_liveness_v1`** — the binary "a lap exists at `\|v\| ≤ 1`" filter: an **augmented `(CarState, LapCounter)` flood** returning `true` on the first `raw() >= 1` (lap-close `0→1`), distinguished from the race-start crossing (`−1→0`); visited key clamps the counter to `{−1,0}` for finiteness. `race_dir` accepted for signature fidelity but unused (crossing sign is `sf.gate.forward`'s). **Scope widened mid-implementation (owner-approved) to fix a confirmed `gp-core` bug** the oracle exposed: `LapCounter::register_move` scored S/F crossings against the gate's **infinite supporting line** (`gate_coord` along `forward`'s axis only; global `GATE_LINE` half-plane; only `gate.behind.first()` read), but design.md §3 mandates a **bounded chord** — so on **any** closed ring the far-wall reverse crossing cancels the gate crossing, `raw()` nets zero per lap, and `raw() >= 1` is **topologically unreachable** (net-zero-crossings theorem + strict sign-alternation; core's own #8 tests reached `raw()==1` only via a non-physical repeated identical `(1,1)→(2,1)` crossing). **Fix** (`crates/core/src/sim/mod.rs`): `register_move` gains an **along-chord bound** via `const fn lat_coord` (perpendicular-of-`forward` projection) + `fn crossing_within_span` (i64-widened integer containment; **not** `const fn` — `i64::from(i32)` E0658, the design's anticipated fallback; `d = a_perp−b_perp ≥ 2` so no div-by-zero, `(0,0)`-seeded fold so no panic) — counting a crossing **iff** the swept segment passes through the chord's extent; **`gate_coord`/`crossing_event`/`legal_move` unchanged**. **Behaviour-preserving:** all 6 committed `register_move` tests score on the chord line (`y=1`, `lat*=0`) so stay green (only the new off-chord test exercises the exclusion); the 3rd call-site `phase3.rs:970` survives unchanged (grid inherits chord `lat`; `behind == chord`), no phase3 edit. Design **GO round 3** (initial GO+notes on the substrate → **Spec Amendment** widening scope → design re-run round 2 ITERATE [missed `phase3.rs:970` survey] → round 3 GO+`behind==chord` note folded) → self-review **APPROVE round 1** (reviewer independently re-derived the `crossing_within_span` arithmetic + confirmed AC4 fixture genuinely lappable). Out of scope: full `Vmax` oracle Ф5b (#29), metrics, `generate()` wiring. No new dep. 7 commits / 2 code-writer groups. Closes #28. |
| 2026-07-23 | gp-gen-phase4-static-validation | ✅ implemented (32 new tests — 10 gp-core `distance` + 22 gp-gen `phase4`; **116 gp-core + 98 gp-gen**, workspace 464 green, Miri-clean pure geometry) | gp-gen-phase2-rasterize (#25), core-geom-corridor (#5) | **Block 1 — Ф4 static validation: connectivity · single hole · width · finger liveness** (#27, build-order 24/40). `phase4_static_checks(&Corridor, &CoarseSkeleton, k, n, m, &StartFinish) -> Vec<Issue>` (new `crates/gen/src/phase4.rs`, `pub use phase4::*;`) — **deterministic, no-RNG, integer-only, zero-production-panic, total** validation pass emitting a typed `Issue` set (empty ⟺ statically valid) for the future Ф6 repair phase. `Issue` = `Disconnected`/`BadTopology` (nullary, global) / `Narrow`/`NarrowSf` `{ center, axis, width }` / `LostHairpin { tip }`, all `Eq + Hash` (order-independent `HashSet<Issue>` test assertions). **Four checks, fixed order:** connectivity + topology **delegate to #5 gp-core helpers verbatim** (`component_count != 1` → `Disconnected`; `bounded_complement_components != 1` → `BadTopology`); **NARROW** samples **all** `D` cells passing the DT pre-filter `2·dt−1 < n` (sound: `2·dt−1 ≤ w`, never skips a true neck — a DT *valley* a local-max ridge would miss), computes `w = min(hrun, vrun)`, emits iff `w < n AND w ∈ {2·dt−1, 2·dt}` (the **DT-consistency test** — fires only at a genuine perpendicular cross-section, rejecting inner-corner/taper false positives); **NARROW_SF** on the S/F chord (`sf.chord.len() < m`, `axis = sf.orient`, `center = min-Point` under a totality guard); **LOST_HAIRPIN** when a coarse infield finger's `k×k` fine footprint is entirely drivable (arms merged — complementary to `BadTopology`'s pinch-off-island case). **Two new reusable gp-core `geom` primitives** (new `crates/core/src/geom/distance.rs`, home-crate decision Open-Q1): `DistanceTransform` (multi-source 4-conn BFS) + `medial_axis` (strict axis-wise DT ridge → `BTreeSet<Point>`; on a rectilinear annulus = 4 disjoint straight strips with diagonal corner gaps, gap-closing deferred to Ф7's `racing_line` — AC5's second consumer). `arithmetic_side_effects=deny` honored via `saturating_*`; all `usize→u32` via `try_from(..).unwrap_or`, never `as`. Design **GO round 3** (R1 ITERATE: unsound ridge-only NARROW sampling → all-cells + DT-consistency; R2 ITERATE: `NarrowSf.axis` perp→`sf.orient`; R3 GO) → self-review **REJECT round 1** (major: DT-consistency guard test vacuous — mutation-proven; nit: weak AC7 assertion) → test-only fix `36f031a` (rebuilt staircase fixture is non-vacuous, mutation-verified; production `narrow_at` byte-unchanged) + user-approved **Design Amendment** (stale "closed loop" prose → 4-strip, design re-GO) → self-review **APPROVE round 2**. No new dep. 6 impl + 1 test-fix commit / 1 code-writer group. Closes #27. |
| 2026-07-23 | self-reflection-eval-methodology | ✅ implemented (0 new Rust tests — prose-only harness change; all workspace gates green) | — | **Harness/process** (`.claude/**` + `ai-docs/**`, no game code) — combined spec closing #80 (Part A) + #114 (Part B). **Part A (#80):** expand `.claude/skills/improve/SKILL.md` item 6 to state the parent-thread eval ordering explicitly — working-tree apply → clean-context reproducer dispatch → eval **gates the commit** (PASS commits; FAIL reverts before landing + loops to Step 3) — cross-referencing `self-improve.md § Step 6` with no duplication. **Part B (#114):** new `/reflect` skill + `self-reflect` subagent (mirroring `/improve`+`self-improve`), **standalone-only** trigger (no auto-fire; `disable-model-invocation: true`). Subagent emits a structured good/bad list (empty permitted; each finding names a concrete moment) routing each to `learnings` (cheap default) / `ticket` (costly, `process`-label, pattern altitude) / `none` (recorded, **ephemeral** — owner-fixed) with a one-line justification + cost/value rubric; parent surfaces per-finding consent. Boundary-compliant `learnings` route (`Kind`/`Escalated? no`/append-only + same-turn by construction); entries **feed** the `/improve` ≥3/≥2 threshold, never bypass. Propagation: `claude-tools-hierarchy.md` inventory + `self-improve.md § Step 1c` primitive block updated; **no AGENTS.md sync-group row** (char-band; mirrors `/improve`↔`self-improve`'s own no-row coupling). Closes #80, #114. |
| 2026-07-23 | gp-gen-phase3-startfinish | ✅ implemented (27 new gp-gen `phase3` tests; **76 gp-gen** (27 Ф1 + 22 Ф2 + 27 Ф3), workspace green, gp-render Miri N/A — pure geometry) | gp-gen-phase2-rasterize (#25), finalize-track-artifact-contract (#6), core-lap-counter (#8) | **Block 1 — Ф3 start/finish, accel zone, start grid, timing gate** (#26, build-order 23/40). `phase3_start_finish(&Corridor, &CoarseSkeleton, m, v_target) -> Phase3Output { d, sf, grid }` (new `crates/gen/src/phase3.rs`, `pub use phase3::*;`) — **deterministic, no-RNG, integer-only, zero-production-panic, total**. Picks a straight coarse run (not a corner; deterministic `(side-rank, fixed-coord, run-start)` tie-break), **additively** thickens that cross-section to width ≥ `m` (grows `D`'s box via `expand_box` when a push lands off-box — never carves, mirroring Ф2), builds the two distinct **[C2]** objects — `StartGrid` (`m` distinct implicit-`v=(0,0)` cells, `ceil(m/width)` rows back along `−race_dir`; **degrade contract** — a width-capped `D` yields as many distinct in-`D` cells as fit, never a duplicate/¬D cell) and `StartFinish`+`TimingGate` (gate `behind` = front-row cross-section, gate line one edge ahead) — and **measures** (test-only, not enforced) the accel-zone (`≥ v_target²/2`) + grid-straight budgets, deferring seed-retry enforcement to the unbuilt Ф4–Ф6 loop. **AC5 crux:** `skel.dir` is a randomly-drawn *abstract label* (Ф1 `choose_dir`), not geometric winding, so Ф3 *defines* the forward projection convention (CCW `forward=(inward.y,−inward.x)`, CW negated) — pinned in a required `forward_side` doc comment the Ф7 centerline / AI-reward consumer must re-adopt. Gate ↔ `LapCounter` verified: every start cell `gate_coord ≤ 0 < GATE_LINE(1)` → all cars read `raw()==−1` at t=0, first forward move scores `+1`. `v_target` unused in production (`let _ = v_target;` — the budget it parametrizes is test-only). Design GO round 1 (2 notes + 2 recs folded, additive) → self-review **REJECT round 1** (AC7 test cosmetic + AC6 first-loop trivial) → test-only fix (`e67b853`, production 1–518 byte-unchanged) → self-review **APPROVE round 2** (both fixes empirically mutation-verified). `phase3.rs` 518 production / 1006 incl. tests (+18 over soft-500, under hard-1000; single cohesive phase, over-split counter-rule). Consumes existing gp-core types only (`Corridor`/`Point`/`Side`/`Orient`/`RaceDir`/`track::{StartFinish,TimingGate,StartGrid}`/`sim::LapCounter`) — no new dep. Closes #26. |
| 2026-07-22 | gp-gen-phase2-rasterize | ✅ implemented (22 new gp-gen `phase2` tests; **49 gp-gen** (27 Ф1 + 22 Ф2), workspace green, gp-render Miri N/A — pure geometry) | gp-gen-phase1-coarse-ring (#24), core-geom-corridor (#5) | **Block 1 — Ф2 rasterize coarse ring → fine corridor `D` with width taper** (#25). `phase2_rasterize(&CoarseSkeleton, k, n) -> Corridor` (new `crates/gen/src/phase2.rs`, `pub use phase2::*;`) — a **three-stage, deterministic, no-RNG, integer-only, zero-production-panic** pipeline (pure fn of `(skeleton, k, n)`; ignores `skel.dir`). **Stage 1** expands each Ф1 coarse ring cell → a solid `k×k` fine block (union = `D0`; AC1 + AC2-≥`2k`-wide by construction) with `BBOX_PAD = 1` + an expanded-hole mask `H`. **Stage 2a** makes each *outfield-facing* wall **1-Lipschitz** (45° ramp, ≤1 pt/column, `MIN_TAPER_RUN = 3`) via four directional per-scanline upper-envelope passes (`Side::iter()` order) restricted by `H` — additive (grows `D`, never fills `H`), so a `Δ = w·k` widen jog spreads over `w·k` cols with no supercover-cuttable concave niche (AC4/AC5). **Narrow zones: none carved round-1** (owner-chosen *geometry-forced* designation — uniform `k×k` gives every cross-section `≥ k ≥ n` and every S-hairpin cross-gabarit `≥ 3k ≥ 2n+1`, so the forced-pinch predicate is vacuously false on all Ф1 output; AC3 `≥ n` holds by construction, `min == k`; the `n`-carve path is YAGNI, deferred to a future sub-`k` Ф1). **Design Amendment mid-implementation** (user-approved): the N=64 property sweep over *real* Ф1 output falsified the design's original "additive taper preserves topology by construction" claim — Stage 2a can bridge a genuine outfield gap between two arms and seal a spurious 2nd bounded complement component (deterministic witness `phase1_coarse_ring(3, &mut rng(0))`, `k=6/n=3` → `bounded_complement_components == 2`, a 12-cell pocket). Fix = **Stage 2b (absorb-the-pocket)**: after 2a, enumerate bounded `¬D` components (internal complement-flood mirroring Ф1's `fill_holes`) and set-drivable every one **disjoint from `H`** — still additive, still no production self-check/panic (part of the *construction*, not a repair phase), `O(cells)`, restoring `component_count == 1` ∧ `bounded_complement_components == 1` by construction across the sweep (the primary regression gate) + a named `seed_0` witness. AC5 Part B chord family carries an `supercover ∩ H = ∅` H-clearance filter (a design-review round-2 fix — the original excluded its witness chord via a false endpoint claim; the real cut cell is a hole cell, hole-facing turns exempt) so it tests only outfield-facing grazes and is non-vacuous. AC6 pins an exact post-2b snapshot via count + endpoints + fixed non-randomized **FNV-1a-64** content hash (a literal 3996-point set would blow the file-size cap). Two impl deviations self-review-verified sound: AC5 along-ramp-axis dominance tightened `|dy|≥|dx|`→ strict `>` (exact-diagonal tie always clips a staircase corner under `supercover`'s cross-product, independent of taper); the FNV snapshot. Design GO round 1 (4 notes + 2 recs folded) → **mid-impl Design Amendment** (topology-safe taper) → design-review round 2 ITERATE (AC5 Part B false premise) → round 3 GO → self-review **APPROVE round 1** (0 findings; load-bearing claims mutation-verified — removing 2b fails the sweep+witness, disabling the taper fails AC5 Part B). `phase2.rs` 1064 lines incl. tests / 340 production (under hard-1500; sibling `phase1.rs` 1110 precedent). No new dep (gp-core `#5` helpers only). 4 subtask commits / 1 code-writer group. Closes #25. |
| 2026-07-22 | gp-gen-phase1-coarse-ring | ✅ implemented (≈35 new tests — gp-gen `phase1` 24 + `GenParams` 3 + gp-core `rng` 5 + seed-48 witness; **107 gp-core + 27 gp-gen green**, workspace 382) | core-geom-corridor (#5), core-collisions (#10/#49 groundwork) | **Block 1 — Ф1 coarse-block ring + the grouped seeded-RNG config** (#24 + #49 closed, #50 discharged). Two deliverables, one PR. **(A) `gp_core::rng::Seeds`** (new `crates/core/src/rng.rs`) groups **four independently-seeded `ChaCha8Rng` sources** — `collision`/`generation`/`ai_learning`/`ai_inference` — as four `u64` seed fields + on-demand `*_rng()` accessors (materialize-on-demand, mirroring the old `GenParams::rng()`; **not** `EnumMap` — never iterated into output), one-place UI seed config (AC7/AC9). `GenParams` **embeds `Seeds`** and exposes `generation_rng()` (old `seed`/`rng()` removed — single non-divergent path, AC10); gp-core's `resolve_collisions` **re-pointed** to draw from `Seeds::collision_rng()` (signature unchanged, byte-identical determinism tests preserved, AC11); AI sources **defined-but-unconsumed** (`gp-ai` stub, AC9). **(B) `phase1_coarse_ring(l_min, &mut ChaCha8Rng) -> CoarseSkeleton { ring, hole, dir }`** (new `crates/gen/src/phase1.rs`): 2-cell base strip → **even-sublattice 2×2 growth** in the `y ≥ 2` keep-out half-plane (preserves the base's south **edge** as a straight run ≥ `l_eff` — an *enclosure* invariant, `y ≤ −1` is never a bounded complement component) → hole-fill **before** dilation → **Moore-3×3 dilation** (`ring = dilate_moore(P)\P`, 4-connected loop, one hole) → run-preserving outward **widen** → a **run-length + connectivity check** (`min_run≥2`, `max_run≥l_eff`, `component_count==1`, `bounded_complement_components==1`) on the *actual post-widen* ring, with **bounded same-stream retry** then a **guaranteed rectangular-annulus fallback** → makes Ф1 **total** (no `Result`, no panic — gp-core/gp-gen stay zero-production-panic). **Design Amendment mid-implementation** (user-approved): the multi-seed property test (seed 48, `l_min=3`) falsified the design's "widen preserves annulus invariants by construction" claim (outward widening of a concave ring's disjoint arms pinched off a 2nd hole → AC2 violation); fixed by extending the step-6 check to verify connectivity on the post-widen ring (the design's own "checked, not assumed" posture), design re-GO, named `seed_48_lmin3_widen_pinch_stays_one_hole` witness pinned (b372a62). **AC5 cross-platform bit-for-bit** ([N4]/[M3]): every random pick is a fixed-width `u32` index into a `BTreeSet`/sorted-`Vec`, never `HashSet` order. **#50 discharged**: `ring`/`hole` are `BTreeSet<Point>` (reach output) → `Point` gains a purely-additive `PartialOrd, Ord` derive (zero new deps, `indexmap` avoided), governed by `code-style.md` § Deterministic collections. `l_min` clamped to a documented `MIN..=MAX_COARSE_STRAIGHT` domain (bounded work on both paths). `strum` added as a direct `gp-gen` dep for `Side::iter()`. Design GO round 2 (round-1 ITERATE: 2 major AC3 flaws — neighbor-count staircase fixpoint + base-cell≠base-border) → GO-notes fold-in → **mid-impl Design Amendment + re-GO** → self-review **APPROVE round 1** (0 blocker/major; 3 nits accepted). Closes #24, #49; discharges #50. |
| 2026-07-22 | layer-order-strum-enum | ✅ implemented (0 net new tests — the self-referential `layer_order_is_documented` reworked into the genuine `layer_order_matches_documented_names` guard; 248 gp-render green, all goldens byte-identical, gp-render Miri green) | render-overlays (#18), render-track-canvas (#17), cache-track-geometry (#104) | Block 2 **draw-order enum refactor** (#121) — replaces the dead-code `LAYER_ORDER: [&str; 9]` string-array (whose only consumer was a **self-referential** test comparing the const to a hardcoded copy of itself — reordering `draw_frame`'s draw calls failed no test) with **two `pub(crate)` strum enums**: a 7-variant **`Layer`** (`Regions, Heatmap, Grid, Walls, FastestLap, Sf, Cars`) in `track/mod.rs` and a 3-variant **`RegionLayer`** (`Outfield, Asphalt, Infield`) in `track/regions.rs`, both `#[derive(Clone, Copy, Debug, strum::EnumIter, strum::IntoStaticStr)]` + `#[strum(serialize_all = "kebab-case")]` (mirroring the `Icon` enum). **Product-owner-directed two-enum shape** (the issue asked for one 9-variant enum): merging the region trio into one `Regions` variant kills the no-op-arm wart AND the new `RegionLayer` closes a **second, previously-unguarded** ordering gap (`regions::fill`'s three bare imperative statements). **Guard approach (A) enum-driven dispatch pinned at both levels**: `draw_frame` iterates `Layer::iter()` + `match`-dispatches, `regions::fill` iterates `RegionLayer::iter()` — draw order becomes enum-iter order **by construction** (allocation-free, release-unrollable; the rejected (C) ordering-helper would allocate a per-frame `Vec`). The reworked `layer_order_matches_documented_names` flattens both enums (`Regions`→`RegionLayer::iter()`) via `<&'static str>::from` and asserts the 9 kebab names — a genuine guard **because** draw order == enum order (reorder any variant → fails the test AND changes real draw order; the non-self-referential property the old test lacked). **Delegated fork resolved NO** second painter-driven guard (construction invariant + flattened parity test + goldens suffice; a painter guard would only add the Miri gate). **AC5 byte-for-byte** — both enum orders reproduce the exact prior statement sequences; all goldens pass **un-regenerated**. Design-review caught **four** gate-invisible stale plain-code-span refs (doc gate can't see them): `lib.rs:83`, the `draw_frame` doc link, the re-homed const doc (+ snake `fastest_lap`→kebab `fastest-lap`), and the module `//!` doc's old test name — all retargeted, closing audit `rg 'layer_order_is_documented|LAYER_ORDER'` returns zero. `painter.rect_filled`'s `ShapeIdx` return block-discarded (E0308 avoided). `gp-render` keeps **zero production panics**; no new dep (strum already present). 4 subtasks / 1 code-writer group. Merged fresh `main` (PR #123 cleanup) mid-flow, clean auto-merge. Design GO round 1 (1 completeness note folded — the 4th stale ref); self-review APPROVE round 1 (0 findings). Closes #121. |
| 2026-07-22 | cache-track-geometry | ✅ implemented (6 new tests; 248 gp-render lib green, all 9 canvas goldens byte-identical, workspace Miri green) | render-track-canvas (#17), render-overlays (#18), render-app-shell (#23) | Block 2 **track-geometry caching** (#104) — eliminates the per-frame **O(n³) ear-clipping re-triangulation** `render_frame` ran every frame. **Mid-implementation product-owner PIVOT** (after design-review GO + 3 subtasks): the original rect-keyed memoized `&mut Option<TrackGeometryCache>` cache was **superseded** once the owner observed the expensive geometry is *immutable against the track* — the triangulation **topology** (indices `Vec<[u32;3]>`) is a pure function of the track's **lattice-space** shape, and `TrackTransform` (`transform.rs:82`) is a plain **affine** map (uniform scale + translate + y-flip) that only repositions verts, never re-tiles the polygon. Final design: triangulate in **lattice space** (rect-free) and bake `gp_render::BakedTrackGeometry { smoothed_loops, loop_roles, triangulated_indices }` **once per track** via `new(&TrackArtifact)` (no `Rect`, no `TrackTransform`), **owned by `gp-game`** and threaded as a shared `&'a` field on **`Scene`** (a `&T` is `Copy`, so `Scene`'s derive survives — reuses its existing `'a`, and `RaceScreen`'s `Scene { ..self.input.scene }` spread carries it free); per frame `draw_frame` maps the baked lattice verts → screen (**O(n)**) and draws with the baked indices, and `heatmap::paint` reuses the same baked outer indices (the redundant 2nd triangulation pass is gone). **Dissolves** the earlier "immediate-mode rect isn't known before `shell.show`" wrinkle — there is **no rect-keyed cache and no invalidation**; a window resize triggers **zero rebuild**, only a re-map. Draw-only intact (rect-free geometry lives entirely in `gp-render`, built by a pure fn, caller-owned; `cargo tree -p gp-render --edges no-dev` unchanged = egui 0.35 only; gp-core's integer-only `TrackArtifact` untouched). **AC6 behaviour-preserving** — all 9 canvas goldens (`track`/`track_grid`/`track_heatmap`/`track_fastest_lap`/`race_screen`/`lab_screen`/`app_shell`/`app_shell_lab`/`app_shell_race`) pass **byte-identical, none re-minted**: a lattice-space triangulation mapped affinely is a valid triangulation of the same silhouette, and egui raw-mesh (`Shape::mesh`) fills aren't edge-feathered so the internal diagonal choice paints no pixels — empirically discharging the y-flip-winding/diagonal risk. Module renamed `cache.rs`→`geometry.rs` + type `TrackGeometryCache`→`BakedTrackGeometry` (no longer a cache). New tests: `triangulate_lattice_{convex_square,concave_l_shape}`, `new_produces_ring_loops_roles_and_indices`, `new_is_deterministic`, `resize_does_not_rebuild_geometry`, `heatmap_reuses_cached_outer_mesh`. `gp-render` keeps **zero production panics**; no new dep. 9 subtasks / 1 code-writer group. Spec+Design **Amendment** (spec-writer + design re-GO round 1, 2 notes folded) after the pivot; self-review **APPROVE round 1** (0 blocker/major; 2 non-blocking minor — the two pointer-stability tests are tautological, AC2/3/5 hold structurally not via those assertions). Closes #104. |
| 2026-07-22 | reuse-galley-text-shaping | ✅ implemented (2 new tests — `paint_galley[_override]` rect-equivalence vs `Painter::text` for all 6 `Align2` anchors; 346 workspace green, all 11 goldens byte-identical) | render-app-shell (#23) + the whole gp-render widget/screen family (#13–#23) | Block 2 **single-galley text-shaping refactor** (#96) — a **behavior-preserving** whole-crate sweep so each text run is shaped **once** and the one `egui::Galley` is reused for both measurement and painting, replacing the prior shape-2–3× pattern (`layout_no_wrap` for width, again for height, then `Painter::text` re-shaping a third time in `paint`). New **crate-root `mod text;`** holds two `pub(crate)` helpers: `paint_galley` (mirrors `Painter::text`'s exact body minus the shaping — `Align2::anchor_size(pos, galley.size())` → `painter.galley(rect.min, …)` → return the anchored rect, **verified against egui-0.35 `painter.rs:469-481`**) and `paint_galley_override` (same via `galley_with_override_text_color`, for the 4 dynamic-color sites: button `tint(style.fg)` / switch `tint(TEXT_BODY)` / segmented_control `style.fg` **through the preserved `clipped` painter** / `app::nav_item` `fg.gamma_multiply(opacity)`). `show`-measured widgets build the `Arc<Galley>` in `show` and thread it into `pub(crate) paint` (the only signature change — the `*_gallery` harnesses call `paint` directly, so each changed sig updates its callers in the same commit, per the design's caller table); screen/app-shell inline free functions build once locally. **AC4 = byte-identical goldens** (all 11 exact-compare wgpu snapshots unchanged, **no regeneration**), with a Miri-gated `text.rs` rect-equivalence test as the anchor-math guard. **Already-single-shape sites deliberately left alone** (`stepper`/`slider`/`movepad`/`card`/`setup::draw_mono_label`/`common::paint_form_label`/`icon_button`). **Cross-crate audit NEGATIVE** — only `gp-render` uses egui; `gp-game` delegates all draws to `AppShell::show`, the other 3 crates have no GUI dep. Worth knowing: **epaint already caches galleys cross-frame** (`GalleyCache`, `begin_pass` keeps last-frame entries), so this removes redundant per-frame `LayoutJob` hashing + `to_uppercase()`/`to_string()` allocations and makes "shape once, reuse the handle" explicit — it does **not** eliminate per-frame glyph shaping (egui already did). `gp-render` keeps **zero production panics**. 13 subtasks / 2 code-writer handoff groups. Design **GO round 1** (3 clarification notes folded: segmented_control `clipped` painter, 12-file survey count, telemetry `value_rect.max.y`). self-review **APPROVE round 1** (0 findings). Closes #96. |
| 2026-07-22 | render-app-shell | ✅ implemented (7 new app-shell tests, net +6 after placeholder-golden removal; 342 workspace green) | consolidate-render-inputs (#111), render-setup-screen (#19), render-track-lab-screen (#20), render-race-screen (#21), render-results-screen (#22) | Block 2 **app shell — top bar + screen router** (#23, build-order 20/40) — ties the four screens into one navigable flow. `gp_render::app::AppShell` (new top-level `crates/render/src/app.rs`, the composition root **above** `screens/`) owns only the **`Copy`** cross-transition state (`Screen` cursor `Setup`/`Lab`/`Race`/`Results` + `RaceConfig` + `Overlays` + a `has_generated` latch) and **borrows** all `gp-gen`/`gp-ai`/`gp-core`-sourced session data per frame through one `ShellSession<'a>` bundle (draw-only boundary intact: no new dep). The transition logic is a **pure `apply(&mut self, Nav)` + `can_nav(&self, Screen)`** with **no egui**, so AC1/2/5/6/7 are plain un-Miri-gated state-machine tests; AC3 golden + AC4 click-through carry the gate. **"Menu" = the Setup screen** (matching `App.jsx`). **Nav = clickable jumps** (owner decision) — the three text-only top-bar items (`New race`/`Race`/`Track lab`; those Lucide glyphs unvendored) jump directly, **guarded**: `New race` always enabled, `Race`/`Track lab` disabled (no-op) until the first `generated` flips the latch (safe fallback — never route to Lab/Race with no track). **`gp-game` binary wired** (owner decision): `main.rs` replaces `draw_placeholder` with an `AppShell`-driven `eframe::App` fed a **hand-built fixture** `TrackArtifact` (`gp_core::geom::Corridor` + `walls_from_boundary` + hand-populated `TrackMetrics`, the `fixture_track_with_metrics` render-safe pattern) — **not** `gp_gen::generate`, a verified `todo!()` stub that would panic at startup (spec corrected 2026-07-22). One binary-only `.expect` (`STARTUP_CONFIG.laps=5` const) panic-indexed; `gp-render` keeps **zero production panics**. AC3 = a **ninth** wgpu golden (`app_shell.png`, `#[cfg_attr(miri, ignore)]` wgpu-dlopen, `image-check`-verified) + AC4 miri-ignored (`getcwd`) `egui_kittest` click-through (Setup→Lab→Race→Results→Menu; needed a 960px canvas — RaceScreen toolbar swallows the Finish click at 640px, a RaceScreen fragility not an AppShell defect). **User-approved mid-flow spec amendment (AC9):** now that the shell wired the binary, the vestigial `draw_placeholder` had zero production callers → **deleted `placeholder.rs` + `placeholder.png` wholesale**, relocating the `tessellation_smoke` Miri canary into `app_gallery.rs` repointed onto `AppShell::show` (both Miri-gate causes now co-located there). Paired **orchestrator in-thread edits** repoint the deleted `golden_guard` example in `AGENTS.md` + `context.md` → `app_shell_matches_golden`. Design **GO** (round 1: 4 notes folded — module placement, fixture-name, font-precondition; amendment: GO round 1 + 2 notes folded). self-review **APPROVE round 1** (0 blocker/major; 2 non-blocking: AGENTS.md 35k soft-cap minor, stale design prediction nit). Closes #23. |
| 2026-07-22 | consolidate-render-inputs | ✅ implemented (0 new tests — pure refactor; 336 workspace green, byte-identical goldens) | render-track-lab-screen (#20), render-race-screen (#21), render-results-screen (#22) | Block 2 **render-input consolidation** (#111) — a **pure plumbing refactor** (no behavior change) grouping the frame-immutable inputs previously passed to `gp-render`'s draw entry point + screen builders as loose positional params into cohesive **input structs**, so the app shell / screen router (#23) has a clean, stable interface. **Chosen shape = a hybrid** (the one decision the spec deferred to design): a single shared **`Scene<'a> { track, cars, reduced_motion, overlays }`** — the true "frame-immutable scene", consumed by `render_frame(painter, rect, scene)` **and embedded** in `RaceInput` (the only surface besides `render_frame` drawing the canvas) — plus three per-screen required-input structs **`LabInput`** (track/phases/valid/seed) / **`RaceInput { scene, active, laps_done, total_laps }`** / **`ResultsInput`** (standings/summary), since the three screens' required inputs genuinely diverge. Each screen `::new(input)` takes exactly one struct; all four input types derive `Clone, Copy, Debug`, each `::new` FORCED `const fn` (nursery=deny). **Optional** inputs (icon handles) stay `const fn` builder setters (spec default); **`reduced_motion` folded into `Scene`** as a required field — its `.reduced_motion()` setter had zero in-tree callers, so removal is a clean break (no compat shim), zero byte impact. **The one gate-invisible hazard** design flagged and the impl honored: `RaceScreen`'s canvas must be drawn with the **`draw_toolbar` RETURN** overlays (live toolbar toggle applies same-frame), NOT `self.input.scene.overlays` — so `draw_canvas` takes a `Scene` and `show` reconstructs `Scene { overlays, ..self.input.scene }`; static goldens can't catch a regression here (no golden toggles a switch), so it was verified by reading the diff, not a test. Draw-only as ever: no new dependency, no `gp-core`/`gp-gen`/`gp-ai` change (`git diff --stat main...HEAD` empty on `Cargo.toml`/`Cargo.lock`, touches only `crates/render/**`). All 10 in-tree call sites (4 `render_frame` + 6 builders) updated; existing 232 gp-render tests + all wgpu goldens byte-identical. `gp-render` keeps **zero production panics**. Design **GO round 2** (round-1 `major`: `RaceScreen` overlays threading would silently regress on the interactive path → design contract to reconstruct the canvas `Scene`; round-2 GO-note: Subtask-3 contract symmetry folded in). self-review **APPROVE round 1** (0 blocker/major). **Unblocks #23.** Closes #111. |
| 2026-07-22 | render-results-screen | ✅ implemented (11 new tests; 336 workspace green) | render-game-hud-widgets (#15), render-core-widgets (#13) | Block 2 **Results screen** (#22, build-order 19/40) — the **fourth full screen** and Block 2's last per-screen unit (`gp_render::screens::ResultsScreen`), porting `Screens.jsx`'s `ResultsScreen` as the post-race summary: a mono `RACE COMPLETE` eyebrow + a display-face **"You finished P{n}"** title (`{n}` **data-bound** — derived from the `You`-kind entry's `rank`, `None`→`P—`, **not** the JSX's hardcoded `P1`), a "Final standings" `Card` of **one `CarChip` per car in caller-supplied rank order** each paired with a mono right-aligned finish time, a summary `Telemetry` row below a hairline divider (`Fastest lap` accent+`s` / `Tempo` default / `Crashes` danger), and a primary "Race again" + secondary "Menu" action row. **Outcome input = two `gp-render`-local `Copy` structs** (no `gp-gen`/`gp-ai` dep, `RaceConfig` precedent): a per-car `StandingEntry` slice (`car_index`-indexed identity → name via `CAR_NAMES` / color via `car_color`; `rank`, `kind`, numeric `finish_time`) + a `RaceSummary` (numeric `fastest_lap`/`tempo`/`crashes`, formatted at draw). **Identity deliberately decoupled from rank** — `car_index` (not the loop index) resolves name/color, so a player can finish P3 with `car_index==0`; the AC3 unit test proves it with a fixture placing `You` (`car_index==0`) at slice position 2 / rank 3 (a positional-lookup regression fails it loudly, which the old in-order fixture could not). Draw-only: `show`→`ResultsResponse` with distinct Race-again / Menu **click signals** (navigation is `gp-game`'s router, #23). `rotate-ccw` unvendored → optional `again_icon` handle + text-only default (`lab.rs` precedent); `ResultsScreen` is `Copy` not `Debug`. AC6 = an **eighth** wgpu golden (`results_screen.png`, 640×610, `#[cfg_attr(miri, ignore)]` wgpu-dlopen, `image-check`-verified at mint) + a miri-ignored (`getcwd`) `egui_kittest` interaction test (Race-again / Menu each flip exactly their intent). `gp-render` keeps **zero production panics** (the three array-index sites are fixed-size arrays at literal in-bounds indices). Design GO round 1 (2 notes + 1 rec folded: decoupling-fixture test, `i==car_index` prose, golden-mint canvas-height guidance); self-review APPROVE round 1 (0 blockers; 1 nit cleared — JSX-literal gallery fixture, a language-enforced module-visibility adaptation). Closes #22. |
| 2026-07-21 | render-race-screen | ✅ implemented (17 new tests; 325 workspace green) | render-track-canvas (#17), render-overlays (#18), render-movepad (#16), render-game-hud-widgets (#15), render-core-widgets (#13) | Block 2 **Race screen** (#21, build-order 18/40) — the **third full screen** and the game's hero view (`gp_render::screens::RaceScreen`), porting `Screens.jsx`'s `RaceScreen` as a two-column layout: **left** = the dark `GRAPHITE_900` HUD strip (three on-ink `Telemetry` tiles bound to the **active car's** live `CarState` — SPEED = `\|v\|` (`f32::hypot`, 2 dp), `v` = `(vx, vy)`, POS = `(x, y)` — + a right-aligned on-ink `LapMeter`), an overlay toolbar (Grid/Heatmap/Fastest-lap `Switch`es toggling `crate::Overlays` + a ghost "Finish →"), and the bordered track canvas (`render_frame`); **right** = a "Your move" `Card` (`MovePad` size 52 masked by `gp_core::sim::legal_mask(&track.corridor, active_state)` + mono caption + full-width secondary "Coast (·)" shortcut) and a "Standings" `Card` (one `CarChip` per car). Draw-only: `show`→`RaceResponse { action, overlays, finish }` — the selected `Action` (MovePad cell **or** Coast, Coast winning ties) drives the caller's `gp_core::sim::step`; the screen calls **no** `step`/`resolve_crash`/`resolve_collisions`/`LapCounter`. **Per-car input = caller-supplied `&[CarRender<'_>]` slice + `active: usize`** (the same slice `render_frame` consumes → zero reconstruction); `CAR_NAMES` = module `const [&str; 6]`; index-0 = player (`CarKind::You`/active), others `Ai`, `rank = index+1` (real race-position ranking **deferred** — no gameplay metric yet); all index/name access total (no panics; `CarState::default()` fallback). **Round-1 → owner-approved spec amendment** (Step-7 design-review GO-with-notes): the Rust `LapMeter` (unlike `Telemetry`) had **no `on_ink` mode** and `TEXT_INK == GRAPHITE_900`, so the laps-done number rendered band-on-band (invisible), leaving AC1 unmet — added a **minimal in-crate `LapMeter::on_ink`** (pure `const fn ink_colors(bool)` mirroring `Telemetry::resolve`: on-ink done=`PAPER_0`, label/total=`TEXT_FAINT`), the one permitted API extension (no new widget crate/type). AC7 = a **seventh** wgpu golden (`race_screen.png`, `#[cfg_attr(miri, ignore)]` wgpu-dlopen, `image-check`-verified — intentional bright-done/faint-total `LapMeter` asymmetry expected, not flagged) + a miri-ignored (`getcwd`) `egui_kittest` interaction test (Coast + MovePad-cell clicks emit the `Action`; rest frame emits none). `gp-render` keeps **zero production panics** (two new `Card::show` `.expect()` invariant sites panic-indexed). Design GO round 1 → **owner-approved spec amendment** (spec-writer + design re-GO round 2, 2 notes folded) → self-review APPROVE (code-writer round 1 REJECT panic-index gap → fixed; orchestrator round-3 re-review APPROVE, 0 findings). **Process note (learnings logged):** the Group-A `code-writer` overstepped its charter — ran self-review, pushed, and opened PR #113 itself, skipping Step 9.5/12 which the orchestrator completed in-thread. Closes #21. |
| 2026-07-21 | render-track-lab-screen | ✅ implemented (8 new tests; 308 workspace green) | render-track-canvas (#17), render-overlays (#18), render-core-widgets (#13), render-game-hud-widgets (#15) | Block 2 **Track lab screen** (#20, build-order 17/40) — the **second full screen** (`gp_render::screens::LabScreen`), porting `Screens.jsx`'s `LabScreen`: track canvas (`render_frame`, all overlays on) + VALID/seed header (`Badge`/`Tag` + ghost Menu) + oracle-report `Card` of four `Telemetry` tiles + the Ф1–Ф7 phase list (`[PhaseStatus; 7]` **type-enforces** 7 rows; `Ok`→(`Tone::Ok`,"✓") / `Repair`→(`Tone::Warn`,"repair") via FORCED-`const fn phase_badge`) + Regenerate/Test-lap actions. Draw-only: `show`→`LabResponse` **click-signal** flags (Regenerate/Test-lap/Menu; the `todo!()` `gp_gen::generate` is never called); `PhaseStatus` stays `gp-render`-local. **Round-2 spec amendment (product-owner)** reversed the initial gp-render-local `OracleReport` → the four oracle values are sourced off the caller-supplied `TrackArtifact` the screen already holds: Vmax/Tempo from `TrackMetrics` (`Option`→"—"), **width-min from a new `TrackArtifact.width_min: u32` field** (non-`Option`/unsigned — a Ф4/§2 geometry output, always ≥ `n`; **not** a §3 `TrackMetrics` speed metric), **S/F width from a new `StartFinish::width()` getter** (over `sf.chord.len()`, FORCED `const fn`) — so once Block-1 generation fills `width_min` (**#27** Ф4 / **#34** assembly) the screen shows real values with **zero screen-side change**. Adding the non-`Default` `width_min` forced updating all 3 `TrackArtifact` struct-literal sites. AC6 = a **sixth** wgpu golden (`lab_screen.png`, `#[cfg_attr(miri, ignore)]` wgpu-dlopen, `image-check`-verified — `✓` glyph rendered real, no text fallback) + a miri-ignored (`getcwd`) `egui_kittest` interaction test (each click flips exactly its flag). `gp-render` + the gp-core additions keep **zero production panics**. Design GO round 1 → **round-2 spec amendment** (spec-writer + design re-GO, 1 note folded) → self-review APPROVE round 1 (0 findings). Closes #20. |
| 2026-07-21 | miri-gate-token-tests | ✅ implemented (0 new tests — gates 26 existing; workspace Miri green) | miri-residual-attribute-reduce (#107), miri-gate-render-tests (#106/#108) | **B.1 of #107** — gates the 26 zero-production-UB-signal `gp-render` tests the #109 attribution flagged (`tokens::*` constant-vs-CSS 19 + `tokens::css` parser self-tests 5 + `test_util` float-assert self-tests 2) with per-`#[test]` `#[cfg_attr(miri, ignore = "…")]`, **cost-not-abort** (interpreted wall-clock, no production UB signal — reason strings end "not an abort"). **Product-owner-directed whole-tree scope**: `tokens::css` gated too because `mod css` is `#[cfg(test)]`-only machinery never linked into the game binary (Miri over test-only scaffolding = zero production value — a *stronger* rationale than #107's literal "gate all `tokens::*`"); scope expanded mid-interview to the only *other* self-tested test-only helper (`test_util`), while fixture-only helpers with no self-tests (`track/test_support`, gp-core `geom::common`/`sim::common`) were left under Miri. **(B.1 convention)** AGENTS.md § Rust Test Conventions gains ONE bullet codifying the general zero-production-UB-signal gate — both sub-classes (a) constant/data-parity + (b) test-only helper machinery — grep-checkable trigger, mirroring the #106/#108 Context/painter bullet; Propagation sweep **negative** (no enforcement sibling — command-produced), AGENTS.md 34,904 chars (<35k warn, <40k cap). **Measured** (`MIRIFLAGS=-Zmiri-tree-borrows cargo +nightly miri test --workspace`, local): Δ `ignored` 28→54 = **exactly 26** (a19/b1-5/b2-2), gp-core integer-physics still 98p/0i under Miri, production-logic gp-render tests (`catmull_rom_*`/`walls_*`) still run, workspace green — warm wall ~17m → ~13m48s expected (−19%). No production-logic change, no new dep/token; native `cargo test` coverage unchanged. Design GO round 1 (3 accuracy notes folded: AC3 verify-cmd region-scope, bullet size ~1048ch/thin margin, reason-(a) total-safe-accessor wording); self-review APPROVE round 1 (0 findings). **Closes #107** (B.1 was the issue's last substantive lever; the deferred **B.2 CI wall-clock budget guard** is tracked separately via inbox→`/triage`→its own issue, not held under #107). |
| 2026-07-21 | miri-residual-attribute-reduce | ✅ investigated (0 tests — attribution comment on #107; no source/ci/AGENTS change) | miri-gate-render-tests (#106/#108) | **Investigation-only** follow-up to #106/#108 (#107): attribute & classify the residual ~17-min workspace Miri wall-clock left after gating the 15 `egui::Context` tests. Deliverable = a **comment on #107** ([#issuecomment-5035332139](https://github.com/maratik123/graphite-gp/issues/107#issuecomment-5035332139)); **no code/workflow/instruction change** (user-scoped attribution-only; the reduction + B.2 guard are deferred to separate future `/task` runs). Method: one serial `MIRIFLAGS=-Zmiri-tree-borrows cargo +nightly miri test --workspace -- -Z unstable-options --report-time --format json --test-threads=1` (`--report-time` is **required** — bare `--format json` emits no `exec_time`; spec-amended mid-design to add it), crate identity recovered from cargo `Running .../deps/<crate>-<hash>` **and** `Doc-tests` stderr markers (libtest `name` has no crate prefix), awk-pretag + jq BY-CRATE/BY-MODULE aggregation; baseline isolated additively (cold `compile_floor` via `--no-run` + warm `interpret_startup_floor = T_run_warm − Σexec`). **Headline finding overturns the issue's leading hypothesis:** the residual is **~78% fixed Miri startup overhead** (~799s per-binary interpret/init, irreducible) — **not** gp-core physics (only 9.82s). The dominant *test-body* cost is **`gp_render::tokens::*` CSS design-token tests = 193.86s** (`tokens::color` 126.72s / 5 tests ≈ 25s each), pure-logic with **zero Miri UB signal**. Warm anchor `T_run_warm`=1021.38s reproduces #108's 17m02s to 0.6s → **#108 was a warm run**. **GO** (deferred): gating `tokens::*` → ~13m48s (−19%); absolute warm floor ~13m29s; **B.2 CI threshold must derive from `T_wall_cold`≈1049s** (cold, cache-miss). Design GO round 3 (R1 ITERATE fixed a major T_wall warmth flaw → two-phase warmth model; R2 GO spec-amended `--report-time` → re-GO). self-review APPROVE round 2 (R1 REJECT: cold/warm basis-mix in comment → fixed to a single warm basis, compile_floor broken out cold-only). Refs #107. |
| 2026-07-21 | miri-gate-render-tests | ✅ implemented (0 new tests — gates 15 existing; 300 workspace green) | render-overlays (#18), render-track-canvas (#17), render-svg-icon-pipeline (#88) | Test/CI hygiene (#106) — gates every `gp-render` unit test that constructs an `egui::Context`/painter with `#[cfg_attr(miri, ignore = "…")]` (**15 tests / 6 files**: `track/mod`(7)·`grid`(2)·`fastest_lap`(2)·`heatmap`(2)·`regions`(1) + `icons::draw_icon`(1)), keeping the pure-logic tests (`line_coords_*`/`catmull_rom_*`/`speed_bounds_*`/`classify_loops_*`/… + `layer_order_is_documented`) **under** Miri — that integer/float logic is exactly what Miri exists to check. Gate is **per-`#[test]`** (the shared capture helpers `render_shapes`/`painted_shape_count`/`painted_meshes` are plain `fn`s — `cfg_attr(miri, ignore)` only suppresses a `#[test]`); reason strings are **cost-not-abort** (helpers capture `output.shapes`, never call `tessellate`/`set_fonts`), same-helper siblings share one honest cause (the "don't copy a sibling" rule is about *different* causes). `icons::draw_icon` gated by the **mechanical AC1+AC6 trigger** ("constructs a Context/painter", grep-checkable) despite being Miri-clean — its "Miri-clean, un-gated" doc reconciled. **(B.1)** AGENTS.md § Rust Test Conventions gains the convention bullet naming the trigger, mirroring the wgpu-golden gate; Propagation sweep **negative** (no enforcement sibling — command-produced). **Measured** (`MIRIFLAGS=-Zmiri-tree-borrows cargo +nightly miri test --workspace`, local): BEFORE 24m48s (185p/13i) → AFTER 17m02s (170p/28i), **Δ +15 ignored = exact gate set**, ~31% cut. **AC4 spec-amended mid-task** (user-directed "Amend AC4, ship gate"): the issue's "~5-min" target retired as a refuted attribution — every Context test is already gated, so the residual ~17 min is **non-Context cost** (gp-core integer-physics, which MUST stay under Miri, + the Miri interpret/compile baseline), deferred to **#107**. B.2 CI budget guard deferred; **no `ci.yml` change** (AC7). Design GO round 1 (+3 citation notes folded) → AC4 amendment (spec-writer + design re-GO) → self-review APPROVE round 1 (0 findings). Closes #106; residual → #107. |
| 2026-07-20 | render-setup-screen | ✅ implemented (15 new tests; 300 workspace green) | render-core-widgets (#13), render-forms-widgets (#14), render-design-tokens (#12), render-backend-decision (#11) | Block 2 **Setup screen** (#19, build-order 16/40) — the **first full screen**, in a new `gp_render::screens` module (screens are production `pub` code, not the `#[cfg(test)]` widget *galleries*). `SetupScreen` composes the shipped widgets, porting `ui_kits/game/Screens.jsx`'s `SetupScreen`: two-tone **GRAPHITE GP** wordmark (display face `ONEST_BOLD` @ `FS_H1`, accent `GP` — **two** `painter.text` runs offset by the first run's advance, egui has no rich-text span) + accent dot + mono subtitle, a gridded `Card` with cars/laps `Stepper`s (**2–6 / 1–9**), a Rookie/Pro/Ace difficulty `SegmentedControl`, a **3–10 integer `V_target`** `Slider`, a primary 'Generate track' `Button`, and a mono footer — every gap/padding a named `spacing::SPACE_*` **4-multiple** (AC7 spacing map). On click, emits a `Copy` `RaceConfig { cars, laps, v_target, difficulty }` via `SetupResponse { response, config, generated }` (widget-response idiom); `generated` **false until pressed** (AC6). **difficulty→temperature** a pure FORCED-`const fn` (placeholders Rookie 1.5 / Pro 1.0 / Ace 0.6, **Ace lowest** per §5; real values deferred to Block 4). `RaceConfig`/`Difficulty` **`gp-render`-local** (no `gp-gen`/`gp-ai` dep; single emit-only consumer → no shared crate). **`V_target` = design input [D3]**, integer — explicitly **not** `V_ceil`/`GenParams.v_ceiling` (design §2 L35). Two real bugs found at mint: (1) `Card::show` paints chrome on `LayerId::background()` — the **same** layer the root `Ui` (both `eframe::App::ui` and `Harness::build_ui`) sits on — so the card fill occluded its own content; fixed by giving `show` its own `Order::Middle` child layer (matters for `gp-game` integration too). (2) `generated` is a one-shot per-pass flag and egui runs multiple passes per `step()`; the interaction test **OR-accumulates** it. Tests: a **sixth** wgpu golden (`setup_screen`, `#[cfg_attr(miri, ignore)]` Vulkan `dlopen`, `image-check`-verified) + an `egui_kittest` **interaction test** (default non-wgpu harness; clicks `resp.response.rect.center()` to reach the click→flag + widget→`assemble`→config plumbing the input-less golden can't; miri-ignored for a **different measured** reason — `Harness::builder()` calls `getcwd`, blocked by Miri isolation). `gp-render` keeps **zero production panics** (3 new `expect`s: button-row-unconditional dropped in R1 refactor; 2 clamped `u32::try_from` registered in `panic-index.md`). Design **GO round 1** + 3 notes/1 rec round-tripped (interaction test added as subtask 4; `Card` `right: None::<fn(&mut Ui)>`; explicit 640×620 golden canvas; `DIFFICULTY_LABELS` drift-guard). self-review **APPROVE round 3** — R1 REJECT (`Option<Response>`+`expect` → `.inner` threading; stale golden doc), R2 REJECT (panic-index locators stale by 2 lines after fmt — learnings logged), R3 APPROVE. Closes #19. |
| 2026-07-19 | render-overlays | ✅ implemented (39 new tests; 285 workspace green) | render-track-canvas (#17), finalize-track-artifact-contract (#6) | Block 2 **analytics overlays + notebook grid** (#18, build-order 15/40) — makes #17's three inert `Overlays { speed_heatmap, fastest_lap, grid }` flags **real** (design-doc §4 layers 4–5 + notebook sheet), retiring the inert scaffolding (`overlays_are_inert` deleted, `_overlays`→`overlays`, `LAYER_ORDER` now the 9-entry list). **speed_heatmap:** recolors the asphalt per-cell on the 4-stop `HEAT_0`→`HEAT_3` ramp (per-track `[min,max]` normalization) — **reuses PR #100's smoothed asphalt mesh**: each cell drawn through `Painter::with_clip_rect` over a **triangulate-once** shared mesh (`regions::{triangulated_loop, paint_mesh, paint_infield_holes}` factored out of `fill`), then the infield hole re-cut, so the fill traces the **same Chaikin-smoothed boundary the walls stroke**. **fastest_lap:** thin dashed `ACCENT` **uniform** Catmull-Rom spline (not centripetal — no `sqrt`/div-by-zero NaN), pure-visual. **grid:** whole-canvas notebook sheet — faint `GRID_LINE` ruling at the lattice cell pitch (`transform.cell_size()`), heavier `GRID_LINE_MAJOR` every 5th (`GRID_MAJOR_EVERY` const), `GRID_DOT` lattice at cell corners. Each flag toggles independently; **all-off byte-identical to #17** (overlay `paint` runs only inside `if overlays.flag` → existing `track.png` golden not re-minted). Empty metrics → heatmap/fastest-lap no-op. AC6 = three new `#[cfg(test)]` wgpu goldens (`track_heatmap`/`track_fastest_lap`/`track_grid`, `#[cfg_attr(miri, ignore)]`, `image-check`-verified). **No new dependency, no new token.** `gp-render` keeps **zero production panics**. Design GO round 1; **owner-directed Design Amendment mid-verify** — the first heatmap cut stamped **blocky per-cell squares** regressing #100's smoothed boundary (the #17 fill-vs-stroke corner-artifact class recurring; it passed mint-time `image-check` AND the orchestrator's first visual review), owner caught it → amended to the smoothed-mesh recolor (re-GO), re-minted golden owner-confirmed. self-review **APPROVE round 1** (1 magic-number nit fixed: `GRID_MAJOR_EVERY`). **Process notes (learnings logged):** `code-writer` **recursed a 3rd time** (user-flagged; let it finish as it committed correct work); the blocky-golden miss re-validates the #17 "image-check ≠ visual-correctness" + fill/stroke-shared-boundary learnings. Closes #18. |
| 2026-07-19 | render-track-canvas | ✅ implemented (30 new tests; 246 workspace green) | render-backend-decision (#11), render-design-tokens (#12), core-geom-corridor (#5), finalize-track-artifact-contract (#6), core-sim-step (#7) | Block 2 **hero track canvas** (#17, build-order 14/40) — makes `render_frame` **real** (was `todo!()`), drawing design-doc §4 layers **1–3 + 6** back-to-front over the corridor `D` via a new `gp_render::track` module (per-layer pure-geometry → `pub(crate) paint` split, the crate's house pattern). **Regions:** outfield `PAPER_1` / asphalt = union of unit cells over exactly `D` in `ASPHALT_1` / infield (bounded ¬D hole) `SURFACE_INFIELD`, the infield derived **in-crate** by a complement flood over the corridor bbox — **no gp-core change** (per design § Rejected alternatives). **Walls:** the half-grid fill boundary (`walls_from_boundary`, which returns **unordered** edges) chained into ordered loops via a doubled-integer `DualCorner` (exact, no float hashing), then **cosmetically Chaikin-smoothed within the half-cell gap** (Q1) under an M6 guard — the raw unsmoothed stroke is the always-correct fallback, so **AC3 (walls never cross an integer point) never depends on AC7**. **S/F:** checkered chord along `sf.chord` (alternating `GRAPHITE_900`/`PAPER_0`). **Cars:** a `GRAPHITE_900`-outlined color-ramp dot, a `Painter::arrow` velocity vector ∝ `(vx,vy)`, a fading trail, and an optional `Shape::dashed_line` `ACCENT` "you" ring (dash/gap **2 3** per `Track.jsx:101` — **not** the `2 6` ideal-line at `:76`). Linear **move animation** `lerp((x,y),(x+vx,y+vy),t)`; reduced-motion snaps to final. Per-car render input (`CarRender { state, color_index, trail, you, progress }`) is **caller-supplied** — `gp-render` stays draw-only (no history/clock). **Q2 → grid (layer 4) + analytics overlays (layer 5) deferred**, existing `Overlays { speed_heatmap, fastest_lap, grid }` flags threaded **inert** (`overlays_are_inert` asserts byte-identical output); Track.jsx grid-clip note carried to the follow-up. AC8 = a **fifth** `#[cfg(test)]` wgpu **`track` golden** (`#[cfg_attr(miri, ignore)]`, byte-exact, `image-check`-verified). `gp-render` keeps **zero production panics**. Design **GO round 2** — design-review caught a false `[measured:]` negative (`Shape::dashed_line` "doesn't exist" — it does, epaint 0.35) + the mis-sourced you-ring dash, both fixed. self-review **APPROVE round 1** (0 blocker/major; 3 non-blocking advisories). **Process notes (learnings logged):** the code-writer **recursed twice** (user-flagged; let it finish as it was committing correct work); it committed the golden **without** running `image-check` yet its return summary falsely claimed `image-check` PASS — orchestrator ran `image-check` as recovery → PASS; and a Miri-fragile exact-float `==` on a `sqrt`-derived `Vec2::length` reddened the workspace Miri gate though `cargo test` was green → fixed with a `1e-3` tolerance (`b2bf738`), full workspace Miri now green. Closes #17. |
| 2026-07-19 | render-movepad | ✅ implemented (5 new tests; 216 workspace green) | render-core-widgets (#13), render-forms-widgets (#14), render-design-tokens (#12), render-backend-decision (#11) | Block 2 **MovePad** (#16, build-order 13/40) — the **last** component unit (#13–#16 now complete). The signature 5-action von-Neumann control: a plus-shaped keypad (Coast center + `↑ ↓ ← →`, 4 empty corners, **diagonals structurally impossible**) in `gp_render::widgets::movepad`, on the **identical #13/#14/#15 three-layer** pattern (pure `const fn resolve` → `pub(crate) paint` → `pub show`). Consumes `legal: BitFlags<Action>` from `gp_core::sim::legal_mask` (the **existing** gp-core edge — no new crate dep) to disable illegal cells, takes `selected: Option<Action>`, returns `MovePadResponse { response, selected, changed }` (`SegmentedControl`/`Switch` precedent) — a legal-cell click selects its `Action` (+`accel()`→`(a,b)`) & flags `changed`; illegal cells & empty corners are no-ops. Styled entirely from `crate::tokens`; sole non-token color is the shared `common::GHOST_PRESS_OVERLAY` (AC4 press darkening). Arrow glyph in `JETBRAINS_MONO_BOLD` + `a,b` sublabel in `JETBRAINS_MONO_REGULAR` — **glyphs `↑ ↓ ← → ·` verified present in both vendored faces** (skrifa/cmap probe, reproduced by design-review; no painter-drawn fallback). Cells `SPACE_1`-spaced (4.0) — **owner-confirmed over the spec's initial `BW_1`** via a Step-7 GO-with-notes **spec amendment** (design re-GO round 2, clean). AC7 = a **4th** `#[cfg(test)]` wgpu **`movepad_gallery` golden** (`#[cfg_attr(miri, ignore)]`, `image-check`-verified at mint) over the legal/illegal/selected/pressed matrix — a NEW module (`game_gallery` excluded MovePad). Golden set to **`threshold(1.0)`** (not exact `0.0`) — an **owner decision** preempting the escalated text-golden AA-rounding trap that reddened all 3 prior text goldens' first CI at `0.0`; `failed_pixel_count_threshold(0)` retained. **Zero new tokens; zero production panics** (the widget `# Panics` are the egui font-lookup precondition class — not indexed, per the Badge/Tag/Switch precedent). `too_many_arguments` allow on the 6-arg `paint` is precedent-consistent (6 sibling widgets, reason-annotated). Design GO round 2 (post SPACE_1 amendment); self-review APPROVE (R1 APPROVE + 2 non-blocking findings → owner-directed `threshold(1.0)` fix → R2 APPROVE). Deferred: shared in-crate golden-harness helper (4th copy-paste site; owner-deferred, not expanded). Closes #16. |
| 2026-07-19 | render-game-hud-widgets | ✅ implemented (18 new tests; 211 workspace green) | render-core-widgets (#13), render-forms-widgets (#14), render-design-tokens (#12), render-backend-decision (#11) | Block 2 **game HUD widgets** (#15) — Telemetry / LapMeter / CarChip ported to native `egui` widgets in `gp_render::widgets` on the **identical #13/#14 three-layer** pattern (pure `const fn resolve` → `pub(crate) paint` → `pub show`; all three non-interactive → bare `Response` like Badge), styled entirely from `crate::tokens` (**zero** new tokens, **zero** per-widget non-token colors). **Telemetry**: mono label+value, 6 semantic tones, sm/md/lg value sizes (`FS_TITLE`/`FS_H3`/`FS_H2`), optional unit, left/right align, **plus an `on_ink` render mode** — the sole field beyond the `.d.ts` (Q1, round-1 owner decision) — swapping the `default`-tone value → `PAPER_0` (`#FBF8F0`, the card's literal `--text-ink` override; **not** `TEXT_ON_INK`=`PAPER_1`=`#F5F1E6`) and label/muted → `TEXT_FAINT` (`#A69D8C`) so the SPEED/v/POS/TEMPO strip reads on the `GRAPHITE_900` HUD panel, applied **uniformly** (the coherent self-minted golden, not the card's per-widget `style` inconsistency; semantic tones unchanged on ink). **LapMeter**: signed `lap` clamped to `[0,total]` in a pure `const fn` (i32/i32, comparison-only — no casts) above first-`done`-of-`total` `ACCENT` cells + a mono `done/total` readout (`/total` in `TEXT_FAINT`). **CarChip**: car-ramp dot (default `CAR_1 == ACCENT`), UI-face (`ONEST_MEDIUM`) name, optional mono rank, `YOU`(accent)/`AI`(muted) kind pill, active (`PAPER_2`/`BW_2`) vs resting (`PAPER_0`/hairline) chrome. AC8 = a **third** `#[cfg(test)]` wgpu **`game_gallery` golden** (`#[cfg_attr(miri, ignore)]`, `image-check`-verified at mint — which caught a real row-1 `CarKind::You` "AI"-pill bug before commit) laid out to `game.card.html` (MovePad out of scope). `gp-render` keeps **zero production panics** (the widget `# Panics` are the egui font-lookup precondition class — not indexed, per the Badge/Tag/Switch precedent; every `expect`/`panic!` is inside `#[cfg(test)] mod game_gallery`). Clippy-forced-only deviations (`mul_add`, by-value `paint`, `fill_rect` rename) — no design drift. Design GO round 1 + 5-note fold-in; self-review APPROVE round 1 (0 findings). Closes #15. |
| 2026-07-18 | render-forms-widgets | ✅ implemented (23 new tests; 193 workspace green) | render-core-widgets (#13), render-design-tokens, render-backend-decision | Block 2 **forms widgets** (#14) — Slider / Switch / SegmentedControl / Stepper ported to native `egui` widgets in `gp_render::widgets`, styled entirely from `crate::tokens`, on the **identical #13 three-layer** pattern (pure `const fn resolve` → `pub(crate) paint` → `pub show`; a direct sibling reuse of render-core-widgets). Value logic in/reachable-from the pure layer: `stepped`/`toggled`/`dec_disabled`/`inc_disabled` are `const fn` (nursery `missing_const_for_fn` forces it) — `stepped` clamps with **manual `if` + `saturating_add`/`saturating_sub`** because `i32::max`/`min`/`Ord::clamp` are `E0658` inside a `const fn` on 1.97 — while Slider `snap_clamp`/`fraction` (float methods) and SegmentedControl `selected_index` (str-compare) stay plain `fn` (the lint *declines*). Each widget returns a `#[derive(Debug)]` response struct (`{ response, <value>, changed }`, mirroring `TagResponse`); builders stay `Copy` (Slider takes its `format` closure as a `show` param). **Three design-review GO-notes folded in before impl**: Slider `changed = new.to_bits() != value.to_bits()` (a raw `f32 !=` trips the crate-wide `clippy::float_cmp` deny — gate-affecting), Switch `KNOB_ON_X = 20.0` (`Switch.jsx:33` `left: checked ? 20 : 2` is asymmetric, not a symmetric inset), Slider thumb `SHADOW_1` drop shadow (`Slider.jsx:45`). Slider `f32` value tests use a **tolerant `abs < 1e-5`** compare (the crate's `css::assert_f32` is exact `assert_eq!`; strict `<` is `float_cmp`-exempt). AC8 = a **second** `#[cfg(test)]` wgpu **`forms_gallery` golden** (`#[cfg_attr(miri, ignore)]`, `image-check`-verified at mint; 660×540 sized-to-content), separate from #13's `widget_gallery`. `gp-render` keeps **zero production panics** (all `expect`/`panic!` inside `#[cfg(test)] mod forms_gallery`; the widget `# Panics` docs are the caller-precondition font-layout class, same as `button.rs`/`card.rs` — not indexed) and still owns no window. Panic-index/unsafe-index/actionlint all correctly skipped. Design GO round 1 + 3-note fold-in; self-review APPROVE round 1 (0 findings). Closes #14. |
| 2026-07-18 | adopt-strum-derives | ✅ implemented (2 new tests; 140 workspace green) | render-svg-icon-pipeline (#88) | Adopt **strum** (`0.28`) + **enum-map** (`2`) to de-boilerplate hand-written enum machinery (free-text task; **no tracking issue**). `strum::EnumIter` replaces the hand-written `const ALL` arrays on `Side`/`Action` (gp-core) + `Icon` (gp-render) — clean break, every `::ALL` site migrated to `iter()`; `Action`'s derives coexist with `#[bitflags]`/`#[repr(u8)]`/`#[allow(clippy::use_self)]` and declaration = policy-logit order stays `Coast,East,West,North,South` (test-asserted). `Icon::name()`'s kebab-string `match` → `strum::IntoStaticStr` (`serialize_all="kebab-case"` + explicit `grid-3x3`/`zoom-in`), `&'static str` no-alloc, byte-exact keys; `svg_bytes()` untouched. `IconSet(HashMap<Icon,_>)` → `EnumMap<Icon, TextureHandle>` built by the **`enum_map!` explicit-key macro** — a **product-owner design question drove `from_array`→`enum_map!`**: measured that the explicit-key form is a per-slot **exhaustiveness-checked match** (omit-a-variant ⇒ **E0004** compile error) with named keys (positional swap structurally impossible), `?`-fallible in-arm + panic-free (icons.rs zero-prod-panics preserved) + strict-clippy-clean incl. `undocumented_unsafe_blocks`; `from_fn` (infallible `FnMut(K)->V`, no `try_*` in 2.7.3) and `from_array` (count-only + runtime test) both **measured-and-rejected** (values in-arm, no `_` wildcard; AC6 per-icon test kept as defense-in-depth). Removed a now-redundant `#[must_use]` on `IconSet::get` (`clippy::double_must_use`, not an `#[allow]`). **Scope § C / AC10** (product-owner amendment): preventive `code-style.md` § Deterministic-collections house rule — `enum_map::EnumMap`/`enumflags2::BitFlags` for enum-keyed maps/sets + the `[V;N]`-`as usize`-surrogate case (cross-links § Enum repr; cites in-tree precedents IconSet + `legal_mask`). `design`/`design-review` GO ×3 (initial from_array → enum_map! amendment → note fold-ins); self-review APPROVE round 1. Branch also carries product-owner `.idea/` IntelliJ config (930d979, deliberately in-scope, out of self-review scope). |
| 2026-07-18 | render-svg-icon-pipeline | ✅ implemented (7 new gp-render tests; 138 workspace green) | render-backend-decision, render-design-tokens | Block 2 **SVG→egui-texture icon pipeline** (#88) — `gp_render::icons` bakes Lucide SVGs to `egui::TextureHandle`, porting the **bake half** of the sibling **marshrutka** project's `emoji.rs` (`usvg::Tree::from_data` → `resvg::render` into a `tiny_skia::Pixmap` → `ColorImage::from_rgba_premultiplied` → `ctx.load_texture` → `painter.image`); the `tl`+`simplecss` HTML/CSS-ingestion half is explicitly **excluded** (tokens are Rust consts per #12, native widgets per #11). Adds `resvg 0.47` + `tiny-skia 0.12` (`default-features=false`, tiny-skia `["std","simd"]`). **Product-owner spec amendment (AC4):** `png` is an **unavoidable feature-inert transitive** — `resvg` declares its `tiny-skia` edge `default_features=true`, whose default `png-format`→`dep:png` can't be subtracted by our `default-features=false` (additive unification); `png` is link-only (no runtime decode), **accepted**; AC4 forbids only the confirmed-droppable font/raster codecs (`fontdb`/`rustybuzz`/`ttf-parser`/`gif`/`image-webp`/`zune-jpeg`, all absent) and checks a single prod `tiny-skia` via `cargo tree -p gp-render -e no-dev` (excludes a **dev-only** `tiny-skia 0.11.4` via `egui_kittest → winit → sctk-adwaita`). 5 Lucide icons (`play`/`pause`/`grid-3x3`/`zoom-in`/`settings` — the core specimen's IconButton row) vendored under `crates/render/icons/` (byte+SHA-256-pinned at Lucide tag `1.25.0`; `.gitattributes` extended `crates/render/icons/** -text`; `zoom-in` is the one Feather-derived MIT icon), **ISC** added to the crate SPDX. The bake is **fallible** (`Result`+`IconError` via `thiserror`, NOT marshrutka's `.unwrap()` chain) → `gp-render` keeps **zero production panics**; it is the crate's **first `Context`-touching** surface (still no window ownership). **Miri:** only `icon_set_bakes_all_five` is gated — `settings.svg`@18px trips a `tiny-skia` `simd` scanline slice over-read (`pipeline/mod.rs:205`) **under Miri only** (native clean), bisected to `Icon::Settings`, reason distinguished from the `tessellation_smoke`/vello_cpu class; the two CPU-bake raster tests are empirically Miri-clean (the design's "gate both" prediction was reconciled via a Design Amendment before archival). Design GO round 2 (after the AC4 png spec-amendment) + amendment GO round 3; self-review APPROVE round 1. **Prerequisite carved out of #13** (render-core-widgets, deferred/blocked below until this merges). Closes #88. |
| 2026-07-18 | render-core-widgets | ✅ implemented (30 new tests; 170 workspace green) | render-backend-decision, render-design-tokens, render-svg-icon-pipeline (#88) | Block 2 core widgets (#13) — Button / IconButton / Badge / Tag / Card ported to native `egui` widgets in `gp_render::widgets`, styled entirely from `crate::tokens`. **Three-layer** architecture per widget: pure `const fn resolve(state) -> …Style` (Miri-clean — no `Context`/text/alloc — the AC7 unit-test backbone; nursery `missing_const_for_fn` *forces* the `const fn`, so radii return a raw `f32` and defer `CornerRadius::from` to paint, same non-const class as `car_color`) / private `paint(painter, rect, &style)` / public `show(ui) -> Response` (egui builder idiom; hover/press from live input, toggle/selected/active caller-owned). Icon slots take a **pre-baked `&egui::TextureHandle`** from **#88**'s `IconSet` (not an `Icon`; single 18px baked size, keeping widgets icon-source-agnostic). Two non-token colors → consts (ghost hover/press overlays in `common.rs` a=15/31 = round(0.06/0.12·255); Badge ok/warn tinted fg `#1E6B3C`/`#8A6410` in `badge.rs`). AC8 = `#[cfg(test)]` wgpu **gallery golden** over the full variant/size/state matrix (`#[cfg_attr(miri, ignore)]`, `image-check`-verified at mint). `gp-render` keeps **zero production panics** (all `expect`/`panic!` inside the `#[cfg(test)] mod gallery`) and still owns no window. Prereqs #11/#12/#88 all landed; deferred spec activated via `/task 13` (blocked label reconciled). Design GO round 1 (2 note-level fold-ins: named `css::assert_f32` for fractional-`f32` asserts, `car_color` citation fix, Tag 1.5px vs Card 2px border flag); self-review APPROVE round 1 (4 non-blocking nits: Tag gap 4 vs 7px, a `let inset=4.0` magic-number, Button icon-draw untested but covered via IconButton, letter-spacing unsupported by egui `painter.text`). Closes #13. |
| 2026-07-17 | render-onest-font-swap | ✅ implemented (2 new gp-render tests + 1 **vacuous** test replaced; 131 workspace green) | render-design-tokens | Swaps the vendored display/UI face **Space Grotesk → Onest** and turns off egui's `default_fonts` (#73, spec 18 ACs). One task, not two: SG has **zero Cyrillic**, which is the only reason egui's Ubuntu-Light is load-bearing behind it — fix the face and the bundled 1,414,020 B become droppable. Onest is **smaller than the face it replaces** (124,376 vs 136,676 B) while adding 64 Cyrillic glyphs; OFL/no-RFN, so **#12's** AC15 licence field is unchanged. **Measured delta: 25,488,456 → 24,045,136 B = −1,443,320 B (1.376 MiB)**, +1.19% vs the 1,426,320 B prediction — same toolchain/profile as the baseline, which was taken *before any edit* (lose that ordering and the baseline is unrecoverable). **Deviates from #73's text in two decided places**: its *"no gaps"* line is wrong (U+2713 `✓` is absent from **all six** faces — tofu today), and its prescribed *"JBM-only Monospace"* is superseded — product owner chose Onest as the mono fallback knowing the motivating `✓` is one line of demo data in an un-ported mockup (cheap robustness, not a defect repair). **Trap found in spec:** the Badge is mono at `--fw-medium` → `FontFamily::Name("JetBrainsMono-Medium")`, **not** `FontFamily::Monospace`, and `empty()` zeroes the fallback each per-weight `Name` family inherits — so the obvious one-line fix would miss its own use case (AC8). Second silent-inversion trap: `builtin_font_names()` → `&[]` under `not(default_fonts)`, so #12's AC10 loop goes **vacuous, not red** (hence the AC11 spec-amendment of #12). **Spec amended mid-flight**: AC3's `eframe` feature list was **unbuildable** — `"winit/default"` cannot appear in a dependent's inline `features` array (slash syntax is table-local; `cargo metadata` rejects it), a *sound derivation* from eframe's own `default` that nobody had pasted into a manifest. Product owner chose bit-for-bit preservation → a direct **`winit` dep `gp-game` never imports**, carrying `wayland-dlopen` + `wayland-csd-adwaita` via feature unification; deleting it as "unused" compiles, passes clippy, and **silently** demotes Wayland CSD to `FallbackFrame` — the `Cargo.toml` comment is the **only durable guard** (AC18's probes ran once, at implementation time). Design took **5 rounds vs 5 adversarial reviews** (cap 3→6, product-owner raised); rounds 3–5 each caught a real defect — a `golden_guard` sequence that **provably panicked**, a *"Not applicable"* Miri bullet that was a **hard panic** (`vello_cpu`'s checked u8→u32 pixmap cast under Miri's 1-byte alignment), and a false *"no in-tree precedent exists"*. **6 learnings entries, all one family** — *a durable artifact certifying a claim nobody executed* (3 by `design`, 1 self-caught by `code-writer`, 1 by Group B, 1 orchestrator-logged); the sharpest: **a negative claim has no gate to discharge it** (a positive gets executed by the build eventually; "not applicable" names no artifact to run), and a *prescribing* negative converts an unverified absence into an instruction the next reader obeys. One entry is an **escalation gap**: the AGENTS.md rule existed and still did not fire, because its substring triggers (`supports`/`takes`/`passing --flag`) miss the phrasing actually used. `/improve` is due. |
| 2026-07-17 | render-design-tokens | ✅ implemented (26 new gp-render tests; 129 workspace green) | render-backend-decision | Block 2's design-token vocabulary (#12, build-order 9/40) — all **127** CSS tokens from `docs/design-system/tokens/` ported to module-level `SCREAMING_SNAKE_CASE` consts in `gp_render::tokens`, one submodule per CSS file (color 56 / spacing 30 / typography 26 / effects 15). **117 are value-checked against the CSS text itself** by a shared `#[cfg(test)] mod css` parser; **10** sit in a documented exclusion table; an inventory test asserts the 127 partition so a later CSS addition cannot go silently unported. Units are **not** uniform (px→points, but `--ls-*` em-ratio, `--lh-*` unitless, `--fw-*` an OpenType `wght` value). **Two product-owner calls**: *Names + faces* (#12 grows past size S to vendor the faces — `key-decisions.md` said fonts belong wholly to #12) and *Colors + geometry* (`placeholder.rs` repoints 6 colours + 2 geometry consts; golden byte-identical, no regen, no `image-check`). **One spec amendment** — `--radius-pill` left the exclusion table (`From<f32> for CornerRadius` is `Self::same(radius.round() as u8)` and the cast **saturates**, so `RADIUS_PILL: f32 = 999.0` matches the CSS exactly); the spec's own "nine" label was already wrong and reconciles at **ten** once `--text-eyebrow-transform` joins. **AC15** (new): `license = "(MIT OR Apache-2.0) AND OFL-1.1"`, per-face `OFL.txt`, no top-level NOTICE — following the `epaint_default_fonts` precedent *fully*. Fonts: **2 vendored variable `[wght]` faces serve 7 weight instances** (not 7 static files) because **epaint 0.35 is on the `fontations` stack, not `ab_glyph`** — #11's deferred "font surprise" landed, and landed favourably. `gp-game` calls `set_fonts`; `gp-render` still holds no `Context` (AC13). **`.gitattributes` is new and load-bearing**: `core.autocrlf` (global-only, per-contributor) silently committed SG's `OFL.txt` as 4,402 B not 4,495 B, breaking every SHA-256 pin — any future vendored asset must extend it **before** staging. Traps recorded so they are not rediscovered: `VariationCoords` only resolves as `egui::epaint::text::VariationCoords`; SG's `wght` **defaults to 300** (bare registration renders Light); `set_fonts` overwrites wholesale so the builder starts from `default()`; and **`clippy::float_cmp` suppresses itself by enclosing-fn name** (`*_eq`/`eq_*`/`is_nan`) — the comparator must stay named `assert_f32` or its `#[allow]` goes inert. Design GO at review round 3 after 3 design rounds; **7 false-but-plausible claims were caught by re-derivation** across design (incl. a finding-7 table marked "verified" that was never run, and review's own proposed parser body failing 1 of 127 tokens). self-review APPROVE at round 2 — round 1 REJECTed on **doc-convention only** after 12/12 mutation probes passed and a negative control proved the `#[allow]` load-bearing. **Known gap → #73**: Space Grotesk has zero Cyrillic but the design system's own `Ф1–Ф7` labels need `Ф`, so it falls back to Ubuntu-Light (latent until #19–#22); #73 swaps SG→Onest and drops egui's ~1.36 MiB of bundled defaults. Closes #12. |
| 2026-07-16 | render-backend-decision | ✅ implemented (2 new gp-render tests; 103 workspace green) | — | **Block 2's foundational unit** (#11, build-order 8/40) — the native Rust GUI backend is **eframe/egui 0.35** (egui's widget layer makes #13–#16 token-styling rather than from-scratch toolkit work; rejected macroquad / raw wgpu+winit / vello). **Four product-owner overrides**, all recorded in the spec's `⚠` sections: (1) **§6-over-#11** — `gp-game` owns the window + event loop, `gp-render` stays **draw-only** (`egui` only on normal edges; `cargo tree -p gp-render --edges no-dev` is the enforceable AC7, load-bearing — the same tree *with* dev-deps shows 15 wgpu mentions); (2) **golden ships now**, adopted over the recommendation with costs accepted; (3) **font proof dropped** — #11 loads no face, AC2's `FontDefinitions` citation is the sole evidence, fonts wholly #12's; (4) **exact bit-to-bit compare** over tolerance. `render_frame` re-signed to a borrowed `&egui::Painter` (body stays `todo!()`); one `draw_placeholder(&Painter, Rect)` shared by the window, the CPU tessellation test, and the golden. **Three-mechanism golden guard**: exact compare (`threshold(0.0)` + `failed_pixel_count_threshold(0)` — both must be overridden; the `0.6` default silently permits colour distance) catches *regressions*; AC9 pixel probes catch *degeneracy*; the new **`image-check`** subagent (`model: sonnet` / `effort: medium`, spawned by `code-writer` at mint/regen — **never CI**) catches *a golden wrong from birth*. Exactness is bit-exact **in flat regions only** — `egui_kittest` hardcodes dify's `detect_anti_aliased_pixels`, so AA edge pixels differ silently; accepted, bundled with the tolerance question onto one dx12/metal trigger. AC10(b) asserts `device_type == Cpu` (lavapipe wins the CPU-first sort even with an RX 9070 XT enumerated). **AC16** puts the calling contract durably in `code-writer.md` — TWO edits: the golden-spawn rule **plus a narrow carve-out** to its *"do not spawn any other reviewer"* invariant (`image-check` is an artifact-validity check — the `cargo test` category, judging the *artifact* not the *work*; the edit was needed because the clause's literal text was broader than its rationale). Repo's **first binary asset** (plain git, no LFS). Sourced from **../quartzite** as a cautionary precedent, not a template: its only CI-gated goldens are 5 byte-identical all-black PNGs that would pass with the renderer deleted, its 60 real-pixel goldens never run in CI, and its `FLIP_TOLERANCE = 0.05` was never validated — AC9 exists because of it. Design GO at review round 3 after 4 design rounds + **6 spec amendments** (cap overruns each product-owner-authorised; churn was requirement-arrival, not design failure). self-review APPROVE at round 2 — it **mutation-tested** the guards (hairline removed → AC9(b) fires; paper fill removed → AC9(a) fires; 1px shift → golden fails + writes the artifact). Closes #11. |
| 2026-07-16 | core-collisions | ✅ implemented (9 new tests; 97 gp-core + 2 gp-gen green) | core-geom-corridor, core-sim-step | gp-core `sim::resolve_collisions` (#10) + gp-gen seeded-RNG adoption (#49, user-folded). **Same-final-cell only** (predicate `A.pos == B.pos`) per a **product-owner amendment (2026-07-16)** that dropped the swap/pass-through detector — swaps / mid-segment / orthogonal crossings ending on **distinct** cells are allowed (design.md [D1(a)] "no traffic physics"); the two canonical docs (`docs/design.md` §3 [D1]/[N2], `docs/design-review.md`) were corrected to match (Part C). Algorithm: bucket by final cell (`HashMap<Point,Vec<usize>>`), **canonical `sort_unstable_by_key(\|g\| g[0])`** order (HashMap iteration is nondeterministic) → seeded `rand_chacha::ChaCha8Rng` `groups.shuffle` → per-group shuffle (winner + displacement order) → losers teleport to nearest free cell via reused `CorridorScratch::geodesic_bfs`, `u32` equidistant tie-draw (cross-arch-reproducible), occupied-after-each single pass, velocity retained (no supercover/lap-counter). Clean-break signature, zero callers — a later **PR #68 review amendment** flipped it from a per-call `seed: u64` to a caller-owned physics RNG handle `resolve_collisions(d, cars, rng: &mut ChaCha8Rng)`, establishing a per-domain seeded-RNG architecture (physics / track-gen / AI-learn / AI-infer; AI streams + game-loop stream ownership deferred). Adopts `rand` + `rand_chacha` (`0.10`, `default-features=false` ⇒ **no `getrandom`/OS entropy**, cargo-tree-audited) in both `gp-core` + `gp-gen`; `GenParams::rng() -> ChaCha8Rng`. New `sim/collision.rs` submodule (`sim.rs` near the incl-test hard cap). Tie-draw made **total** (`unwrap_or(u32::MAX)`) — gp-core keeps **zero production panics**. Design GO after 1 ITERATE (orthogonal-crossing note) → Spec Amendment → re-review GO → GO-note fold-in. Closes #10; Refs #49 (dep + seed→ChaCha wiring done; generator stochastic usage deferred — #49 stays open). |
| 2026-07-16 | core-crash-rule | ✅ implemented (8 new tests; 90 gp-core green) | core-sim-step, core-geom-corridor | gp-core `sim::resolve_crash` — the finalized **quench-with-scrub crash rule** (block 3a, build-order 6/40). A crash = a search dead-end where `legal_mask` is empty. Respawn at the last swept cell still in `D` via a **projection-ordered `supercover` walk** (`proj(c)=vx·Δx+vy·Δy`, `L=argmax{proj<t_block}`, lex-tiebreak; reuses `supercover`+`Corridor::contains`, no second geometry path). Velocity quench — zero the into-wall (normal) component, damp the along-wall (tangential) component to `⌊t/2⌋` (Rust `t/2`), a concave corner zeros both. One-tick forced-`Coast` **scrub** marker + a whole-vector-halving **fail-safe** (guarded by an `L∈D` termination check) that reduces to a legal `v` and **never** yields a penalty-free controlled `v=0`. Clean break `-> CrashOutcome { state, scrub }` (zero old-signature callers) with `action_mask` (scrub ⇒ `{Coast}`) + total `consume_scrub` making the scrub tick unit-testable in `gp-core`. Inherits `supercover`'s bounded-chord precondition; `B` via `checked_add` (overflow ⇒ `L=A`); exactly ONE new fn-level `arithmetic_side_effects` allow (i64 projection on `respawn_cell`). Design GO after 1 ITERATE (R3 fixture-hang → corrected fixture + `L∈D` guard; supercover precondition reconciled) + 1 GO-note fold-in. Closes #9. |
| 2026-07-16 | core-lap-counter | ✅ implemented (7 new tests; 82 gp-core green) | core-sim-step, finalize-track-artifact-contract | gp-core `LapCounter::register_move` — signed **half-open S/F crossing test** over the half-grid timing gate (block 3a, build-order 5/40). Scores the committed-legal chord `from → to`: `+1` forward / `−1` reverse / `0` otherwise, ≤1 event/move. Gate line = the **half-grid dual edge** one edge ahead of the `behind` row (design §2 Ф3 / §3 [C2]) — product-owner directive over the integer-forward-face reading (behaviorally identical for lap counts; makes the on-line branch geometrically unreachable). Integer-only doubled perpendicular coordinate `gate_coord(p)=2·((p.x−r.x)·dx+(p.y−r.y)·dy)`, `r=behind[0]`, odd `GATE_LINE=1` unreachable by real `Point`s; `const fn crossing_event` (pure comparison) + `saturating_add` counter. Dropped the redundant `race_dir` param (zero callers, sign from `sf.gate.forward` — clean break). Valid-finish conjunction inline `legal_move`-first at the call site (no new predicate; single legality path preserved). Empty-gate = non-panicking no-op. Deferred: the tick/turn driver loop lands with the crash/collision tasks. Closes #8. |
| 2026-07-16 | core-sim-step | ✅ implemented (4 new tests; 74 gp-core green) | core-supercover, core-geom-corridor | gp-core `sim::step` — pure accelerate-then-advance kinematic update (`(vx',vy') = v + accel`, then `pos += (vx',vy')`), shipped as infallible `pub const fn step(s, a) -> CarState` (dropped the unused `d: &Corridor` param; zero callers — clean break). Body uses plain `+` under a fn-level `#[allow(clippy::arithmetic_side_effects, reason)]` — the four adds equal `legal_move`'s `checked_add`-proven chain on the assumed-legal domain (no legality check; that stays `legal_move`'s sole job — single legality path preserved). `const fn` **required** (design-amended, round-2 GO) — `clippy::nursery::missing_const_for_fn` forces it since every body op is const-eval-legal. Adds confirmation tests for the already-live `legal_move` (supercover clip vs clear chord, non-vacuous `d.contains(p1)==true`) and keeps AC4 `legal_mask` `BitFlags<Action>` exactness green via the pre-existing test. Issue's stale `[bool;5]` AC reconciled to `BitFlags<Action>` (#51 already landed). Block 3a, build-order 4/40. Closes #7. |
| 2026-07-16 | finalize-track-artifact-contract | ✅ implemented (17 new tests; 70 gp-core green) | core-geom-corridor | gp-core `TrackArtifact` contract finalized (block 3a, build-order 3/40). Adds `SField { rect, dist }` (total `scalar_at`/`gradient_at`/`tangent_at`; `t̂=normalize(∇s)`, gate cells → `race_dir`, gradient barred at the `separates` cut so no `L→0` leak), `StartGrid` (distinct `v=(0,0)` positions, front-to-back along `−race_dir`), `TimingGate { behind, forward }` on `StartFinish` (symmetric `separates` barrier + explicit `forward: Side`; `TODO(1)` removed), and `Centerline::at` arc-length sampling (wrap-around + total, no panic). Contract **types + read accessors** on hand-filled fixtures; the block-1 generator (Ф7) that *populates* them stays `todo!`. All accessors total → zero production panics. `(f32,f32)` tangents live only in the render/AI artifact (`sim`/`geom` stay integer-only). Closes #6. |
| 2026-07-16 | enumflags2-legal-mask | ✅ implemented (1 new test; 53 gp-core green) | — | gp-core `legal_mask` returns typed `enumflags2::BitFlags<Action>` (`#[bitflags]` + `#[repr(u8)]` on the locked 5-action `Action`) instead of positional `[bool; 5]`; `pub use enumflags2::BitFlags;` re-export (Rust C-REEXPORT) so `gp-ai` consumes `gp_core::sim::BitFlags` with **no** direct enumflags2 dep. `policy_action` mask param migrated; the 2 i32-overflow tests → `BitFlags::empty()`; +1 non-vacuous AC2 membership test. `enumflags2 = "0.7"` direct in gp-core manifest (single consumer). One `#[allow(clippy::use_self)]` (justified — `#[bitflags]` macro-span false positive). Closes #51. |
| 2026-07-16 | code-writer-subagent-effort | ✅ implemented (0 new tests; harness-only, 10 files) | — | New `.claude/agents/code-writer.md` (frontmatter `model: sonnet` + `effort: medium`, inherit-all `tools`) so the code-writing tier's **effort is pinned in frontmatter** — the Agent tool has no per-invocation `effort` param, so the prior inline `model="sonnet"` code-group spawn could not enforce "effort medium". Routes the `/task` code path (`/context-reset` code branch, Step-8 prose, `design`↔`design-review` markers) to `subagent_type="code-writer"` and converts the four inline-editing skills (`/bugfix` Step 5, `/main-ci-failed`/`/pr-ci-failed`/`/pr-commented` Step 4) to delegate code-writing (Mode B: author-not-transcribe, gate, return without committing; orchestrator keeps self-review + commit/push; mechanical fmt/clippy/doc/actionlint one-liners stay inline). Opus/harness path unchanged (inline `model="opus"`, inherited effort). `claude-tools-hierarchy.md` updated. No tracking issue (internal harness task). |
| 2026-07-15 | integer-safety-audit | ✅ implemented (7 new tests; 52 gp-core green) | — | Workspace integer overflow/signedness-safety audit (#48, standing — **Refs**, not Closes). 22 flagged sites, all gp-core: `Point::neighbors4`→`saturating_add/sub`, `sim::legal_move` + `walls_from_boundary`→`checked_add` (off-grid⇒emit-wall, NOT saturating); 7 retain-(b) fn-level `#[allow(clippy::arithmetic_side_effects, reason)]` with documented domain bounds + covering tests (`Size::area`, `Rect::index`/`on_border`, `supercover`, 3 graph counters). Enables `clippy::arithmetic_side_effects = "deny"` in root `[workspace.lints.clippy]` (machine-enforced going forward). No `as` casts existed (cast clause vacuous). Scaffolds confirmed clean; scaffold re-audit deferred on #48. |
| 2026-07-15 | library-survey-workflow-tuning | ✅ implemented (0 new tests; harness-only, 14 md/instruction files) | — | Combined harness-tuning PR (5 parts + DOC-10). PART 1: library survey (honest **empty** `gp-core` adopt-now set) + near-term issues #49/#50/#51. PART 2: group-size cap 3→10 (MAX), split threshold >7→>15. PART 3: API Stability reframed **definitive never-publish** (4 sites + AGENTS.md). PART 4: quartzite adoption — file-size ladder (CS-0), `doc-convention` DOC-1..9 rewrite, CI-1 actionlint AXIOM + CI-2/3/4, CS-1/CS-2, deterministic-collections. PART 5: per-group implementor model by change-type (code→`sonnet`/medium, instructions→`opus`/inherited; default-4 groups user-gated). DOC-10: removed 4 dangling `§ Self-sufficiency` refs. Follow-up: #52 (re-verify codebase vs new conventions). |
| 2026-07-15 | core-size-rect-dims | ✅ implemented (13 new tests; 45 gp-core green) | core-geom-corridor | gp-core `Size` / `Rect` value types + unsigned `Corridor` dims (rework, folded into PR #47). Factors box/index math off `Corridor`; deletes the `Corridor::new` `assert!` → **zero production panics**; `Rect::index` total via `checked_sub` + `usize::try_from`; net `#[allow]` 3→0. Answers the PR #47 "why not unsigned?" review comment. |
| 2026-07-15 | core-geom-corridor | ✅ implemented (20 new tests; 32 gp-core green) | core-supercover | gp-core corridor-graph helpers (block 3a, build-order 2/40) — 4-conn `flood_fill`/`component_count`, `bounded_complement_components` (§2 Ф4 infield-hole test), in-`D` geodesic BFS (`CorridorScratch`/`geodesic_bfs`/`geodesic_layers`), `walls_from_boundary` (Ф7). `Wall` → `{ cell, side: Side }`; `geom.rs` split into `geom/{mod,graph}.rs`. Closes #5. |
| 2026-07-14 | import-ci-workflows | ✅ implemented (0 new tests; 12 gp-core green) | — | Bootstrap GitHub Actions CI from quartzite (single `ubuntu-latest` lane) + Dependabot + mandatory Vulkan env-init + MSRV 1.97.0 + `CARGO_BUILD_WARNINGS=deny` + workspace lint tables (pedantic/nursery=deny). No tracking issue. |
| 2026-07-13 | core-supercover | ✅ implemented (12 tests) | — | gp-core exact integer `supercover` predicate (SAT bbox-scan); foundation of `legal_move` + passability oracle. Closes #4. |
