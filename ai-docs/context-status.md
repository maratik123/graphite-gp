# Project context — implementation status log

> Detailed per-issue implementation status for **graphite-gp**, extracted from [`context.md`](context.md) so the orientation page stays under the instruction-file size cap (AGENTS.md § Build & Test). **Read on demand.** All relative links resolve from `ai-docs/` (this file's directory), identical to `context.md`.

## Status (2026-07-18)

- Design: **finalized** (`docs/design.md`), reviewed across 4 rounds.
- Code: **scaffold + geom physics primitives** — module structure, `TrackArtifact` type, and stub APIs in place. `crates/core/src/geom/` (split into `mod.rs` + a private `graph.rs`) implements the exact integer `supercover` predicate (full §3 C4 test table) plus the corridor-graph helpers: 4-conn `flood_fill` / `component_count`, `bounded_complement_components` (the §2 Ф4 infield-hole test), in-`D` geodesic BFS (`CorridorScratch::geodesic_bfs` reusable-scratch visitor + eager `geodesic_layers`), and `walls_from_boundary` (Ф7 dual edges). The box/index math is factored into `Size { width, height }` (unsigned) + `Rect { origin, size }` value types; `Corridor` is `{ rect, cells }` with **unsigned** dimensions — negative dims are unrepresentable, so `Corridor::new` needs no `assert!` and **gp-core has zero production panics** (`Rect::index` is total via `checked_sub` + `usize::try_from`). Every integer op is now **overflow- and signedness-safe by construction** (issue #48): `Point::neighbors4` saturates, `legal_move` / `walls_from_boundary` use `checked_add`, and each deliberately-retained bounded raw op carries a documented domain bound + covering test. `Wall` is `{ cell, side: Side }` (4-way outward `Side`). `legal_mask` returns a typed `enumflags2::BitFlags<Action>` set (re-exported from `gp-core::sim` so consumers need no direct `enumflags2` dep) rather than a positional `[bool; 5]` — issue #51. The **`TrackArtifact` contract is now finalized** (issue #6): `SField` distance/gradient/tangent accessors (`t̂ = normalize(∇s)`, gate cells → `race_dir`, gradient barred at the gate cut), `StartGrid` (distinct `v=(0,0)` positions, front-to-back along `−race_dir`), the `TimingGate` half-grid segment on `StartFinish` (`separates` barrier + explicit `forward: Side`), and `Centerline::at` arc-length sampling (wrap-around + total, no panic) — contract types + read accessors on hand-filled fixtures; the block-1 generator that populates them stays `todo!`. `sim::step` — the pure accelerate-then-advance state advance (`(vx',vy') = v + accel`, then `pos += (vx',vy')`) — is implemented as an infallible `const fn` on the assumed-legal domain (no legality check; that stays `legal_move`'s sole job), joining the already-live `legal_move` / `legal_mask` path (issue #7). `LapCounter::register_move` — the signed **half-open S/F crossing test** over the half-grid timing gate (`+1` forward / `−1` reverse, ≤1 event/move via a doubled integer perpendicular coordinate whose odd `GATE_LINE` value is unreachable by real `Point`s), with the `legal_move`-first valid-finish conjunction expressed at the call site — is implemented (issue #8). `sim::resolve_crash` — the finalized **quench-with-scrub crash rule** (a search dead-end where `legal_mask` is empty): respawn at the last swept cell still in `D` (a projection-ordered `supercover` walk, no second geometry path), zero the into-wall velocity component and damp the along-wall component to `⌊t/2⌋` (a concave corner zeros both), plus a one-tick forced-`Coast` **scrub** marker and a whole-vector-halving **fail-safe** (guarded by an `L∈D` termination check; inherits `supercover`'s bounded-chord precondition) that never yields a penalty-free controlled `v=0` — is implemented as a pure `resolve_crash(d, s) -> CrashOutcome` whose `action_mask` / (total) `consume_scrub` make the scrub tick unit-testable inside `gp-core` (issue #9). `sim::resolve_collisions` — the **same-final-cell collision resolver** (issues #10 + #49): cars ending a turn on the same cell are bucketed, a seeded `rand_chacha` ChaCha8 RNG (canonical `sort_unstable_by_key(|g| g[0])` order → `groups.shuffle` → per-group shuffle → `u32` equidistant tie-draw, cross-arch-reproducible) picks the winner + displacement order, and each loser teleports to the nearest free cell via the in-`D` geodesic BFS (velocity retained, no supercover/lap-counter touch, single occupied-after-each pass). Per a **product-owner amendment (2026-07-16)** the predicate is same-final-cell **only** — the swap/pass-through detector (`docs/design.md` [D1]/[N2]) was dropped, so swaps / mid-segment / orthogonal crossings ending on **distinct** cells are allowed, and the two canonical docs were corrected to match. The tie-draw is total (`u32::try_from(..).unwrap_or(u32::MAX)`) so **gp-core keeps zero production panics**. A follow-up **PR #68 review amendment** changed `resolve_collisions` to take a **caller-owned physics RNG handle** (`rng: &mut ChaCha8Rng`, drawn from but never re-seeded) rather than a per-call `seed: u64`, establishing a **per-domain seeded-RNG architecture** — one long-lived stream each for physics / track generation / AI learning / AI inference (the AI streams and the block-3b game-loop stream ownership are deferred). Issue #49 also adds the `rand` + `rand_chacha` (`0.10`, `default-features = false` ⇒ no `getrandom`/OS entropy) stack to **both** `gp-core` and `gp-gen`, with a seeded `GenParams::rng() -> ChaCha8Rng` (gp-gen's generator stochastic usage stayed deferred at the time — #49's remainder, **since closed by the Ф1 PR below**). **97 gp-core + 2 gp-gen unit tests** (as of that PR). The `gen` / `render` / `ai` algorithms are still `todo!()` (marked `TODO(<block>)`). Whole workspace builds clean.
- **Visual base:** the Claude Design "Graphite GP Design System" is imported to [`docs/design-system/`](../docs/design-system/IMPORT.md) and adopted as the canonical visual language; render target is a **native Rust GUI** (design tokens/components are a spec to port, not runnable web code). The backend is **eframe/egui 0.35** (#11) — chosen because its widget layer makes #13–#16 token-styling tasks rather than from-scratch toolkit work; rationale + rejected alternatives in [`key-decisions.md`](key-decisions.md).
- **Golden-image testing (block 2, from #11):** `gp-render` ships a committed golden PNG (`crates/render/tests/snapshots/placeholder.png` — the repo's **first binary asset**, plain git, no LFS) rendered offscreen through `egui_kittest`'s wgpu/Vulkan harness. Three mechanisms guard it, each catching what the others cannot: **exact compare** (`threshold(0.0)` + `failed_pixel_count_threshold(0)` — both must be overridden; the `0.6` default silently permits colour distance) catches *regressions*; **AC9 pixel probes** catch *degeneracy*; and the **`image-check` subagent** (spawned by `code-writer` at mint/regen only, **never CI**) catches *a golden that was wrong from birth*. Exactness is bit-exact **in flat regions only** — `egui_kittest` hardcodes dify's `detect_anti_aliased_pixels`, so AA-classified edge pixels differ silently with no diff artifact; accepted, revisit when dx12/metal lanes join the CI matrix. Adapter is asserted `DeviceType::Cpu` (llvmpipe), so a bit-exact golden can never be minted against a discrete GPU.
- **Design tokens + fonts (block 2, #12):** `gp_render::tokens` ports all **127** CSS tokens from [`docs/design-system/tokens/`](../docs/design-system/tokens/) to module-level `SCREAMING_SNAKE_CASE` consts — one submodule per CSS file (`color` 56 / `spacing` 30 / `typography` 26 / `effects` 15), of which **117 are value-checked against the CSS text itself at test time** by a shared `#[cfg(test)] mod css` parser, and **10** sit in a documented exclusion table (the three `--font-*` stacks, three `cubic-bezier` eases, two CSS-gradient recipes, `--shadow-inset`, `--text-eyebrow-transform`). The inventory test asserts the 127 partition, so a token added to the CSS later cannot go silently unported. Units are **not** uniform — px→logical points, but `--ls-*` is an em ratio, `--lh-*` a unitless ratio, `--fw-*` an OpenType `wght` axis value; see the module doc's unit table before using a bare `f32`. `gp_render::fonts::definitions()` returns an `egui::FontDefinitions` registering **7 weight instances** from **2 vendored variable `[wght]` faces** (**Onest** 400/500/600/700, JetBrains Mono 400/500/700; **304 KiB** total, OFL-1.1, no Reserved Font Name, each beside its `OFL.txt`), built on **`FontDefinitions::empty()`** — #12 built on `default()` precisely to keep egui's bundled fallbacks alive behind a Latin-only display face; **#73 removed the reason and then the fallbacks** (see the #73 bullet below), so the builder now starts from `empty()` **explicitly**. That word matters: with `default_fonts` off, `default()` **is** `empty()` (`epaint fonts.rs:496-502`) — the collapse is silent and compiles either way, so the explicit call is what keeps the intent readable. `gp-game` installs it; **`gp-render` still constructs no `egui::Context`** (AC13 holds). Traps worth not rediscovering: `VariationCoords` is reachable only as `egui::epaint::text::VariationCoords` (not re-exported at egui's top level); a variable face's `wght` axis default is **not** whatever weight you meant to register (Space Grotesk's was **300** → bare registration rendered Light; Onest's is **400**), so explicit `FontTweak::coords` stays **mandatory** — #73 kept it and says so, because "the default is now 400, so we can drop the override" is exactly the plausible-but-wrong inference the axis invites; and `clippy::float_cmp` **suppresses itself by enclosing-fn name** (`*_eq` / `eq_*` / `is_nan`), so the CSS comparator must stay named `assert_f32` or its `#[allow]` goes inert. **Gap closed — #73 (2026-07-17):** Space Grotesk had **zero Cyrillic** while the design system's own `Ф1–Ф7` phase labels (`ui_kits/game/Screens.jsx`) contain `Ф` (U+0424), so `Ф1` would have rendered in two typefaces (`Ф` from egui's Ubuntu-Light, `1` from Space Grotesk). **Onest** replaced it — OFL, no RFN, variable, full Cyrillic, and **12 KB smaller** than the face it replaced. Fixing the face removed the *reason* egui's bundled fallbacks were load-bearing, so `default_fonts` went off in the same task: **−1,443,320 B (1.376 MiB) measured** (25,488,456 → 24,045,136), within 1.19% of prediction. Two coupled changes, one task, because Onest is what made dropping the defaults safe.
- **Onest swap + `default_fonts` off (block 2, #73):** the font stack is now **Onest + JetBrains Mono and nothing else** — egui's four bundled faces (Ubuntu-Light, Hack, NotoEmoji, emoji-icon-font; 1,414,020 B) are gone, via the **feature flag only** (`epaint_default_fonts` is an optional crate gated by `epaint/default_fonts`; removing map entries at runtime would not shrink the binary). Five things here are load-bearing and none of them look it:
  - **Empty family ≠ missing family.** `FontDefinitions::empty()` binds exactly two keys (`Proportional`/`Monospace` → `vec![]`); a `FontFamily::Name(..)` is bound **not at all**. Empty → `GlyphInfo::INVISIBLE` (**silent**); missing → `panic!` (`epaint fonts.rs:1027-1031`) (**loud**). Two paths ~380 lines apart in one file with **opposite** failure modes — a design doc conflated them and shipped a provably-panicking test sequence past one review round.
  - **The wgpu harness goldens' early `return` is the panic defence, not dead code** (e.g. `app_shell_matches_golden` in `app_gallery.rs`; the `*_gallery` screen/widget goldens share the idiom). `egui_kittest`'s `Harness::from_builder` runs the app closure at *build* time against a `None` ctx (`builder.rs:189,239`), so a post-build `set_fonts` is **unreachable**. The test installs fonts *inside* the closure, returns before drawing on frame 1 (a `fonts_installed` latch), draws from frame 2, then `run_steps(1)` — **not** `run()`/`run_ok()`, which loop on repaint requests. Deleting the `return` reproduces `FontFamily::Name(..) is not bound to any fonts`.
  - **`gp-game`'s `winit` dep is never `use`d and must not be deleted.** `"winit/default"` **cannot** be written in a dependent's inline `features` array (slash syntax is legal only inside a crate's *own* `[features]` table — `cargo metadata` rejects it), so the direct dep carries `wayland-dlopen` + `wayland-csd-adwaita` to the single shared winit via feature unification. Deleting it **compiles, passes clippy, and silently** demotes Wayland CSD to `FallbackFrame`. `unused_crate_dependencies` is configured nowhere; **the `Cargo.toml` comment is the only durable guard** (the AC18 probes ran once, at implementation time).
  - **gp-render's two Miri-gate *causes* are distinct, and both now live in `app_gallery.rs`** (the vestigial `placeholder.rs` that once held them was deleted with #23 once its `draw_placeholder` scaffold had zero production callers; the `tessellation_smoke` canary was relocated verbatim, repointed onto `AppShell::show`). A **wgpu golden** (e.g. `app_shell_matches_golden`): FFI/`dlopen`s the Vulkan ICD. `tessellation_smoke`: since it draws text, it hard-panics on `bytemuck::cast_slice_mut::<u8,u32>` in `vello_cpu`'s `copy_solid` (Miri's allocator grants `u8` alignment; native malloc over-aligns) — **not** UB, and **not** the same reason. Copying one justification onto the other would be a false justification. Under Miri, gp-render therefore exercises only its **pure production-logic** tests (`fonts.rs`, the track/widgets `const fn resolve` + geometry layers, the icon CPU-bakes) — its `tokens::*` constant-vs-CSS tests, the `#[cfg(test)]` `tokens::css` parser self-tests, and the `test_util` float-assert self-tests are **all Miri-gated too** (**B.1 / #107**, 2026-07-21): zero-production-UB-signal, cost-not-abort (`~194 s` of interpreted wall-clock, no UB signal — see AGENTS.md § Rust Test Conventions).
  - **`Fonts::has_glyph` is unsound as a coverage oracle** — it is `resolve_face(c) != replacement_face_key`, where the replacement face is the first carrying `◻`, and `◻` U+25FB is absent from **both** vendored faces. Glyph-coverage assertions go through **`skrifa`** (already in-graph as epaint's own dep; free as a dev-dep).
- **`.gitattributes` — vendored binary/text assets (from #12):** the repo now carries `crates/render/fonts/** -text`. This is **load-bearing, not cosmetic**: `core.autocrlf` set in any contributor's personal `~/.gitconfig` (it is global-only on at least one dev machine, never in-repo) rewrites CRLF endings **at `git add` time**, which silently committed Space Grotesk's `OFL.txt` as 4,402 B instead of its upstream 4,495 B — breaking both the SHA-256 pin and the OFL's redistribution requirement, with no error. Because the setting is per-contributor, the committed bytes otherwise depend on *who* runs `git add`; `.gitattributes` is the only **committed** lever. It need only **exist on disk** (not be committed) to take effect, so it must land **before** any asset is staged. Vendoring pin: `google/fonts` @ `389b770410cc0b7c21c85673bfa2077420fe7f65`, SHA-256 + git-blob per file, recorded in the #12 design. **Any future vendored asset must extend this file before being staged.**
- **SVG icon pipeline (block 2, #88):** `gp_render::icons` bakes Lucide SVGs → `egui::TextureHandle`, porting the *bake half* of the sibling `marshrutka` project's `emoji.rs` (`usvg::Tree::from_data` → `resvg::render` into a `tiny_skia::Pixmap` → `ColorImage::from_rgba_premultiplied` → `ctx.load_texture`). Adds `resvg 0.47` + `tiny-skia 0.12` (`default-features = false`); the direct `tiny-skia` edge is **feature-inert** — `resvg` forces `tiny-skia`'s default `png-format`, so `png` is an **unavoidable link-only transitive** (accepted; we never decode PNGs), while the droppable font/raster codecs (`fontdb`/`rustybuzz`/`ttf-parser`/`gif`/`image-webp`/`zune-jpeg`) stay absent — verified with `cargo tree -p gp-render -e no-dev` (which excludes a **dev-only** `tiny-skia 0.11.4` reaching the crate via `egui_kittest → winit → sctk-adwaita`). Five Lucide icons (`play`/`pause`/`grid-3x3`/`zoom-in`/`settings` — the core specimen's IconButton row) are vendored under `crates/render/icons/` (byte + SHA-256-pinned at Lucide tag `1.25.0`; `.gitattributes` extended with `crates/render/icons/** -text`), and **ISC** added to the crate SPDX. The bake is **fallible** (`Result` + `IconError`, not marshrutka's `.unwrap()` chain), so `gp-render` keeps **zero production panics**; it is the crate's **first `Context`-touching** surface (still no window ownership). Miri: `settings.svg` @ 18 px trips a `tiny-skia` `simd` scanline over-read **under Miri only** (native clean), so that one `IconSet::new` test is `#[cfg_attr(miri, ignore)]`d with a mechanism-specific reason; the two CPU-bake raster tests are genuinely Miri-clean. Prerequisite carved out of **#13** (core widgets); now that #88 has merged, **#13 is landed** — see the next bullet.
- **Core widgets (block 2, #13):** `gp_render::widgets` ports the five design-system core components — Button, IconButton, Badge, Tag, Card — to native `egui` widgets styled entirely from `crate::tokens`. Each widget is **three layers**: a pure `const fn resolve(state) -> …Style` mapping (Miri-clean — no `egui::Context`/text/alloc — the AC7 unit-test backbone; `clippy::missing_const_for_fn` nursery-deny *forces* the `const fn`, so radii return a raw `f32` and defer `CornerRadius::from` to paint, same non-const class as `car_color`), a private `paint(painter, rect, &style)` layer, and a public `show(ui) -> Response` interaction shell (hover/press from live `egui` input; toggle/selected/active caller-owned). Icon slots take a **pre-baked `&egui::TextureHandle`** from #88's `IconSet` (not an `Icon`, keeping widgets icon-source-agnostic; single 18 px baked size). The two non-token colors become consts — ghost hover/press overlays in `widgets/common.rs`, Badge ok/warn tinted fg in `badge.rs`. AC8 is a `#[cfg(test)]` wgpu **gallery golden** over the full variant/size/state matrix (`#[cfg_attr(miri, ignore)]`, image-check-verified at mint, driven through the private `paint` layer with forced states). `gp-render` still owns no window and keeps **zero production panics** (all `expect`/`panic!` are inside the `#[cfg(test)] mod gallery`).
- **Forms widgets (block 2, #14):** `gp_render::widgets` adds the four design-system **forms** components — Slider, Switch, SegmentedControl, Stepper — on the identical #13 **three-layer** pattern (pure `const fn resolve` → private `paint` → public `show`), styled entirely from `crate::tokens`. Value logic lives in / is reachable from the pure layer: `stepped`/`toggled`/`dec_disabled`/`inc_disabled` are `const fn` (`stepped` clamps with manual `if` + `saturating_add`/`saturating_sub` because `i32::max`/`min`/`Ord::clamp` are `E0658` inside a `const fn` on 1.97), while Slider `snap_clamp`/`fraction` and SegmentedControl `selected_index` stay plain `fn` (nursery `missing_const_for_fn` *declines* on float methods / str-compare). Slider's `changed` uses `f32` **bit-equality** (`new.to_bits() != value.to_bits()`) to stay `clippy::float_cmp`-clean, and its `f32` value tests use a tolerant `abs < 1e-5` compare (the crate's `css::assert_f32` is exact `assert_eq!`). Each widget returns a per-widget `#[derive(Debug)]` response struct (`{ response, <value>, changed }`, mirroring `TagResponse`); builders stay `Copy` (Slider takes its `format` closure as a `show` param). Two GO-note fidelity fixes vs the `.jsx` ground truth: Switch knob-on x is the hard-coded `KNOB_ON_X = 20.0` (`Switch.jsx:33` `left: checked ? 20 : 2`, asymmetric — not a symmetric inset), and the Slider thumb carries a `SHADOW_1` drop shadow (`Slider.jsx:45`). AC8 is a **second** `#[cfg(test)]` wgpu **`forms_gallery` golden** (`#[cfg_attr(miri, ignore)]`, image-check-verified at mint, driven through the private `paint` layers with forced states). `gp-render` still owns no window and keeps **zero production panics**.
- **Game HUD widgets (block 2, #15):** `gp_render::widgets` adds the three design-system **game** readout components — Telemetry, LapMeter, CarChip — on the identical #13/#14 **three-layer** pattern (pure `const fn resolve` → private `paint` → public `show`), styled entirely from `crate::tokens` (zero new tokens; zero per-widget non-token colors). Telemetry is a mono label+value metric with six semantic tones, sm/md/lg value sizes (`FS_TITLE`/`FS_H3`/`FS_H2`), optional unit, `left`/`right` align, and — the **one field beyond the `.d.ts`** — an **`on_ink` render mode** (Q1) that swaps the `default`-tone value → `PAPER_0` and label/muted → `TEXT_FAINT` so the SPEED/v/POS/TEMPO HUD strip reads on the graphite panel; the binding is the card's literal `#FBF8F0`/`#A69D8C` bytes (**not** `TEXT_ON_INK`=`PAPER_1`), applied **uniformly** — the coherent self-minted golden, not the card's per-widget style inconsistency. LapMeter clamps a signed `lap` to `[0,total]` in a pure `const fn` (i32/i32, comparison-only — no casts) and fills the first `done` of `total` `ACCENT` cells above a mono `done/total` readout. CarChip renders a car-ramp dot (default `CAR_1 == ACCENT`), UI-face name, optional mono rank, a `YOU`/`AI` kind pill, and active-vs-resting chrome (`PAPER_2`/`BW_2` vs `PAPER_0`/hairline). AC8 is a **third** `#[cfg(test)]` wgpu **`game_gallery` golden** (`#[cfg_attr(miri, ignore)]`, image-check-verified at mint) laid out to `game.card.html` (MovePad out of scope). `gp-render` still owns no window and keeps **zero production panics** — the widgets' `# Panics` document egui's internal font-lookup precondition (not a direct panic site, matching Badge/Tag); every `expect`/`panic!` is inside `#[cfg(test)] mod game_gallery`.
- **MovePad control (block 2, #16):** `gp_render::widgets` adds the signature game control — the 5 von-Neumann accelerations as a plus-shaped keypad (Coast center, `↑ ↓ ← →` around; the 4 corners empty, diagonals structurally impossible) — on the identical #13/#14/#15 **three-layer** pattern (pure `const fn resolve` → private `paint` → public `show`), styled entirely from `crate::tokens` (zero new tokens; the sole non-token color is the shared `common::GHOST_PRESS_OVERLAY`). It consumes `legal: BitFlags<Action>` from `gp_core::sim::legal_mask` (the **existing** gp-core edge — no new crate dependency) to disable illegal cells, takes `selected: Option<Action>`, and returns `MovePadResponse { response, selected, changed }` (`SegmentedControl`/`Switch` precedent) — clicking a legal cell selects its `Action` and flags `changed`; illegal cells and empty corners are no-ops. Each cell draws its arrow glyph (`JETBRAINS_MONO_BOLD`) + `a,b` sublabel (`JETBRAINS_MONO_REGULAR`); the arrows/coast (`↑ ↓ ← → ·`) were verified present in **both** vendored faces (skrifa/cmap probe — the `✓`-tofu caution did not extend to arrows, so no painter-drawn fallback), and cells are `SPACE_1`-spaced (the `.jsx` `gap:4` ground truth, owner-confirmed over the spec's initial `BW_1`). Pressed darkening reuses `common::GHOST_PRESS_OVERLAY`. AC7 is a **fourth** `#[cfg(test)]` wgpu **`movepad_gallery` golden** (`#[cfg_attr(miri, ignore)]`, image-check-verified at mint) over the legal/illegal/selected/pressed matrix — a new module, since `game_gallery` excluded MovePad. `gp-render` still owns no window and keeps **zero production panics** (the widget's `# Panics` document egui's font-lookup precondition, matching #13–#15). A **deferred cleanup** is recorded in the design: the wgpu golden harness is now copy-pasted across **four** gallery modules and wants a shared in-crate `#[cfg(test)]` test helper (scope intentionally not expanded here).
- **Track canvas (block 2, #17):** `gp_render::track` turns `render_frame` (was `todo!()`) into the real design-doc §4 hero canvas, drawing layers **1–3 + 6** back-to-front over the corridor `D`. **Regions:** outfield `PAPER_1`, asphalt = union of unit cells over exactly `D` in `ASPHALT_1`, infield (the bounded ¬D hole) in `SURFACE_INFIELD` — the infield is derived **in-crate** by a complement flood over the corridor bbox (no gp-core change). **Walls:** the half-grid fill boundary from `walls_from_boundary`, chained into ordered loops (edges arrive **unordered**) and then **cosmetically Chaikin-smoothed within the half-cell gap** (Q1) under an M6 guard — no generated vertex may enter a drivable cell or change `D`, and the raw unsmoothed stroke is the always-correct fallback, so AC3 (walls never cross an integer point) never depends on AC7. **S/F:** a checkered chord along `sf.chord`, alternating `GRAPHITE_900`/`PAPER_0` cells. **Cars:** a `GRAPHITE_900`-outlined color-ramp dot, a `Painter::arrow` velocity vector whose direction/length ∝ `(vx,vy)`, a fading trail, and an optional `Shape::dashed_line` `ACCENT` "you" ring (dash/gap 2/3, `Track.jsx:101` — **not** the `2 6` ideal-line at `:76`). The **move animation** is a pure linear `lerp((x,y),(x+vx,y+vy),t)`; reduced-motion snaps to the final position. Per-car render input (`CarRender { state, color_index, trail, you, progress }`) is **caller-supplied** — `gp-render` stays draw-only, buffering no history or clock (`gp-game` owns the clock). Each layer keeps the crate's pure-geometry → `paint` split (geometry is Miri-clean, unit-tested; the transform / trail fade / lerp are the only sub-cell math, all in gp-render — the physics core stays integer-only). **Q2 → grid (layer 4) + analytics overlays (layer 5: speed heatmap, fastest-lap/ideal-line) were deferred here**, the existing `Overlays { speed_heatmap, fastest_lap, grid }` flags threaded **inert** (no-op) — **now made real in #18** (see next bullet). AC8 is a **fifth** `#[cfg(test)]` wgpu **`track` golden** (`#[cfg_attr(miri, ignore)]`, image-check-verified — the golden-harness copy-paste is now at five sites, still deferred). `gp-render` keeps **zero production panics**. Trap worth not rediscovering: an exact float `==` on a `sqrt`-derived magnitude (`Vec2::length`) diverges ~2 ULP native-vs-Miri and reds the workspace Miri gate though `cargo test` is green — use a tolerance for `sqrt`/`hypot`/`length` comparisons, exact only for linear/integer-valued f32.
- **Analytics overlays + notebook grid (block 2, #18):** `gp_render::track` makes the three `Overlays { speed_heatmap, fastest_lap, grid }` flags **real** (design-doc §4 layers 4–5 + the notebook sheet), retiring #17's inert scaffolding (`overlays_are_inert` deleted, `_overlays`→`overlays`, `LAYER_ORDER` then the 9-entry list — later replaced by the `Layer` (7-variant) + `regions::RegionLayer` (3-variant) strum `EnumIter` enums that `draw_frame`/`regions::fill` now iterate for enum-driven dispatch, with a flattened-names guard replacing the self-referential `layer_order_is_documented` pin, #121). **speed_heatmap** recolors the asphalt per-cell on the 4-stop `HEAT_0`→`HEAT_3` ramp (per-track `[min,max]` normalization) — and crucially **reuses PR #100's smoothed asphalt mesh**: each cell's color is drawn through `Painter::with_clip_rect` over a **triangulate-once** shared mesh, then the infield hole is re-cut, so the heatmap's silhouette traces the **same Chaikin-smoothed boundary the walls trace**. (An owner correction fixed a first cut that stamped **blocky per-cell squares** — the #17 fill-vs-stroke corner-artifact class recurring; the fix, `regions::{triangulated_loop, paint_mesh, paint_infield_holes}` factored out of `fill`, is what shares the boundary.) **fastest_lap** draws `metrics.fastest_lap` as a thin dashed `ACCENT` **uniform** Catmull-Rom spline over the asphalt (uniform, not centripetal — no `sqrt`/divide-by-zero NaN), pure-visual (never touches `D`/metrics). **grid** is the whole-canvas 'notebook sheet' — faint `GRID_LINE` ruling at the lattice cell pitch (`transform.cell_size()`, not the fixed 24 px decorative token), a heavier `GRID_LINE_MAJOR` every 5th, and a `GRID_DOT` lattice at cell corners. Each flag toggles independently; **all-off is byte-identical to #17** (each overlay `paint` runs only inside `if overlays.flag`, so the existing `track.png` golden needs no re-mint), and empty metrics → heatmap/fastest-lap no-op. Three new `#[cfg(test)]` wgpu goldens (`track_heatmap`/`track_fastest_lap`/`track_grid`, `#[cfg_attr(miri, ignore)]`, image-check + owner-visual). **No new dependency, no new token** — every color (`HEAT_*`, `GRID_*`, `ASPHALT_1`, `ACCENT`) already existed. `gp-render` keeps **zero production panics**. Recurring trap re-confirmed: `image-check` PASS + exact-compare is not visual-correctness — a fill layered over a stroked boundary must reuse that **same** boundary, checked by-eye against the baseline.
- **Setup screen (block 2, #19):** the first full **screen** lands in a new `gp_render::screens` module — screens are production `pub` code, so they sit in their own module, not the `#[cfg(test)]` widget *galleries* under `widgets/`. `SetupScreen` composes the shipped widgets (porting `ui_kits/game/Screens.jsx`'s `SetupScreen`): the two-tone **GRAPHITE GP** wordmark (display face `ONEST_BOLD` @ `FS_H1`, accent `GP` — **two** `painter.text` runs offset by the first run's measured advance, since egui has no rich-text span) + accent dot + mono `GRID VECTOR RACING` subtitle, then a gridded `Card` holding cars/laps `Stepper`s (bounded **2–6 / 1–9**), a Rookie/Pro/Ace difficulty `SegmentedControl`, a **3–10 integer `V_target`** `Slider`, a primary 'Generate track' `Button`, and a mono footer — every gap/padding snapped to a named `spacing::SPACE_*` **4-multiple** (AC7 spacing map). The button click emits a `Copy` `RaceConfig { cars, laps, v_target, difficulty }` via `SetupResponse { response, config, generated }` (the crate's widget-response idiom); `generated` is **false until pressed**. **Difficulty→temperature** is a pure FORCED-`const fn` (placeholders Rookie 1.5 / Pro 1.0 / Ace 0.6, **Ace lowest** per §5), real values deferred to Block 4 (the single edit site when tuned). `RaceConfig`/`Difficulty` stay **`gp-render`-local** — the crate depends on neither `gp-gen` nor `gp-ai` and this is a single emit-only consumer, so **no shared crate** (revisit when `gp-game`/`gp-gen`/`gp-ai` consume it). **`V_target` is the design input [D3]**, an integer in `[3,10]` — explicitly **not** `V_ceil`/`GenParams.v_ceiling` (the oracle's float BFS bound); the two must not be conflated (design-doc §2 L35). Two real bugs surfaced at mint: (1) `Card::show` paints its chrome onto `LayerId::background()` — the **same** layer the root `Ui` (both `eframe::App::ui` and `Harness::build_ui`) sits on — so on a shared background the opaque card fill **occluded its own content** (first golden minted an empty card); fixed by giving `SetupScreen::show` its own `Order::Middle` child layer (this will matter for `gp-game`'s integration too, not just the test). (2) `SetupResponse.generated` is a **one-shot per-pass** flag and egui runs multiple passes per `step()`, so a "latest frame" `Cell` missed the click pulse — the interaction test **OR-accumulates** the flag across passes. Two new tests: a **sixth** `#[cfg(test)]` wgpu golden (`setup_screen`, `#[cfg_attr(miri, ignore)]` for the Vulkan `dlopen`, image-check-verified) and an `egui_kittest` **interaction test** (default non-wgpu harness, no `render()`) that clicks `resp.response.rect.center()` to exercise the click→`generated` flag + widget→`assemble`→config plumbing the input-less golden cannot reach — miri-ignored for a **different, measured** reason (`Harness::builder()` calls `getcwd`, blocked by Miri isolation), not a copy of the golden's Vulkan reason. `gp-render` keeps **zero production panics** — the 3 new `expect`s (button-row unconditional; clamped `u32::try_from` ×2) are registered in [`panic-index.md`](panic-index.md).
- **Track lab screen (block 2, #20):** the second full **screen** — `gp_render::screens::LabScreen` ports `Screens.jsx`'s `LabScreen`, pairing the track canvas (`render_frame` with all overlays on) with a VALID/seed header (`Badge`/`Tag` + ghost Menu), an oracle-report `Card` of four `Telemetry` tiles, the Ф1–Ф7 generation-phase list (a `[PhaseStatus; 7]` — the array type-enforces "exactly 7 rows"; `Ok`→(`Tone::Ok`,"✓") / `Repair`→(`Tone::Warn`,"repair") via a FORCED-`const fn phase_badge`), and Regenerate / Test-lap action buttons. **Oracle report is sourced entirely off the caller-supplied `TrackArtifact` the screen already holds** (not a separate report struct — a **round-2 spec amendment** reversed the initial gp-render-local `OracleReport`): Vmax = `track.metrics.vmax_attain`, Tempo = `track.metrics.tempo` (both genuinely `Option` → "—" placeholder), Width min = a **new `gp_core::track::TrackArtifact.width_min: u32` field** (non-`Option`/unsigned: a validated Ф7 artifact always has a measured width `≥ n = ⌈m/2⌉ ≥ 1`, and it is a lattice-point count — a Ф4/§2 **geometry** output, **not** a §3 `TrackMetrics` speed metric), and S/F width = a **new `StartFinish::width()` getter** over `sf.chord.len()` (FORCED `const fn`; getter over a value already on the artifact, no new field). Both width values always render a real number (no "—"). The payoff: once block-1 generation fills `width_min` (#27 Ф4 computes it, #34 assembly stores it) the screen shows real values with **zero screen-side change and no call-site copying** — the tile already reads `track.width_min`; until then `width_min` is hand-populated in fixtures (the `golden.rs::scene_track_with_metrics` pattern). Draw-only as ever: `show` returns `LabResponse` with Regenerate / Test-lap / Menu **click-signal** flags (the `todo!()` `gp_gen::generate` is never called — the screen owns no RNG/seed); the `PhaseStatus` type stays `gp-render`-local (no `gp-gen` dep). Adding the non-`Default` `width_min` field forced updating all 3 `TrackArtifact` struct-literal sites (2 gp-core-test + 2 render fixtures inherit via mutate-a-base). Covered by a sixth miri-ignored wgpu golden (`lab_screen.png`, image-check-verified at mint — the `✓` glyph rendered real, no text fallback needed) plus a miri-ignored `egui_kittest` interaction test (each click flips exactly its `LabResponse` flag). `gp-render` + the gp-core additions keep **zero production panics**.
- **Race screen (block 2, #21):** the third full **screen** and the game's hero view — `gp_render::screens::RaceScreen` ports `Screens.jsx`'s `RaceScreen` as a two-column layout: left = the dark `GRAPHITE_900` HUD strip (three on-ink `Telemetry` tiles bound to the **active car's** live `CarState` — SPEED = `|v|` (`f32::hypot`, 2 dp), `v` = `(vx, vy)`, POS = `(x, y)` — plus a right-aligned on-ink `LapMeter` for laps-done / total), an overlay toolbar (Grid/Heatmap/Fastest-lap `Switch`es toggling the `crate::Overlays` flags + a ghost "Finish →"), and the bordered track canvas (`render_frame`); right = a "Your move" `Card` wrapping the `MovePad` (sized 52, masked by `gp_core::sim::legal_mask(&track.corridor, active_state)`) with a mono caption + full-width secondary "Coast (·)" shortcut, and a "Standings" `Card` of one `CarChip` per car. **Per-car input is a caller-supplied `&[CarRender<'_>]` slice + an `active: usize` index** — the same slice `render_frame` already consumes, so it flows into the canvas with **zero reconstruction**; name/kind/rank/active derive from the index (`CAR_NAMES` = module-level `const [&str; 6]`, ported from JSX; index-0 = player `CarKind::You`/active, others `Ai`, `rank = index+1` — real race-position ranking is **deferred**, no gameplay ranking metric exists yet). All index/name access is total (`.get(...)` with fallbacks — `CarState::default()` for an out-of-range active car), keeping the draw layer panic-free. Draw-only as ever: `show` returns `RaceResponse { action, overlays, finish }` — the selected `Action` (MovePad cell click **or** Coast shortcut, Coast winning ties) drives the caller's `gp_core::sim::step`; the screen calls **no** `step`/`resolve_crash`/`resolve_collisions`/`LapCounter` orchestration (that's `gp-game`, block 3b). **Round-1 → owner-approved spec amendment:** the JSX HUD band makes all readouts legible via per-element light-color overrides, but the Rust `LapMeter` (unlike `Telemetry`) had **no `on_ink` mode** and `TEXT_INK == GRAPHITE_900`, so the laps-done number would render band-on-band (invisible) — a real port gap leaving AC1 visually unmet. The amendment (product-owner approved during Step-7 design-review) added a **minimal in-crate `LapMeter::on_ink`** (a pure `const fn ink_colors(bool)` color-trio resolver mirroring `Telemetry::resolve`: on-ink done = `PAPER_0`, label/total = `TEXT_FAINT`; off-ink unchanged) — the *one* permitted API extension, no new widget crate/type. Covered by a seventh miri-ignored wgpu golden (`race_screen.png`, image-check-verified at mint — the intentional bright-done/faint-total `LapMeter` asymmetry expected, not flagged) plus a miri-ignored `egui_kittest` interaction test (Coast + MovePad-cell clicks emit the `Action`; a rest frame emits none). Keeps **zero production panics** (the two new `Card::show` `.expect()` invariant sites are panic-indexed).
- **Results screen (block 2, #22):** the fourth full **screen** and Block 2's last per-screen unit — `gp_render::screens::ResultsScreen` ports `Screens.jsx`'s `ResultsScreen` as the post-race summary: a mono `RACE COMPLETE` eyebrow + a display-face **"You finished P&lt;n&gt;"** title (`<n>` **data-bound**, not the JSX's hardcoded `P1` — derived from the `You`-kind entry's `rank`, `None`→`P—` placeholder, never a panic), a "Final standings" `Card` of **one `CarChip` per car in caller-supplied rank order** each paired with a mono right-aligned finish time, a summary `Telemetry` row below a hairline divider (`Fastest lap` accent+`s` / `Tempo` default / `Crashes` danger), and a primary "Race again" + secondary "Menu" action row. **Outcome input is two `gp-render`-local `Copy` structs** (no `gp-gen`/`gp-ai` dep, mirroring `RaceConfig`'s precedent): a per-car **`StandingEntry`** slice (`car_index`-indexed identity → name via `CAR_NAMES`, color via `car_color`; `rank`, `kind`, numeric `finish_time`) + a **`RaceSummary`** (numeric `fastest_lap`/`tempo`/`crashes`, formatted at draw time). **Identity is deliberately decoupled from rank** — `car_index` resolves name/color, NOT the iteration index, so a player can finish P3 with `car_index==0`; the AC3 unit test proves this with a fixture placing the `You` car (`car_index==0`) at slice position 2 / rank 3, an assertion the old in-order fixture could not distinguish. Draw-only as ever: `show` returns `ResultsResponse` with distinct Race-again / Menu **click signals** (the screen routes no navigation — `gp-game`'s screen router, #23, does). `rotate-ccw` is unvendored (5 svgs in `crates/render/icons/`, no rotate), so `again_icon` is an optional handle with a text-only default (the `lab.rs::regenerate_icon` precedent); `ResultsScreen` is `Copy` not `Debug` (holds `Option<&TextureHandle>`). Covered by an eighth miri-ignored wgpu golden (`results_screen.png`, 640×610, image-check-verified at mint) + a miri-ignored `egui_kittest` interaction test (Race-again / Menu clicks each flip exactly their own intent). Keeps **zero production panics** (the three array-index sites are all fixed-size arrays at literal in-bounds indices — no bounds check emitted).
- **Render-input consolidation (block 2, #111):** a pure **plumbing refactor** (no behavior change; goldens byte-identical) that groups the frame-immutable inputs previously passed to `gp-render`'s draw entry point and screen builders as loose positional params into cohesive **input structs**, giving the app-shell/router (#23) a clean, stable interface. **Chosen shape is a hybrid** (the one decision the spec deferred to design): a single shared **`Scene<'a> { track, cars, reduced_motion, overlays }`** — the true "frame-immutable scene" concept, consumed by `render_frame(painter, rect, scene)` **and embedded** in `RaceInput` (the only surface besides `render_frame` that draws the canvas) — plus three per-screen required-input structs **`LabInput`** (track/phases/valid/seed) / **`RaceInput { scene, active, laps_done, total_laps }`** / **`ResultsInput`** (standings/summary), since the three screens' required inputs genuinely diverge. Each screen `::new(input)` takes exactly one struct; all four input types derive `Clone, Copy, Debug` and each `::new` is FORCED `const fn` (nursery=deny). **Optional** inputs (icon handles) stay as `const fn` builder setters (spec default); **`reduced_motion` folded into `Scene`** as a required field — its `.reduced_motion()` setter had zero in-tree callers, so removal is a clean break (no compat shim, AGENTS.md § API Stability), zero byte impact. **The one gate-invisible hazard** the design flagged and the impl honored: `RaceScreen`'s canvas must be drawn with the **`draw_toolbar` RETURN** overlays (live toolbar toggle applies same frame), NOT `self.input.scene.overlays` — so `draw_canvas` takes a `Scene` and the `show` call site reconstructs `Scene { overlays, ..self.input.scene }`; static goldens can't catch a regression here (no golden toggles a switch), so it was verified by reading the diff, not a test. Draw-only as ever: no new dependency, no `gp-core`/`gp-gen`/`gp-ai` change (diff touches only `crates/render/**`). All 10 in-tree call sites (4 `render_frame` + 6 screen-builder) updated; existing 232 gp-render tests + all wgpu goldens stay byte-identical. `gp-render` keeps **zero production panics**. Unblocks #23.
- **App shell — top bar + screen router (block 2, #23):** ties Block 2's four screens into one navigable flow. `gp_render::app::AppShell` (new top-level `crates/render/src/app.rs`, the composition root **above** `screens/` — it imports the four screens, so nesting inside `screens/` would invert the dependency) owns only the **`Copy`** cross-transition state — a `Screen` cursor (`Setup`/`Lab`/`Race`/`Results`), the `RaceConfig`, the `Overlays`, and a `has_generated` latch — and **borrows** all `gp-gen`/`gp-ai`/`gp-core`-sourced session data per frame through one `ShellSession<'a>` bundle (draw-only boundary intact: no new dep, no `gp-core`/`gp-gen`/`gp-ai` change). The transition logic is a **pure `apply(&mut self, Nav)` + `can_nav(&self, Screen)`** with **no egui** — which is what lets AC1/2/5/6/7 be plain state-machine unit tests that construct no `Context`/painter and so stay **un-Miri-gated** (only AC3 golden + AC4 click-through touch a `Harness` and carry the gate). **"Menu" is the Setup screen** (matching `App.jsx`); `Lab.menu`/`Results.menu` both route there. **Nav is clickable jumps** (owner decision) — the three top-bar items (`New race`/`Race`/`Track lab`, text-only since `flag`/`gamepad-2`/`flask-conical` are unvendored, the `lab.rs` precedent) jump directly to any screen, **guarded** by the latch: `New race` always enabled, `Race`/`Track lab` disabled (click = no-op) until the first `SetupResponse.generated` — the safe fallback that never routes to Lab/Race with no track. The **`gp-game` binary is wired** (owner decision): `main.rs` replaces `draw_placeholder` with an `AppShell`-driven `eframe::App` fed **hand-built fixture** session data — a `TrackArtifact` from `gp_core::geom::{Corridor, walls_from_boundary}` + hand-populated `TrackMetrics` (the `track/mod.rs::fixture_track_with_metrics` render-safe pattern), **not** `gp_gen::generate` (a `todo!()` stub that would panic at startup — spec corrected 2026-07-22); `main.rs` retains `.set_fonts(gp_render::fonts::definitions())` before the first `shell.show` (RaceScreen/LabScreen panic without fonts). One new binary-only production panic (`i32::try_from(STARTUP_CONFIG.laps).expect(...)`, `laps=5` const) registered in [`panic-index.md`](panic-index.md); `gp-render` itself keeps **zero production panics**. Covered by 5 pure state-machine tests + a ninth miri-ignored wgpu golden (`app_shell.png`, image-check-verified) + a miri-ignored `egui_kittest` click-through (Setup→Lab→Race→Results→Menu). One layout note surfaced: the click-through needs a **960px** canvas (RaceScreen's toolbar swallows the Finish click at 640px — a RaceScreen toolbar fragility, not an AppShell defect; `race_gallery` uses 900px for the same reason).
- **Single-galley text-shaping refactor (block 2, #96):** a **behavior-preserving** sweep across all of `gp-render` so each text run is shaped **once** and the one `egui::Galley` is reused for both measurement and painting, replacing the prior shape-2–3× pattern (a `layout_no_wrap` for width, again for height, then `Painter::text` re-shaping a third time in `paint`). A new **crate-root `mod text;`** holds two `pub(crate)` helpers — `paint_galley` (mirrors `Painter::text`'s exact body minus the shaping: `Align2::anchor_size(pos, galley.size())` → `painter.galley(rect.min, …)` → return the anchored rect) and `paint_galley_override` (same, via `galley_with_override_text_color`, for the dynamic-color sites — button/switch/segmented_control/`app::nav_item` whose draw color isn't known until after allocation). `show`-measured widgets build the `Arc<Galley>` in `show` and thread it into `pub(crate) paint` (the only signature change; the `*_gallery` harnesses call `paint` directly, so each changed signature updates its callers in the same commit); screen/app-shell inline free functions build once locally. **Byte-identical goldens are the safety net** — all 11 exact-compare wgpu goldens stay bit-identical, no snapshot regenerated (AC4); a new Miri-gated `text.rs` test proves `paint_galley[_override]`'s returned rect equals `Painter::text`'s for all six `Align2` anchors. **Already-single-shape draw sites were deliberately left alone** (`stepper`/`slider`/`movepad`/`card`/`setup::draw_mono_label`/`common::paint_form_label`/`icon_button`). **Cross-crate audit was NEGATIVE** — only `gp-render` uses `egui`; `gp-game` delegates every draw to `AppShell::show` and the other three crates have no GUI dep, so no other crate is in scope. Worth knowing (informed the "leave further caching for later" call): **epaint already caches galleys across frames** (`GalleyCache` in `text/fonts.rs`; `begin_pass` keeps every entry drawn last frame), so this refactor removes redundant per-frame `LayoutJob` hashing + `to_uppercase()`/`to_string()` allocations and makes "shape once, reuse the handle" explicit — it does **not** eliminate per-frame glyph shaping (egui already did). `gp-render` keeps **zero production panics** (all new `expect`/`panic!` are inside `#[cfg(test)]`).
- **CI / tooling:** GitHub Actions CI (`ubuntu-latest`) is in place — `changes`-gated format/build/test/clippy/docs + a **required** Miri gate (`miri` job → `miri-pass` aggregator, context `Miri`, #76) + `-pass` branch-protection gates, sccache, and a mandatory Linux software-Vulkan env-init in the `test` job (ready for the block-2 wgpu/Vulkan renderer). Plus Dependabot (cargo + github-actions), MSRV **1.97.1** (`resolver = "3"` retained — virtual workspace), `CARGO_BUILD_WARNINGS=deny`, and a strict workspace lint policy (`clippy::pedantic`/`nursery` = `deny`, `missing_docs`/`broken_intra_doc_links` = `deny`, `clippy::arithmetic_side_effects` = `deny` — issue #48, machine-enforcing integer-overflow safety) in the root `Cargo.toml` + `clippy.toml`, each crate opting in via `[lints] workspace = true`. See [`code-style.md`](code-style.md) § Linter posture.
- **Block 1 (`gp-gen`) has started — Ф1 coarse-block ring + the grouped seeded-RNG config (#24 + #49 closed, #50 discharged):** the first generator phase lands, and with it the project's per-domain seeded-RNG architecture is realized as a concrete type. **`gp_core::rng::Seeds`** (new `crates/core/src/rng.rs`) groups **four independently-seeded `ChaCha8Rng` sources** — `collision` / `generation` / `ai_learning` / `ai_inference` — as four `u64` seed fields with on-demand `*_rng()` accessors (materialize-on-demand, mirroring the old `GenParams::rng()` idiom; not an `EnumMap` since it is never iterated into output), giving one-place UI seed config. `GenParams` now **embeds `Seeds`** and exposes `generation_rng()` (the old standalone `seed`/`rng()` removed — single non-divergent generation path); `gp-core`'s `resolve_collisions` is **re-pointed** to draw from `Seeds::collision_rng()` (signature unchanged — the struct materializes the same `ChaCha8Rng`, so its byte-identical determinism tests hold); the `ai_*` sources are **defined-but-unconsumed** (`gp-ai` is still a stub). **`phase1_coarse_ring(l_min, &mut ChaCha8Rng) -> CoarseSkeleton { ring, hole, dir }`** (new `crates/gen/src/phase1.rs`) builds the coarse annulus: a 2-cell base strip → **even-sublattice 2×2 growth** restricted to the `y ≥ 2` half-plane (keep-out that preserves the base's south **edge** as a straight run ≥ `l_eff`, an *enclosure* invariant — `y ≤ −1` is never a bounded complement component) → hole-fill **before** dilation → **Moore-3×3 dilation** (`ring = dilate_moore(P) \ P`, 4-connected loop, one hole by construction) → run-preserving outward **widen** → a **run-length + connectivity check** (`min_run ≥ 2`, `max_run ≥ l_eff`, `component_count == 1`, `bounded_complement_components == 1`) on the *actual* post-widen ring, with **bounded same-stream retry** then a **guaranteed rectangular-annulus fallback**, making Ф1 **total** (no `Result`, no panic — `gp-core`/`gp-gen` stay zero-production-panic). AC2 is by-construction for the pre-widen ring **then re-verified** post-widen (the design's "widen preserves invariants by construction" claim was falsified in-flight by a seed-48/`l_min=3` pinch regression and amended to checked-with-fallback). Determinism is **cross-platform bit-for-bit** ([N4]/[M3]): every random pick is a fixed-width `u32` index into a `BTreeSet`/sorted-`Vec` candidate list, never `HashSet` order. **#50 (deterministic-order collections) is discharged** here: `ring`/`hole` are `BTreeSet<Point>` (reaching output), so `Point` gains a purely-additive `PartialOrd, Ord` derive — zero new deps (`indexmap` avoided), governed by [`code-style.md` § Deterministic collections](code-style.md). `l_min` is clamped to a documented `MIN..=MAX_COARSE_STRAIGHT` domain so both primary and fallback paths have a bounded work ceiling. **107 gp-core + 27 gp-gen unit tests** (multi-seed property test over 64 seeds + a named seed-48 regression witness + snapshot + fallback-rate/clamp-boundary/forced-exhaustion). `strum` added as a direct `gp-gen` dep for `Side::iter()`.
- **Ф2 rasterize coarse ring → points `D` with width taper (#25):** `phase2_rasterize(&CoarseSkeleton, k, n) -> Corridor` (new `crates/gen/src/phase2.rs`, `pub use phase2::*;`) expands each Ф1 coarse ring cell into a solid `k×k` fine block (union = baseline `D0`; AC1/AC2 by construction, wide zones ≥`2k`), then tapers abrupt outer-wall steps and closes the resulting topology — a **three-stage, deterministic, no-RNG, integer-only, zero-production-panic** pipeline (pure fn of `(skeleton, k, n)`, byte-identical per input; `Ф2` ignores `skel.dir`). **Stage 2a** makes each *outfield-facing* wall **1-Lipschitz** (advances ≤1 fine point/column = 45° ramp, `MIN_TAPER_RUN = 3` the "several"-column floor) via four directional per-scanline upper-envelope passes restricted by the expanded-hole mask `H` — additive (only grows `D`, never fills `H`), so a `Δ = w·k` widen jog (Ф1's only abrupt step, always outfield-facing) spreads over `w·k` columns with no concave niche a supercover can cut (AC4/AC5). **Narrow zones: none carved in round-1** (owner-chosen *geometry-forced* designation) — uniform `k×k` makes every cross-section `≥ k ≥ n` and every S-hairpin cross-gabarit `≥ 3k ≥ 2n+1`, so the forced-pinch predicate is vacuously false on all Ф1 output and AC3's `≥ n` floor holds by construction (`min cross-section == k`; the `n`-carve path is YAGNI, deferred to a future Ф1 that emits sub-`k` arms). **A design-amendment landed mid-implementation** (owner-approved): the multi-seed property sweep over *real* Ф1 output (not just the hand-built one-jog fixture) falsified the design's original "additive taper preserves topology by construction" claim — Stage 2a can bridge a genuine outfield gap between two arms and seal a spurious second bounded complement component (deterministic witness: `phase1_coarse_ring(3, &mut rng(0))`, `k=6/n=3` → `bounded_complement_components == 2`, a 12-cell pocket). Fix = **Stage 2b (absorb-the-pocket):** after 2a, enumerate the bounded components of `¬D` (internal complement-flood, mirroring Ф1's `fill_holes`) and set-drivable every one **disjoint from `H`** — still additive, still no production self-check/panic (it is part of the *construction*, not a repair phase), `O(cells)`, restoring `component_count == 1` ∧ `bounded_complement_components == 1` by construction across the whole N=64 sweep (the primary regression gate) plus a named `seed_0` witness test. AC6 pins an exact post-2b snapshot (count + endpoints + FNV-1a-64 content hash — a literal 3996-point set would blow the file-size cap). **49 gp-gen unit tests** (27 Ф1 + 22 Ф2). Ф2 uses only gp-core `#5` corridor helpers (`Corridor`, `component_count`, `bounded_complement_components`, `supercover`) — no new dep.
- **Ф3 start/finish, accel zone, start grid, timing gate (#26):** `phase3_start_finish(&Corridor, &CoarseSkeleton, m, v_target) -> Phase3Output { d, sf, grid }` (new `crates/gen/src/phase3.rs`, `pub use phase3::*;`) — a **deterministic, no-RNG, integer-only, zero-production-panic, total** phase consuming Ф1's `CoarseSkeleton` + Ф2's fine `D`. It picks a straight coarse run (not a corner, deterministic `(side-rank, fixed-coord, run-start)` tie-break), thickens that cross-section **additively** to width ≥ `m` (grows `D`'s box via `expand_box` when a push would land off-box — never carves, mirroring Ф2's discipline), builds the **two distinct [C2] objects** — the `StartGrid` (`m` distinct implicit-`v=(0,0)` cells across `ceil(m/width)` rows back along `−race_dir`, with a **degrade contract**: a width-capped `D` yields as many distinct front-to-back cells as fit, never a duplicate/¬D cell) and the `StartFinish`+`TimingGate` (`gate.behind` = front-row cross-section, gate line one edge ahead) — and **measures** (test-only, not enforced) the accel-zone (`≥ v_target²/2`) and grid-straight budgets, deferring seed-retry enforcement to the unbuilt Ф4–Ф6 loop. **AC5 crux:** `skel.dir` is a randomly-drawn *abstract label* (Ф1 `choose_dir`), **not** geometric winding, so Ф3 *defines* the forward projection convention (CCW `forward=(inward.y,−inward.x)`, CW negated) — pinned in a required `forward_side` doc comment that Ф7 centerline/AI-reward MUST re-adopt to avoid an inverted sign. Gate ↔ `LapCounter` contract verified: every start cell lands at `gate_coord ≤ 0 < GATE_LINE(1)`, so all cars read `raw()==−1` at `t=0` and a first forward move scores `+1`. **27 new gp-gen unit tests** (all 9 ACs + determinism snapshot + 32-seed totality sweep). `v_target` is unused in production (`let _ = v_target;`) — the budget it parametrizes is test-only per the "measure and expose" posture. Consumes only existing gp-core types (`Corridor`, `Point`, `Side`, `Orient`, `RaceDir`, `track::{StartFinish, TimingGate, StartGrid}`, `sim::LapCounter`) — no new dep.
- **Ф4 static validation — connectivity, single hole, width, finger liveness (#27):** `phase4_static_checks(&Corridor, &CoarseSkeleton, k, n, m, &StartFinish) -> Vec<Issue>` (new `crates/gen/src/phase4.rs`, `pub use phase4::*;`) — a **deterministic, no-RNG, integer-only, zero-production-panic, total** validation pass running four checks in fixed order (connectivity → topology → width → finger liveness) and emitting a typed `Issue` set (empty ⟺ statically valid) for the future Ф6 repair phase. **`Issue`** = `Disconnected` / `BadTopology` (nullary, global) / `Narrow { center, axis, width }` / `NarrowSf { center, axis, width }` / `LostHairpin { tip }`, all `#[derive(Clone, Copy, PartialEq, Eq, Hash, Debug)]` so tests assert an order-independent `HashSet<Issue>`. **Connectivity + topology delegate to the #5 gp-core helpers verbatim** (`component_count != 1` → `Disconnected`; `bounded_complement_components != 1` → `BadTopology` — the helper already counts only bounded ≥1-cell holes, so `!= 1` directly encodes AC2). **Two new reusable integer primitives land in gp-core `geom` (new `crates/core/src/geom/distance.rs`, `pub use distance::*`)** — the home-crate decision (spec Open-Q1): `DistanceTransform` (multi-source 4-conn BFS from the `¬D` frontier; `compute`/`at`/`const fn rect`) and `medial_axis(&DistanceTransform) -> BTreeSet<Point>`, positioned so Ф7's `racing_line(medial_axis(D))` (#28+) is the second consumer with no re-export; they reuse `Corridor`'s private box/index machinery cross-submodule exactly as `graph.rs` does. The **width check** is the load-bearing part and took the design/design-review loop **3 rounds** to get right: **round-1 fixed an unsound "sample only DT-ridge cells" approach** — a narrow neck is a DT *valley* along the corridor axis, so ridge-only sampling (local *maxima*) provably excludes every neck (two-rooms-plus-doorway counterexample) → broke AC3/AC5. The shipped algorithm **samples ALL `D` cells passing the DT pre-filter `2·dt(p)−1 < n`** (sound: `2·dt−1 ≤ w` always, so a truly-wide cell is never skipped), computes `w = min(hrun, vrun)`, and emits `Narrow` **iff `w < n AND w ∈ {2·dt−1, 2·dt}`** — the **DT-consistency test**, which fires only when the cell is genuinely centered in its shorter run (a real perpendicular cross-section), rejecting the inner-corner / taper / staircase false positives that ridge-restriction had existed to suppress. Completeness: at any neck the center cell has `dt = ⌈w/2⌉` so `w ∈ {2·dt−1, 2·dt}` — no sub-`n` section is missed. **`NARROW_SF`** is a dedicated check on the S/F chord — `sf.chord.len() < m` → `NarrowSf`, with **`axis = sf.orient`** (round-2 fix: `sf.orient` IS the chord's own cross-corridor orientation and `chord.len()` is measured along it, so the narrow dimension runs along `sf.orient`, NOT its perpendicular — the earlier "perp of sf.orient" would have handed Ф6 the wrong wall-push direction) and `center = sf.chord.iter().copied().min()` under an `if let Some` totality guard (matching `Narrow`'s min-`Point` anchor convention). **Finger liveness:** `infield_fingers(skel)` extracts coarse-hole peninsulas (degree-1 tip → walk the degree-≤2 chain) and `absorbed(finger, d, k)` reports `LostHairpin { tip }` when the finger's `k×k` fine footprint is **entirely** drivable (arms merged); a finger pinched into a *separate island* is `BadTopology`'s job, not `LostHairpin`'s (complementary, keeping AC7 fixtures clean). `arithmetic_side_effects = deny` honored via `saturating_*` (`2·dt−1`) + bound-documented `#[allow(reason=…)]` (BFS `+1`, counters); all `usize→u32` width conversions are `u32::try_from(..).unwrap_or(u32::MAX)`, never `as`. **Medial-axis output shape (spec Open-Q2):** the strict axis-wise DT ridge (strict vertical-max OR strict horizontal-max) — verified empirically to leave diagonal gaps at rectilinear corners (a rectilinear annulus ridge is **4 disjoint straight strips**, not one closed loop), with corner-bridging/thinning explicitly deferred to Ф7's `racing_line` resample per the design's AC5-consumer reconciliation (the Task-1 test asserts the verified 4-strip shape, not the aspirational "closed loop"). Pure integer BFS/set code, Miri-clean (no FFI/GPU, no `#[cfg_attr(miri, ignore)]`). **32 new tests** (10 `distance.rs` + 22 `phase4.rs`, incl. the four AC7 adversarial fixtures each asserting exactly its intended `HashSet<Issue>`, a sharp single-cross-section neck, a doorway-neck completeness witness, a staircase-taper no-false-positive witness, and AC8 determinism/totality). No new dependency; consumes only existing gp-core types.
- **Next implementation step: block 3a is COMPLETE** — `supercover` (#4), the corridor-graph helpers (#5), the `TrackArtifact` contract (#6), `sim::step` + `legal_move` / `legal_mask` (#7), `LapCounter::register_move` (#8), `resolve_crash` (#9), and `resolve_collisions` (#10) are all landed; **zero open `block:core` issues**. Per the §6 build order (`3a → (1 ∥ 2) → 4`), the next units are **block 1 (`gp-gen`)** — the Ф1–Ф7 generator pipeline (#24–#34) that populates the `TrackArtifact` contract — **Ф1 coarse-block ring (#24) + the grouped seeded-RNG config (#49, now closed; #50 discharged) + Ф2 rasterize-to-`D` (#25) + Ф3 start/finish + accel zone + start grid + timing gate (#26, see its bullet above) have landed**, + **Ф4 static validation (#27, landed — see its bullet above)**, so the next unit is **Ф5 passability oracle (#28/#29)** — and, in parallel, **block 2 (`gp-render`)**, whose foundational GUI-backend scaffold (#11) and **design tokens (#12)** are both now **landed** — eframe/egui 0.35 picked, window/loop standing in `gp-game`, `render_frame` re-signed to take `&egui::Painter`, the golden-image harness in place, and `gp_render::tokens` + `gp_render::fonts` now carrying the whole design-system vocabulary. #12 also discharged the font question `key-decisions.md` assigned to it (#11 deliberately loaded none). Block 2 continues at the **component units (#13–#16)**, which style egui widgets from these consts — the **SVG icon pipeline (#88)** landed first (a prerequisite carved out of #13 so its Button/IconButton icon slots can draw real Lucide icons — `gp_render::icons`, a `resvg`+`tiny-skia` bake ported from the sibling `marshrutka` project), then **#13's core widgets** (Button/IconButton/Badge/Tag/Card in `gp_render::widgets`, styled from `crate::tokens`), then **#14's forms widgets** (Slider/Switch/SegmentedControl/Stepper, same three-layer pattern, second `forms_gallery` golden), then **#15's game HUD widgets** (Telemetry/LapMeter/CarChip, same three-layer pattern, third `game_gallery` golden matching the HUD specimen), and finally **#16's MovePad** (the plus-shaped 5-action von-Neumann control with legality masking, fourth `movepad_gallery` golden). With #16 landed, **all component units (#13–#16) are complete**, the **hero track canvas (#17)** made `render_frame` real (regions/walls/S-F/cars + move animation over `D`), and the **analytics overlays + notebook grid (#18)** then made the `Overlays` flags real (speed heatmap reusing the smoothed asphalt mesh, dashed fastest-lap spline, whole-canvas grid). The **Setup screen (#19)** is the first full **screen** — `gp_render::screens::SetupScreen` composes the widgets and emits a `RaceConfig` on 'Generate track' — the **Track lab screen (#20)** is the second (`gp_render::screens::LabScreen`: track canvas + oracle report off the caller-supplied `TrackArtifact` + Ф1–Ф7 phase list, all draw-only; it added the `TrackArtifact.width_min: u32` field + `StartFinish::width()` getter, populated by block-1 #27/#34 later), and the **Race screen (#21)** is the third and the game's hero view (`gp_render::screens::RaceScreen`: two-column HUD strip + overlay toolbar + track canvas on the left, "Your move" `MovePad`/Coast `Card` + "Standings" `CarChip` `Card` on the right; emits `RaceResponse { action, overlays, finish }` for the caller to drive `sim::step`, draw-only; added the owner-approved minimal `LapMeter::on_ink` extension for dark-band legibility), and the **Results screen (#22)** is the fourth and final per-screen unit (`gp_render::screens::ResultsScreen`: "You finished P&lt;n&gt;" header + Final-standings `Card` of one `CarChip` per car in caller-supplied rank order + a summary `Telemetry` row (Fastest lap / Tempo / Crashes), emitting `ResultsResponse` Race-again / Menu navigation intents, draw-only; outcome fed by a `gp-render`-local `StandingEntry` slice + `RaceSummary`). The **render-input consolidation (#111)** then bundled every screen's frame-immutable inputs into cohesive structs (shared `Scene` + per-screen `LabInput`/`RaceInput`/`ResultsInput`), a pure byte-identical refactor giving the router clean inputs, and the **app shell / screen router (#23)** then tied the four screens into one navigable flow (`gp_render::app::AppShell` — clickable-jump top-bar nav with a generate-gated guard, pure-logic router, `gp-game` binary wired to drive it with hand-built fixture data). Block 2's remaining surface is the live game-screen assembly / turn loop (block 3b) that replaces the fixtures with real generation → sim → AI, deferred until block 1 (`gp-gen`) lands. The **track-geometry caching (#104)** then removed the per-frame O(n³) re-triangulation `render_frame` did every frame: the chained/Chaikin-smoothed loops, outer/hole roles, and — crucially — the ear-clipping triangulation **indices** are all a pure function of the track's *lattice-space* shape (`TrackTransform` is a plain affine map, so triangulation topology is invariant under the render rect), so they are baked **once per track** into a rect-free `gp_render::BakedTrackGeometry { smoothed_loops, loop_roles, triangulated_indices }` (built by `new(&TrackArtifact)`, no `Rect`), **owned by `gp-game`** and threaded in as a shared `&'a` field on `Scene` — per frame `draw_frame` only maps the baked lattice verts → screen (O(n)) and draws with the baked indices. This dissolved the earlier "immediate-mode rect isn't known before `shell.show`" wrinkle: there is **no rect-keyed cache and no invalidation** — a window resize triggers zero rebuild, only a re-map. Draw-only intact (rect-free geometry lives entirely in `gp-render`, built by a pure fn, owned by the caller; no new dep, gp-core's integer-only `TrackArtifact` untouched); behaviour byte-identical (all 9 canvas goldens pass unchanged — a lattice-space triangulation mapped affinely is a valid triangulation of the same silhouette, and egui raw-mesh fills aren't edge-feathered so the internal diagonal choice paints no pixels).
  - **Not 3a** (a recurring mis-attribution — both are tracked under later blocks): the **passability oracle** is block 1 (`gp-gen` Ф5a #28 / Ф5b #29), and the **tick/turn driver loop** — ordering `legal_move` → `step` → `register_move` → crash/collision resolution, then reading `laps()`/`raw()` for the win check — is **block:game** (#43, the full game loop). 3a ships the pure per-move primitives; sequencing them into a running game is block 3b/game.
