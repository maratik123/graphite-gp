**Tag** — a square, grid-aligned label chip. Heavier and more structural than Badge; supports a leading color dot (e.g. a car color) and an optional remove button.

```jsx
<Tag color="var(--car-2)">Rival · Blue</Tag>
<Tag selected>Track 04</Tag>
<Tag onRemove={() => drop(id)}>Filter</Tag>
```
