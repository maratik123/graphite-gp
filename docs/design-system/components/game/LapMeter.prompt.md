**LapMeter** — lap progress shown as a row of cells that fill as laps complete, with a mono `n/total` readout. Driven by the signed lap counter (`laps = max(0, counter)`).

```jsx
<LapMeter lap={2} total={5} />
```
