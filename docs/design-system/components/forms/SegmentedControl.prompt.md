**SegmentedControl** — a compact row of mutually exclusive options; the selected segment fills graphite. Use for difficulty, overlay mode, or track shape.

```jsx
<SegmentedControl value={diff} onChange={setDiff}
  options={['Rookie', 'Pro', 'Ace']} />
```
