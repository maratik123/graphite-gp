**Switch** — a boolean toggle. Use for analytics overlays and options.

```jsx
<Switch label="Speed heatmap" checked={heat} onChange={setHeat} />
<Switch label="Fastest lap" checked={lap} onChange={setLap} />
```
